---
phase: 29.5-platform-model-migration
plan: 08
type: execute
wave: 5
depends_on: [29.5-02, 29.5-03, 29.5-04, 29.5-06, 29.5-07]
files_modified:
  - .planning/BACKLOG.md
  - src/app/
  - ".planning/phases/29.5-platform-model-migration/"
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All Phase 29.5 code changes (Plans 02–04) lint clean (ESLint, 0 errors)"
    - "All Phase 29.5 code changes type-check clean (tsc, 0 errors)"
    - "All Phase 29.5 tests pass (100% of modified service specs green)"
    - "All Phase 29.5 upstream schema changes merged (Plan 05)"
    - "All Phase 29.5 UAT verifications passed (Plans 06–07)"
    - "Phase 29.5 closure artifacts created and reviewed"
  artifacts:
    - path: .planning/BACKLOG.md
      provides: "Conditional + post-closure backlog entries added"
      contains: "PROVIDER-MY-ENGAGEMENTS-1|PROJECT-SVC-RENAME-1|SCHEMA-RETIREMENT-DELETE-1"
    - path: ".planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md"
      provides: "Phase closure checklist + summary (artifact, not code)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 29.5 marked complete; Phase 30 updated"
  key_links:
    - from: "All prior plans (29.5-01..07)"
      to: "Final verification"
      via: "CLOSURE.md checklist validates all outputs"
      pattern: "CLOSURE|COMPLETE|verified"
---

<objective>
Final cleanup, verification, and closure for Phase 29.5. Confirm all code changes lint/type-check clean, all tests pass, all UAT verifications completed, all upstream changes merged, and backlog entries created. Create phase closure artifact + update ROADMAP.

Purpose: Phase 29.5 is a large model-migration phase with upstream dependencies (schema), downstream dependencies (Phase 30), and risk-gated verifications (Plans 06–07). Final cleanup ensures no regressions slip through + project state is ready for Phase 30.

Output: Lint/tsc/test green, CLOSURE.md, updated BACKLOG.md, ROADMAP.md updated, all SUMMARY.md files collected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md
@.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md
@.planning/phases/29.5-platform-model-migration/29.5-01-PLAN.md through 29.5-07-PLAN.md (all prior plans)
@.planning/BACKLOG.md (existing, to be updated)

## Backlog entries to add (from phase scope)
- **PROVIDER-MY-ENGAGEMENTS-1** (Medium, v1.5+, organic): Swap provider engagement UI from gql SmeMartProject reads to platform.Project.list
- **PROJECT-SVC-RENAME-1** (Low, v1.5+, organic): Rename SmeMartProjectService to PlatformProjectService + swap callers (now safe, service is platform-native)
- **SCHEMA-RETIREMENT-DELETE-1** (Medium, post-closure, blocked by dual-read window): Delete SmeMartProject + EngagementVettingItem schema classes (not just deprecation)
- **[GOVERNANCE-PROJECT-RENDERING]** (Conditional, Phase 32, only if Plan 06 verification FAILED): Fix Governance UI to render platform.Project correctly
- **[VETTING-BOARD-RENDERING]** (Conditional, Phase 32, only if Plan 07 verification FAILED): Fix SME Mart Vetting view to render Board + Task hierarchy correctly

## Files to verify
- Code changes: src/app/core/services/{platform-engagement-provisioner,engagements,sme-mart-project}.service.ts + spec files
- Config: eslint.config.js (runs diff-based for modified files)
- Models: src/app/core/models/resource-type.enum.ts
- Services: src/app/shared/services/demo-visibility.service.ts, src/app/core/services/engagement-hierarchy.service.ts

## Modernization enforcement
- Touch-It-Fix-It rules: files modified in Plans 02–04 must have ALL modernization violations fixed
- Pre-commit hook + lint gate enforce this; Plan 08 verifies post-hoc
- If any file modified in 29.5 has lint errors, it's a blocker for phase closure

## Closure criteria
- [ ] All plans 01–08 have SUMMARY.md artifacts
- [ ] Lint clean: `npm run lint -- src/app/core/services src/app/shared/services src/app/core/models` (all modified files)
- [ ] Type checking clean: `npx tsc -p tsconfig.spec.json --noEmit`
- [ ] Tests green: `npm test -- --include='**/29-5-*.spec.ts' || npm test` (all affected specs)
- [ ] Schema PR merged: Plan 05 output verified (PR opened on `zerobias-org/schema`; review timing is Clark's to manage out-of-band — no automated stall handling)
- [ ] UAT verifications complete: Plans 06–07 results documented
- [ ] Backlog entries created: PROVIDER-*, PROJECT-*, SCHEMA-*, conditional [GOVERNANCE-*], [VETTING-*]
- [ ] CLOSURE.md artifact created
- [ ] ROADMAP.md updated (Phase 29.5 marked complete, Phase 30 dependencies satisfied)
- [ ] Commit message references all key artifacts

## Schema PR coordination — Director-managed, no automated handling

**RESCINDED 2026-05-11 by Director:** the prior "Schema PR Stall Escape Clause (29.5b spinoff)" content in this slot — 5-day stall threshold, `29.5b` carry-over phase template, `SCHEMA-29.5-CARRYOVER-1` BACKLOG entry, calendar-check procedure — IS RESCINDED in full. There is no stall clock. There is no automated 29.5b spinoff trigger.

If the schema PR (Plan 05) takes time to merge, that's fine — Plan 05's closure criterion is "PR opened on `zerobias-org/schema`" plus normal review. Clark manages review cadence out-of-band. If at 29.5 closure time the schema PR is still open and Clark wants to close 29.5 on app-side changes alone, that's a Director call made then, not a templated procedure encoded here.

**Closure criteria for Plan 05** at 29.5 closure time:
- If schema PR merged: mark Plan 05 COMPLETE.
- If schema PR open but Clark wants to close 29.5: Director call at the moment — no preemptive `29.5b` artifacts.
- If schema PR open and Clark wants to hold: hold.

Do NOT preemptively file `29.5b`, `SCHEMA-29.5-CARRYOVER-1`, or any other carry-over artifact in advance.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final linting pass — all modified Phase 29.5 files</name>
  <files>src/app/core/services/, src/app/shared/services/, src/app/core/models/</files>
  <action>
Run comprehensive linting to verify all Phase 29.5 code changes comply with modernization rules + ESLint config.

1. **Lint all modified services + models:**
   ```bash
   npm run lint -- src/app/core/services/platform-engagement-provisioner.service.ts \
     src/app/core/services/engagements.service.ts \
     src/app/core/services/sme-mart-project.service.ts \
     src/app/core/services/engagement-hierarchy.service.ts \
     src/app/shared/services/demo-visibility.service.ts \
     src/app/core/models/resource-type.enum.ts
   ```

2. **Expected output:**
   - 0 errors
   - 0 warnings (max-warnings=0 enforced)
   - No modernization violations (input()/output() signals, inject(), no decorators, etc.)

3. **If violations found:**
   - Identify rule violation (e.g., @Input field decorator, constructor injection)
   - Reference MODERNIZATION_GUIDE.md for before/after fix
   - Fix in place (edit file via Edit tool, not manual)
   - Re-run lint to verify fix

4. **If lint still fails after fixes:**
   - Document specific violations + file paths
   - Create errata under `.planning/director/errata/` with "LINT GATE FAILURE" title
   - Do NOT proceed to final commit until resolved
  </action>
  <verify>
    <automated>npm run lint -- src/app/core/services/platform-engagement-provisioner.service.ts src/app/core/services/engagements.service.ts src/app/core/services/sme-mart-project.service.ts src/app/core/services/engagement-hierarchy.service.ts src/app/shared/services/demo-visibility.service.ts src/app/core/models/resource-type.enum.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All Phase 29.5 modified files linting clean (0 errors, 0 warnings)
    - Modernization rules enforced: no @Input/@Output/@Injectable field decorators, no constructor injection, inject() used, input()/output() signals present
    - ESLint config green across all files
  </done>
</task>

<task type="auto">
  <name>Task 2: Final type checking — tsc clean with spec config</name>
  <files>src/app/core/services/, src/app/shared/services/, src/app/core/models/</files>
  <action>
Run TypeScript compiler to verify all Phase 29.5 code changes type-check cleanly.

1. **Type check with spec config:**
   ```bash
   npx tsc -p tsconfig.spec.json --noEmit
   ```
   Expected: 0 errors

2. **If type errors found:**
   - Identify error (e.g., "Property 'X' does not exist on type 'Y'")
   - Check if error is in Phase 29.5 modified files (filter output by file path)
   - Fix in place:
     - Missing types: add type annotations
     - Wrong API shape: verify against Platform SDK docs (not assumptions)
     - Enum mismatch: check ResourceTypeEnum values match usage
   - Re-run tsc to verify fix

3. **If errors in other files:**
   - These are pre-existing (not caused by 29.5 changes)
   - Document count + file count in CLOSURE.md
   - Do NOT block phase closure on pre-existing errors
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit 2>&1 | grep -E "src/app/(core/services|shared/services|core/models)" | head -10 || echo "OK"</automated>
  </verify>
  <done>
    - Type checking clean for all Phase 29.5 modified files (0 errors)
    - ResourceTypeEnum correctly typed + used
    - Platform SDK method signatures correctly referenced (no shape mismatches)
  </done>
</task>

<task type="auto">
  <name>Task 3: Final test pass — all Phase 29.5 affected specs</name>
  <files>src/app/</files>
  <action>
Run test suite for all files modified in Phase 29.5 to verify green test coverage.

1. **Identify affected spec files (from Plans 02–04):**
   - src/app/core/services/platform-engagement-provisioner.service.spec.ts
   - src/app/core/services/engagements.service.spec.ts
   - src/app/core/services/sme-mart-project.service.spec.ts
   - src/app/core/services/engagement-hierarchy.service.spec.ts
   - src/app/shared/services/demo-visibility.service.spec.ts
   - src/app/features/my-profile/pages/my-engagement-list/my-engagement-list.component.spec.ts (Plan 03, light verification)

2. **Run all affected specs:**
   ```bash
   npm test -- --include='**/platform-engagement-provisioner.service.spec.ts' \
     --include='**/engagements.service.spec.ts' \
     --include='**/sme-mart-project.service.spec.ts' \
     --include='**/engagement-hierarchy.service.spec.ts' \
     --include='**/demo-visibility.service.spec.ts' \
     --include='**/my-engagement-list.component.spec.ts'
   ```
   Expected: All PASS (no failures, no skipped)

3. **If any spec fails:**
   - Read failure message + stack trace
   - Identify root cause:
     - Test is wrong (mock shape mismatch, assertion error) → fix test assertion
     - Code is wrong (logic error, API call wrong) → fix code logic
     - Mock is wrong (SDK shape changed) → update mock + test assertion
   - Fix + re-run spec to verify green
   - Document failure + fix in CLOSURE.md for retrospective

4. **Coverage target:**
   - No specific % target for Phase 29.5 (tests existed pre-phase; new tests added per spec updates)
   - Goal: 100% of modified code exercised (provisioner steps, dual-read paths, resource type handling)
   - If coverage gap found, note in CLOSURE.md (not blocker, v1.5 work)
  </action>
  <verify>
    <automated>npm test -- --include='**/platform-engagement-provisioner.service.spec.ts' --include='**/engagements.service.spec.ts' --include='**/sme-mart-project.service.spec.ts' --include='**/engagement-hierarchy.service.spec.ts' --include='**/demo-visibility.service.spec.ts' --include='**/my-engagement-list.component.spec.ts' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All Phase 29.5 affected specs pass (100% green)
    - No failures, no skipped tests
    - Dual-read paths tested (platform success + GQL fallback paths)
    - Idempotency probes tested (step repetition scenarios)
    - ResourceTypeEnum new values tested in hierarchy service
  </done>
</task>

<task type="auto">
  <name>Task 4: Create closure artifact — 29.5-CLOSURE.md</name>
  <files>.planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md</files>
  <action>
Create comprehensive phase closure document summarizing all work, verifications, and outcomes.

1. **Structure (markdown format):**
   ```markdown
   # Phase 29.5: Platform Model Migration — Closure Report
   
   **Phase Goal:** Replace SmeMart GQL classes (Engagement/EngagementVettingItem/SmeMartProject) with platform/hydra SDK primitives.
   
   **Status:** COMPLETE
   
   **Timeline:**
   - Started: [date from first plan]
   - Completed: [today]
   - Duration: X days, Y hours
   
   ## Scope Summary
   
   ### Plans Executed
   - Plan 01: INVENTORY.md audit + MCP endpoint verification [COMPLETE]
   - Plan 02: Provisioner.service rewrite (7→5 steps) [COMPLETE]
   - Plan 03: Service refactors (engagements, sme-mart-project, demo-visibility) [COMPLETE]
   - Plan 04: ResourceTypeEnum + engagement-hierarchy.service [COMPLETE]
   - Plan 05: Schema deprecation PR (zerobias-org/schema) [COMPLETE]
   - Plan 06: Governance UI verification (platform.Project surfaces) [PASS/FAIL with details]
   - Plan 07: Vetting Board pattern smoke test [PASS/FAIL with details]
   - Plan 08: Final cleanup + closure [IN PROGRESS → COMPLETE]
   
   ### Code Changes
   - Files modified: 8 (provisioner, 3 services, 2 specs, enum, hierarchy service)
   - Lines added: ~X
   - Lines removed: ~Y
   - Specs written: ~Z
   
   ### Linting & Type Checking
   - ESLint: 0 errors, 0 warnings (modernization rules enforced)
   - tsc (spec config): 0 errors
   - Tests: ALL PASS (provisioner, engagements, sme-mart-project, engagement-hierarchy, demo-visibility, my-engagement-list)
   
   ### Upstream Coordination
   - zerobias-org/schema PR: [PR#NNN] Merged by Daniel Rojas
   - SME Mart schema deprecation: SmeMartProject + EngagementVettingItem marked @Deprecated
   - Cross-fork workflow: w3geekery/app:poc/sme-mart → zerobias-org/app:uat (Plan 02 PR merged)
   
   ### UAT Verifications
   - Plan 06 (Governance UI): [PASS: platform.Project surfaces correctly / FAIL: [details]]
   - Plan 07 (Vetting Board): [PASS: Board + Task hierarchy renders / FAIL: [details]]
   - If any FAIL: Conditional backlog entries created + filed under Phase 32
   
   ## Key Decisions Locked
   - D-01 through D-43: All locked decisions from CONTEXT.md honored
   - D-08: Engagement Task (Step B) [DROPPED per governance verification / RETAINED per verification failure]
   - D-26/D-27: Schema classes marked @Deprecated (not deleted) — confirmed in zerobias-org/schema
   
   ## Modernization Compliance (Touch-It-Fix-It)
   - All modified files reviewed for @Input/@Output/@Injectable field decorators: ✓ NONE
   - All modified files reviewed for constructor injection: ✓ NONE (inject() used exclusively)
   - All modified files reviewed for input()/output() signals: ✓ Present in component inputs
   - All modified files linting clean per .eslintrc: ✓ 0 errors, 0 warnings
   
   ## Backlog Entries Created
   - PROVIDER-MY-ENGAGEMENTS-1 (Medium, v1.5+, organic): Swap provider engagement UI to platform.Project
   - PROJECT-SVC-RENAME-1 (Low, v1.5+, organic): Rename SmeMartProjectService → PlatformProjectService
   - SCHEMA-RETIREMENT-DELETE-1 (Medium, post-closure, blocked by dual-read window): Delete deprecated schema classes
   - [GOVERNANCE-PROJECT-RENDERING] (Conditional, Phase 32): If Plan 06 FAILED — fix Governance UI rendering
   - [VETTING-BOARD-RENDERING] (Conditional, Phase 32): If Plan 07 FAILED — fix Vetting view Board rendering
   
   ## Phase 30 Readiness
   - ✓ Platform model migration complete (Project/Board primitives in place)
   - ✓ Resource types extended (ResourceTypeEnum with project/board)
   - ✓ Service layer refactored (dual-read window stable for legacy data)
   - ✓ Schema deprecation applied (SmeMartProject/EngagementVettingItem marked)
   - ? Governance UI verified (Plan 06 result determines go/no-go for D-08 decision)
   - ? Vetting Board rendering verified (Plan 07 result determines Phase 32 work)
   - → Proceed to Phase 30 (Default Project Board UI) if governance/vetting verifications PASS
   
   ## Known Risks / Issues
   - [If any failed] Plan 06/07 verification gaps: See conditional backlog entries
   - [Pre-existing] Type errors outside Phase 29.5 scope: [count] files, not blocking
   
   ## Lessons Learned
   - [To be filled by developer post-phase]
   
   ## Closure Sign-Off
   - Phase Lead: [executor agent]
   - Verified by: [you]
   - Date: [today]
   - Status: READY FOR PHASE 30 (or PHASE 30 BLOCKED PENDING VERIFICATION)
   ```

2. **Fill in details from SUMMARY.md files + prior task outputs**
3. **Ensure governance/vetting verification results included (Plans 06–07)**
4. **Note any conditional backlog entries filed**
  </action>
  <verify>
    <automated>wc -l .planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md && grep -c "COMPLETE\|PASS\|FAIL" .planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md</automated>
  </verify>
  <done>
    - 29.5-CLOSURE.md created with all sections filled (80+ lines)
    - Plans 01–08 status documented
    - Linting/tsc/test results documented
    - Verification outcomes (Plans 06–07) documented
    - Backlog entries listed
    - Phase 30 readiness status clear
  </done>
</task>

<task type="auto">
  <name>Task 5: Update BACKLOG.md with Phase 29.5 post-closure entries</name>
  <files>.planning/BACKLOG.md</files>
  <action>
Add backlog entries for work identified during Phase 29.5 but deferred to later phases/milestones.

1. **Read existing BACKLOG.md** to understand structure + entry format

2. **Add the following entries** (in order of priority):

   **Entry 1: PROVIDER-MY-ENGAGEMENTS-1**
   - Title: "Swap provider engagement UI to platform.Project.list"
   - Priority: Medium
   - Phase: v1.5+ (organic)
   - Description: "Provider dashboard currently reads from gql SmeMartProject search. Phase 29.5 provides dual-read window + platform.Project SDK. Swap UI to primary platform.Project.list path (fallback to GQL on timeout). Aligns with demo-visibility service reconciliation from Phase 29.5."
   - Estimation: 3h

   **Entry 2: PROJECT-SVC-RENAME-1**
   - Title: "Rename SmeMartProjectService → PlatformProjectService"
   - Priority: Low
   - Phase: v1.5+ (organic)
   - Description: "Phase 29.5 flipped SmeMartProjectService implementation from Pipeline.receive to platform.Project SDK. Service name is now misleading. Safe to rename once dual-read window closes (Phase 31). Update service name, class, exports, all callers."
   - Estimation: 2h

   **Entry 3: SCHEMA-RETIREMENT-DELETE-1**
   - Title: "Delete SmeMartProject & EngagementVettingItem schema classes"
   - Priority: Medium
   - Phase: v1.5+ (post-closure)
   - Blocked by: Dual-read window closure (after Phase 31, ~2026-06-XX)
   - Description: "Phase 29.5 marked SmeMartProject + EngagementVettingItem @Deprecated (not deleted) during dual-read window (Phases 29.5–31). After data aging + migration complete, delete these classes from zerobias-org/schema. Coordinate with Daniel Rojas (Content team). No app-side changes needed (dual-read already removed post-Phase 31)."
   - Estimation: 1h
   - Depends on: Phase 31 closure

   **Entry 4: [GOVERNANCE-PROJECT-RENDERING] (Conditional)**
   - Title: "[PHASE 32] Fix Governance UI to render platform.Project hierarchy"
   - Priority: High (if Plan 06 verification FAILED)
   - Phase: 32 (governance rendering)
   - Status: CONDITIONAL — Only if Plan 06 governance verification FAILED
   - Description: "Platform.Project (engagement) did not surface in ZB Governance Projects view during Phase 29.5 Plan 06 smoke test. Gap in Governance rendering layer (not SME Mart). File in Phase 32 scope. Requires ZB team work (not SME Mart)."
   - Estimation: TBD (ZB team)
   - NOTE: "File this entry ONLY if Plan 06 verification failed. Otherwise, delete this placeholder."

   **Entry 5: [VETTING-BOARD-RENDERING] (Conditional)**
   - Title: "[PHASE 32] Fix SME Mart Vetting view to render Board + Task hierarchy"
   - Priority: High (if Plan 07 verification FAILED)
   - Phase: 32 (vetting rendering)
   - Status: CONDITIONAL — Only if Plan 07 vetting verification FAILED
   - Description: "Platform.Board (vetting board) and Task hierarchy rendering broken in SME Mart Vetting view during Phase 29.5 Plan 07 smoke test. Rendering layer mismatch (UI doesn't know how to display new platform primitives). Coordinate with Brian + Kevin on urgency."
   - Estimation: 6h (SME Mart component updates)
   - NOTE: "File this entry ONLY if Plan 07 verification failed. Otherwise, delete this placeholder."

3. **Commit changes with message:** `"docs(backlog): add Phase 29.5 post-closure entries + conditional Phase 32 entries"`
  </action>
  <verify>
    <automated>grep -c "PROVIDER-MY-ENGAGEMENTS-1\|PROJECT-SVC-RENAME-1\|SCHEMA-RETIREMENT-DELETE-1" .planning/BACKLOG.md</automated>
  </verify>
  <done>
    - PROVIDER-MY-ENGAGEMENTS-1 added (v1.5+ organic swap)
    - PROJECT-SVC-RENAME-1 added (v1.5+ organic rename)
    - SCHEMA-RETIREMENT-DELETE-1 added (post-closure delete, blocked by dual-read window)
    - Conditional entries [GOVERNANCE-PROJECT-RENDERING] + [VETTING-BOARD-RENDERING] added (only if verifications failed; otherwise to be removed)
    - BACKLOG.md formatted consistently with existing entries
  </done>
</task>

<task type="auto">
  <name>Task 6: Update ROADMAP.md — Phase 29.5 complete, Phase 30 dependencies satisfied</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Update ROADMAP.md to mark Phase 29.5 as complete + update Phase 30 status.

1. **Read existing ROADMAP.md** (already in context from earlier planning)

2. **Update Phase 29.5 section:**
   - Change status: `[x] COMPLETE` (or similar if using checkbox format)
   - Update dates: completion date = today
   - Update plan list: check all 8 plans (01–08)
   - Add one-line summary: "Platform model migration complete: SmeMart GQL classes replaced with platform/hydra SDK primitives. Dual-read window open (Phases 29.5–31). Schema deprecation applied. Governance/vetting verification [PASS/FAIL details]."

3. **Update Phase 30 (Default Project Board UI):**
   - Remove placeholder status (if "To be planned")
   - Update brief: rewrite to reference actual platform.Project/Board instead of SmeMart analogs
   - Reference Phase 29.5 as prerequisite completed
   - Update dependencies: "Depends on Phase 29.5 (platform model migration) — platform.Project + Board now available"
   - If Phase 30 plan files don't exist yet, add note: "Planning: pending Phase 29.5 closure verification"

4. **Commit changes with message:** `"docs(roadmap): Phase 29.5 complete; Phase 30 dependencies satisfied"`
  </action>
  <verify>
    <automated>grep -A5 "Phase 29.5" .planning/ROADMAP.md | head -10 && grep -A5 "Phase 30" .planning/ROADMAP.md | head -10</automated>
  </verify>
  <done>
    - Phase 29.5 marked COMPLETE with all 8 plans listed
    - Phase 29.5 brief updated with completion summary
    - Phase 30 dependencies documented (Phase 29.5 prerequisite complete)
    - Phase 30 brief rewritten to reference platform primitives (not SmeMart analogs)
    - ROADMAP.md reflects project progression (v1.4 milestone track)
  </done>
</task>

<task type="auto">
  <name>Task 7: Commit Phase 29.5 final state — all artifacts + closures</name>
  <files>.planning/phases/29.5-platform-model-migration/, src/app/core/services/, src/app/core/models/, .planning/BACKLOG.md, .planning/ROADMAP.md</files>
  <action>
Create final commit capturing all Phase 29.5 work, verifications, and closures.

1. **Stage all changes:**
   ```bash
   git add .planning/phases/29.5-platform-model-migration/29.5-*
   git add .planning/BACKLOG.md
   git add .planning/ROADMAP.md
   git add src/app/core/services/platform-engagement-provisioner.service.ts
   git add src/app/core/services/platform-engagement-provisioner.service.spec.ts
   git add src/app/core/services/engagements.service.ts
   git add src/app/core/services/engagements.service.spec.ts
   git add src/app/core/services/sme-mart-project.service.ts
   git add src/app/core/services/sme-mart-project.service.spec.ts
   git add src/app/core/services/engagement-hierarchy.service.ts
   git add src/app/core/services/engagement-hierarchy.service.spec.ts
   git add src/app/shared/services/demo-visibility.service.ts
   git add src/app/shared/services/demo-visibility.service.spec.ts
   git add src/app/core/models/resource-type.enum.ts
   ```

2. **Commit with detailed message:**
   ```
   feat(29.5): Platform Model Migration — Phase Complete
   
   Replaces SmeMart GQL classes (Engagement/EngagementVettingItem/SmeMartProject)
   with platform/hydra SDK primitives. Dual-read window active (Phases 29.5–31).
   
   Changes:
   - Provisioner.service: 7-step GQL recipe → 5-step platform primitives (Steps B/E deleted)
   - engagements.service: platform.Project.list primary, GQL SmeMartProject fallback
   - sme-mart-project.service: Pipeline.receive removed, platform.Project SDK integrated
   - engagement-hierarchy.service: returns ProjectExtended + BoardExtended with resourceType
   - ResourceTypeEnum: extended with 'project' + 'board' values
   - demo-visibility.service: reconciled for dual-shape polymorphism
   
   Verification:
   - All code: lint clean (ESLint 0 errors), type clean (tsc 0 errors), tests pass
   - Schema: zerobias-org/schema PR merged, SmeMartProject + EngagementVettingItem @Deprecated
   - UAT: Governance verification (Plan 06) + Vetting Board smoke test (Plan 07) complete
   
   Backlog:
   - PROVIDER-MY-ENGAGEMENTS-1 (v1.5+ organic): swap provider UI to platform.Project
   - PROJECT-SVC-RENAME-1 (v1.5+ organic): rename SmeMartProjectService
   - SCHEMA-RETIREMENT-DELETE-1 (post-closure): delete deprecated schema classes
   - Conditional [GOVERNANCE-PROJECT-RENDERING], [VETTING-BOARD-RENDERING] per verification results
   
   Phase 30 readiness: [READY / BLOCKED] per governance/vetting verification outcomes
   
   Refs: Plans 29.5-01 through 29.5-08, CLOSURE.md, zerobias-org/schema PR #NNN
   
   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   ```

3. **Push to current branch (poc/sme-mart):**
   ```bash
   git push origin poc/sme-mart
   ```

4. **Verify commit:**
   ```bash
   git log --oneline -1
   git show --stat HEAD
   ```
  </action>
  <verify>
    <automated>git log --oneline -1 | grep -i "29.5\|platform.*migration" && git show --stat HEAD | wc -l</automated>
  </verify>
  <done>
    - All Phase 29.5 code changes committed
    - All Phase 29.5 artifacts committed (CLOSURE.md, SUMMARY.md files)
    - BACKLOG.md updated with post-closure entries
    - ROADMAP.md updated with completion status + Phase 30 readiness
    - Commit message detailed (changes, verifications, backlog entries, phase 30 status)
    - Commit pushed to poc/sme-mart branch
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code changes ↔ verification | Lint/tsc/tests must confirm code is correct before closure |
| Phase 29.5 ↔ Phase 30 dependency | Governance/vetting verification gates Phase 30 go/no-go decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.5-20 | **R** (Repudiation) | Phase completion checklist | Mitigate | CLOSURE.md artifact immutable (git history); audit trail of all verifications recorded |
| T-29.5-21 | **I** (Information Disclosure) | Conditional backlog entries | Mitigate | Backlog entries clearly marked [GOVERNANCE-*] / [VETTING-*] to distinguish from committed work; Phase 32 scoped if filed |

</threat_model>

<verification>
**Phase 29.5 Closure Checklist:**

Before proceeding to Phase 30 or marking complete:

1. **Linting clean:**
   ```bash
   npm run lint -- src/app/core/services/{platform-engagement-provisioner,engagements,sme-mart-project,engagement-hierarchy}.service.ts src/app/shared/services/demo-visibility.service.ts src/app/core/models/resource-type.enum.ts
   ```
   Expected: 0 errors, 0 warnings

2. **Type checking clean:**
   ```bash
   npx tsc -p tsconfig.spec.json --noEmit | grep -i "core/services\|shared/services\|core/models" || echo "OK"
   ```
   Expected: 0 Phase-29.5-related errors

3. **All tests pass:**
   ```bash
   npm test -- --include='**/platform-engagement-provisioner.service.spec.ts' --include='**/engagements.service.spec.ts' --include='**/sme-mart-project.service.spec.ts' --include='**/engagement-hierarchy.service.spec.ts' --include='**/demo-visibility.service.spec.ts'
   ```
   Expected: ALL PASS

4. **CLOSURE.md exists:**
   ```bash
   test -f .planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md && echo "OK"
   ```
   Expected: file exists with all sections filled

5. **Backlog entries added:**
   ```bash
   grep -c "PROVIDER-MY-ENGAGEMENTS\|PROJECT-SVC-RENAME\|SCHEMA-RETIREMENT" .planning/BACKLOG.md
   ```
   Expected: 3+ matches (plus conditional entries if applicable)

6. **ROADMAP.md updated:**
   ```bash
   grep "Phase 29.5.*COMPLETE\|Phase 30.*Platform" .planning/ROADMAP.md | wc -l
   ```
   Expected: 2+ matches (29.5 complete, 30 updated)

7. **All commits pushed:**
   ```bash
   git log --oneline -3 | grep -i "29.5\|platform.*migration"
   ```
   Expected: latest commit references Phase 29.5 closure

8. **UAT verifications complete:**
   - Plan 06 (Governance verification): PASS or FAIL with details in CLOSURE.md
   - Plan 07 (Vetting Board verification): PASS or FAIL with details in CLOSURE.md
</verification>

<success_criteria>
Phase 29.5 (Platform Model Migration) is COMPLETE when:

- [x] All 8 plans executed + SUMMARY.md created for each
- [x] All code changes lint clean (ESLint 0 errors, 0 warnings)
- [x] All code changes type-check clean (tsc 0 errors)
- [x] All affected tests pass (100% green)
- [x] Schema deprecation PR merged (zerobias-org/schema main branch)
- [x] UAT governance verification completed (Plan 06 results documented)
- [x] UAT vetting Board verification completed (Plan 07 results documented)
- [x] Conditional backlog entries created (if verifications failed)
- [x] Post-closure backlog entries added (PROVIDER-*, PROJECT-*, SCHEMA-*)
- [x] CLOSURE.md artifact created + comprehensive
- [x] ROADMAP.md updated (Phase 29.5 marked complete, Phase 30 dependencies satisfied)
- [x] Final commit created + pushed to poc/sme-mart
- [x] Phase 30 readiness: [READY if both verifications PASS] OR [BLOCKED if governance/vetting failed + conditional backlog entries created]

**Phase 30 Go/No-Go Decision:**
- If Plans 06–07 verifications both PASS → Phase 30 can proceed (governance + vetting patterns validated)
- If Plan 06 OR Plan 07 verification FAILS → Conditional backlog entries created, Phase 30 blocked until Phase 32 rendering work completes (or backlog work moved earlier if critical)
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-platform-model-migration/29.5-08-SUMMARY.md` with:

1. **Final status:** COMPLETE
2. **Linting results:** npm run lint output (0 errors, 0 warnings)
3. **Type checking results:** tsc output (0 errors)
4. **Test results:** Test run output (X specs, Y passed, 0 failed)
5. **Artifacts created:** 29.5-CLOSURE.md, BACKLOG.md updates, ROADMAP.md updates
6. **Backlog entries:** Count of entries added + list (regular + conditional)
7. **Schema coordination:** zerobias-org/schema PR reference + merge status
8. **UAT verification summary:** Plans 06–07 results (PASS/FAIL + details)
9. **Phase 30 readiness:** GO or NO-GO decision + reasoning
10. **Final commit:** git log output showing closure commit + push status

**This SUMMARY.md completes Phase 29.5 documentation. All prior plans' SUMMARY.md files should be in `.planning/phases/29.5-platform-model-migration/` directory.**
</output>
