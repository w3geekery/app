---
phase: 29.5
plan: 08
type: outcome
wave: 5
executed-on: 2026-05-12
executed-by: gsd-execute (Claude Opus 4.7)
status: COMPLETE
---

# Phase 29.5 Plan 08 — Wave 5 Close

**Final status:** COMPLETE. Phase 29.5 closed at HEAD `e39b236` (pre-Plan-08 commit) with all gates green and CLOSURE.md authored. Three organic backlog entries filed; two pre-authored conditional entries dropped per Director rules (Plan 06 PASSED, Plan 07 superseded).

---

## Gate Results (HEAD = `e39b236`)

| Gate | Command | Result |
|------|---------|--------|
| Lint (29.5 modified files only) | `npx eslint --max-warnings=0 <12 files>` | **0 errors, 0 warnings** |
| tsc (app config) | `npx tsc -p tsconfig.app.json --noEmit` | **0 errors** |
| tsc (spec config — canonical gate) | `npx tsc -p tsconfig.spec.json --noEmit` | **0 errors** |
| Targeted tests (Plans 02-04 modified specs) | `npm test -- --include=<provisioner+engagements+sme-mart-project specs>` | **3 test files passed, 50/50 tests passed** (1.75s) |

**Drift gate counts:**

| Surface | Counts |
|---------|--------|
| Lint errors on 29.5 modified files | **0** |
| Lint errors on rest of repo (MODERN-CLEANUP-1, pre-existing) | ~1430 (unchanged; not in 29.5 scope) |
| tsc errors (app config, whole project) | **0** |
| tsc errors (spec config, whole project) | **0** |
| Tests passed | **50/50** |
| Tests failed | **0** |
| Tests skipped | **0** |

**Full-repo lint note:** `npm run lint` (script does `eslint .`) reports the same ~1430 pre-existing errors as before Phase 29.5 — those are tracked under `MODERN-CLEANUP-1` and only checked diff-based by pre-commit + CI. Phase 29.5's own modified files (the 12 listed in CLOSURE.md) are lint-clean.

---

## Artifacts Created

| File | Purpose |
|------|---------|
| `.planning/phases/29.5-platform-model-migration/29.5-CLOSURE.md` | Comprehensive closure report — Plans 01–08 status, errata links, gate results, locked decision triad, follow-ups, sign-off |
| `.planning/phases/29.5-platform-model-migration/29.5-08-SUMMARY.md` | This file |

## Artifacts Modified

| File | Change |
|------|--------|
| `.planning/BACKLOG.md` | Added 3 organic Platform Alignment entries: `PROVIDER-MY-ENGAGEMENTS-1`, `PROJECT-SVC-RENAME-1`, `SCHEMA-RETIREMENT-DELETE-1`. (Did NOT re-add Director's `D-49-NAMESPACE-MIGRATE-1` or `VETTING-PLATFORM-MIGRATE-1` — already present at commit `e39b236`.) |
| `.planning/ROADMAP.md` | Phase 29.5 marked COMPLETE (8/8 plans); Phase 30 entry annotated with brief-revision-needed banner (D-46/D-50 invalidate prior tier-tag assumptions in commit `b7f9b80`); progress table updated; Phase 29.5 row added with completion date |

---

## Backlog Entries

**Organic (3 added):**

1. **`PROVIDER-MY-ENGAGEMENTS-1`** — Medium, v1.5+ organic. Swap provider engagement UI from GQL `SmeMartProject` reads to `platform.Project.list`. Trigger: next time provider dashboard is touched, OR before dual-read window closes (Phase 31).
2. **`PROJECT-SVC-RENAME-1`** — Low, v1.5+ organic. Rename `SmeMartProjectService` → `PlatformProjectService` once dual-read window closes. Pure mechanical refactor.
3. **`SCHEMA-RETIREMENT-DELETE-1`** — Medium, post-closure. Delete `SmeMartProject` + `EngagementVettingItem` schema classes from `zerobias-org/schema`. Blocked by dual-read window closure (after Phase 31).

**Conditional (2 dropped):**

- `[GOVERNANCE-PROJECT-RENDERING]` — dropped. Plan 06 PASSED (5/5 SDK assertions); filing condition (FAIL) not met.
- `[VETTING-BOARD-RENDERING]` — dropped. Superseded by Director-filed `VETTING-PLATFORM-MIGRATE-1` (broader scope per errata 031).

**Director-filed (2 recognized, not re-added):**

- `D-49-NAMESPACE-MIGRATE-1` — hard prerequisite for Phase 31.
- `VETTING-PLATFORM-MIGRATE-1` — v1.5+ vetting modernization.

---

## Schema PR Coordination (Plan 05)

Per Director rescind: there is no stall clock; merge cadence is managed out-of-band. Phase 29.5 closes on app-side state alone. Director handles the cross-fork PR step (poc/sme-mart → zerobias-org/app:uat) post-closure, and any escalation on the schema PR review is also out-of-band.

---

## UAT Verification Summary

| Plan | Outcome | Notes |
|------|---------|-------|
| Plan 06 — UAT Engagement/Project SDK round-trip | **PASS** (5/5 assertions) | UI cross-check (browser-based) staged for Clark in `29.5-06-SUMMARY.md`. Deferred, not blocking. |
| Plan 07 — UAT Vetting Board smoke | **PREMISE-OBSOLETE / DEFERRED via Director Option A** | Premise (vetting `platform.Board` lazy-create) was never implemented in code; v3 amendment correctly excluded vetting modernization. Backlog routed via `VETTING-PLATFORM-MIGRATE-1`. Errata 031 records the decision. |

---

## Phase 30 Readiness

**GO** — with one prerequisite: Director rewrites the Phase 30 brief at commit `b7f9b80` to reflect the D-46/D-49/D-50 ratification triad (Engagement-as-Project hierarchy, tag-namespace flip, Project-vs-Workspace naming). Prior tier-tag assumptions in the brief are invalidated by 29.5 decisions.

No 29.5 outstanding work blocks Phase 30. The 5 follow-up backlog entries do not gate Phase 30:
- `D-49-NAMESPACE-MIGRATE-1` gates Phase **31** (not 30).
- `VETTING-PLATFORM-MIGRATE-1` is v1.5+.
- `PROVIDER-MY-ENGAGEMENTS-1`, `PROJECT-SVC-RENAME-1`, `SCHEMA-RETIREMENT-DELETE-1` are v1.5+ or post-closure.

---

## Final Commit

Will be created as a single Plan 08 commit:

> `docs(29.5-08): Wave 5 close — Phase 29.5 complete (CLOSURE.md + ROADMAP + backlog)`

Branch lands at ~42 ahead of `origin/poc/sme-mart`. **NOT pushed** per Director instruction. Director handles the cross-fork PR (`w3geekery/app:poc/sme-mart` → `zerobias-org/app:uat`) at Wave 5 close after final review.

---

**Plan 08: ✅ COMPLETE. Phase 29.5: ✅ COMPLETE.** Wave 5 closed 2026-05-12.
