# Phase 29.5: Platform Model Migration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 29.5-platform-model-migration
**Areas discussed:** 6 tactical Open Questions from Director's brief (`.planning/director/phase-29.5-brief.md` §"Open questions still need discuss-phase"). Discussion was batched (one plain-text round of 6 recommendations + Director verdict) per Clark's framing: "discuss-phase is TACTICAL, NOT DIRECTIONAL — most concerns are already locked." `AskUserQuestion` is globally banned (2026-04-30); plain-text exchange used throughout.

---

## Q1 — Governance verification gate (Engagement Task drop)

| Option | Description | Selected |
|--------|-------------|----------|
| Drop Engagement Task speculatively + verify on UAT (29.5 deliverable) | Drop `aha1-N` Task from recipe. Add UAT smoke deliverable: provision test org → confirm Governance UI surfaces `platform.Project` records. Fall back to placeholder Task + `[GOVERNANCE-PROJECT-RENDERING]` BACKLOG entry if verification fails. | ✓ |
| Keep Engagement Task as placeholder until Governance UI confirmed | Conservative; keep `aha1-N` Task as placeholder; remove later. Fewer recipe changes; reads slightly less from "5 steps" target. | |
| Keep Engagement Task indefinitely (no drop) | Risk-averse; doubles the surfacing surface (Project + Task) permanently. Conflicts with brief's "Eliminate GQL surface area" core directive. | |

**User's choice:** Speculative-drop with verification gate as 29.5 deliverable.
**Notes:** Director's lean default in the brief was speculative-drop. Aligns with Clark's "Eliminate GQL surface area as much as feasible" directive. Verification cost is half a session; downside (placeholder fallback) is well-defined.

---

## Q2 — Vetting Board `boardType`

| Option | Description | Selected |
|--------|-------------|----------|
| `list` (checkbox-shape) | Vetting requirements are paired req-sat tasks; satisfied/not is the primary state. Multi-stage transitions not in v1.4 scope. | ✓ |
| `kanban` (multi-stage) | Works if vetting has multi-stage transitions (e.g., draft → submitted → reviewed → approved). Extra UI surface. | |
| `timeline` | Time-axis ordering. Not relevant for vetting. | |

**User's choice:** `list`.
**Notes:** Director's lean default. Workspace default Board stays `kanban` + `isDefault=true`. Vetting Board is `isDefault=false`, lazy-created on first vetting requirement (not eagerly created in platform-engagement provisioning).

---

## Q3 — Existing `SmeMartProject` records post-retirement

| Option | Description | Selected |
|--------|-------------|----------|
| Deprecate without delete (dual-read) | Keep schema class definitions; stop CREATING new records; reads work for both old (GQL) and new (`platform.Project`) shapes until data ages out. | ✓ |
| Delete schema classes immediately | Forces a one-shot data migration; high risk; breaks demo data reads if lifecycle semantics don't allow orphan reads. | |
| Keep classes + keep writing them indefinitely | Defeats the migration. Locked decision: zero new GQL classes; three retired. | |

**User's choice:** Deprecate without delete; dual-read until data ages out.
**Notes:** Daniel Rojas confirms during PR review that orphan reads work. Plan 01 escalates if lifecycle semantics surface a problem before merging the schema PR. Eventual deletion deferred to `SCHEMA-RETIREMENT-DELETE-1` (gated on dual-read having zero remaining readers, not a calendar date).

---

## Q4 — Phase 30 / 31 dependency

| Option | Description | Selected |
|--------|-------------|----------|
| Strict sequential 29.5 → 30 → 31 | Phase 30 brief at `b7f9b80` is stale; rewrite post-29.5-closure. Phase 31 unchanged, depends on 30. | ✓ |
| Phase 30 starts in parallel on still-locked subset | Some Phase 30 work is independent of model migration (placeholders for 046/066/065). Risk: unknown overlap. | |

**User's choice:** Strict sequential. No parallel start.
**Notes:** Phase 30 brief refresh is a Director artifact (NOT a 29.5 deliverable). Director updates `phase-30-brief.md` post-29.5-closure to reference real `platform.Project` + `platform.Board` instead of SmeMart-side analogs.

---

## Q5 — `SmeMartProjectService` post-migration naming

| Option | Description | Selected |
|--------|-------------|----------|
| Keep the name, change the implementation | Existing class is the import surface for many callers (`ProjectDetail`, `MyProjects`, etc.). Rename = wide unrelated diff in a high-touch phase. Implementation flips from GQL+Pipeline to `platform.Project.list/get/create/update`. | ✓ |
| Rename to `ProjectService` and migrate consumers | Cleaner name post-migration. High churn cost; touches many call sites. | |
| Add new `PlatformProjectService` alongside, deprecate old | Dual-service pattern adds confusion during the transition. | |

**User's choice:** Keep the name; flip the implementation. File `PROJECT-SVC-RENAME-1` (Low, organic-cleanup) for v1.5+ rename.
**Notes:** Plan 01 decides whether to introduce a thin `PlatformProjectService` alongside (default: no — fold).

---

## Q6 — Provider-side My Engagements query shape

Initial framing offered three angles; Director amended the recommendation to split buyer-side (implement) vs provider-side (spec-only).

### Q6.1 — Buyer-side My Engagements

| Option | Description | Selected |
|--------|-------------|----------|
| `platform.Project.list({ ownerId: <buyerOrgId>, tagId-classifier-filter })` | Already half-implemented at `engagements.service.ts:51` (uses `buyerZerobiasOrgId`). Method swap from GQL `Engagement` → `platform.Project.list`. | ✓ |
| Continue using GQL `Engagement` reads (no change) | Conflicts with 29.5 retirement of `Engagement` class. | |

**User's choice:** Implement in 29.5. Method swap as described.

### Q6.2 — Provider-side My Engagements

| Option | Description | Selected |
|--------|-------------|----------|
| Spec-only via MCP describe; document in INVENTORY.md; do NOT implement UI in 29.5; backlog `PROVIDER-MY-ENGAGEMENTS-1` | In v1.4 the only platform engagements are operator-provisions-customer-org. ZeroBias-as-provider is NOT a member of the engagement Project (locked decision). ZB-staff visibility comes from admin Provisioning tab + admin views, not from the buyer-facing My Engagements page. | ✓ |
| Implement provider-side UI in 29.5 alongside buyer-side | Was original Claude recommendation. Director identified the scoping issue: provider-side query needs marketplace-engagement work + ZB-staff "all engagements" surface — both v1.5+. | |

**User's choice:** Spec-only + backlog `PROVIDER-MY-ENGAGEMENTS-1` (Medium, v1.5+).
**Notes:** Plan 01 MCP describe targets: `portal.Project.search`, `platform.Project.list`. Confirm `memberPrincipalId` filter param name on each. INVENTORY.md flags the edge case (NOT block on): if ZB-org admins eventually need a "platform engagements I provide" view via My Engagements (vs admin tooling), that's the v1.5+ task.

---

## Claude's Discretion

- Plan 01 wave structure / numbering — planner decides.
- Whether to introduce a thin `PlatformProjectService` alongside the existing `SmeMartProjectService` (default: no; fold).
- Whether to bundle vetting Board pattern smoke into Plan 01 or split as separate Plan 0X.
- Internal partitioning of the 13 `engagement-*` services into REWRITE / RECONCILE / KEEP / NOT-TOUCHED — judgment call during inventory.

## Deferred Ideas

- **`PROVIDER-MY-ENGAGEMENTS-1`** (Medium, v1.5+) — provider-side My Engagements UI. Spec query shape in 29.5 INVENTORY.md but do NOT implement.
- **`PROJECT-SVC-RENAME-1`** (Low, organic-cleanup) — rename `SmeMartProjectService` → `ProjectService` in v1.5+.
- **`SCHEMA-RETIREMENT-DELETE-1`** (Medium, post-29.5-closure) — delete schema classes once dual-read has zero remaining readers.
- **`[GOVERNANCE-PROJECT-RENDERING]`** (conditional) — only files if Q1 verification fails on UAT.
- **`EngagementMetadata` GQL class** — planted seed for v1.5+; not created in 29.5.
- **Activity-workflow definitions for vetting requirement types** — authored later when concrete requirements land.
- **Marketplace-engagement provisioning recipe** — eager-create vetting Board + provider-side membership in Step G; v1.5+.
- **`PERMS-AUDIT-1`** — hydra Role scoped-grant primitives ready; actual audit is its own phase.
- **Phase 30 brief refresh** — Director artifact post-29.5-closure.
