---
phase: 29.5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
must_haves:
  truths:
    - "Codebase partitioned into REWRITE / RECONCILE / KEEP / NOT-TOUCHED buckets with justification"
    - "13 engagement-*.service.ts files classified; REWRITE targets identified"
    - "MCP describe pins collected for all 6 platform/portal/hydra endpoints"
    - "Member-filter param shape confirmed for platform.Project.list and portal.Project.search"
    - "INVENTORY.md created with dispositions, audit notes, and next-plan dependencies"
  artifacts:
    - path: ".planning/phases/29.5-platform-model-migration/INVENTORY.md"
      provides: "Codebase partitioning, service audit, MCP describes, pre-fill map"
      min_lines: 200
  key_links:
    - from: "INVENTORY.md"
      to: "Plan 02 provisioner.service.ts"
      via: "Constants block extension spec"
      pattern: "WORKSPACE_PROJECT_NAME, workspaceProjectDescription"
    - from: "INVENTORY.md"
      to: "Plan 03 engagements.service.ts"
      via: "platform.Project.list member-filter shape"
      pattern: "ownerId, tagId filter params"
---

<objective>
Audit the codebase to partition classes and services into REWRITE / RECONCILE / KEEP / NOT-TOUCHED buckets, confirm platform SDK endpoint signatures via MCP describe, and produce INVENTORY.md consumed by downstream plans.

Purpose: Risk reduction. Phase 29.5's scope depends on knowing exactly which 13 engagement-*.service.ts files need refactoring vs. which are already platform-ready or GQL-non-engagement. MCP describes lock down the exact param shapes (member filters, Project required fields, Board boardType options).

Output: INVENTORY.md (codebase audit table + MCP pin results + pre-fill query spec for provider-side My Engagements).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/director/phase-29.5-brief.md
@.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md
@.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md
@.planning/docs/SDK_VERIFICATION_SOURCES.md
@.planning/notes/projects-boards-models.md
@.planning/notes/zb-task-reference.md

# Code sources
@src/app/core/services/platform-engagement-provisioner.service.ts
@src/app/core/services/engagements.service.ts
@src/app/core/services/sme-mart-project.service.ts
@src/app/core/services/sme-mart-tag.service.ts
@src/app/pages/my-engagements/my-engagement-list.component.ts
@src/app/core/models/sme-mart-resource.model.ts

# Schema repo reference
@~/Projects/zb/zerobias-org/schema/package/w3geekery/sme-mart/classes/engagement.yml (read for audit baseline)

# SDK reference
@~/Projects/zb/clients/packages/platform-sdk/src/models/ (check Project, Board, Task shape)
@~/Projects/zb/clients/packages/hydra-sdk/src/models/tag.ts (Tag API already understood)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit — partition 13 engagement-*.service.ts files</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (output)</files>
  <read_first>
    @.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md (D-42 audit scope)
    @.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md (section "File Classification")
    @src/app/core/services/ (all service files, grep for "Engagement" / "EngagementVettingItem" / "SmeMartProject")
  </read_first>
  <action>
Run grep to find all references to retired GQL classes across the 13 engagement-*.service.ts files. Partition into:
- **REWRITE** — services calling `graphqlRead.query('Engagement', ...)` or `graphqlRead.getById('SmeMartProject', ...)`
- **RECONCILE** — services that reference `SmeMartTask` but can delegate to `platform.Task` SDK calls
- **KEEP** — services that don't reference retired classes (platform-adjacent or marketplace-only)
- **NOT-TOUCHED** — services not in the engagement-*.service.ts cluster (false positives from grep)

Record in INVENTORY.md table format:
| Service | File | References | Disposition | Notes |
|---------|------|-----------|-------------|-------|
| engagement-context | src/app/core/services/engagement-context.service.ts | Engagement (3), SmeMartProject (2) | REWRITE | Dependency on retired classes; methods must consume platform.Project API |
| engagement-hierarchy | src/app/core/services/engagement-hierarchy.service.ts | resource_type switch (project/board) | KEEP | Switch already extensible; add board cases only |
| ... | ... | ... | ... | ... |

Key search commands:
- `grep -rn "graphqlRead.query('Engagement'" src/app/core/services/`
- `grep -rn "SmeMartProject" src/app/core/services/engagement-*.service.ts`
- `grep -rn "EngagementVettingItem" src/app/core/services/`
- `grep -rn "Pipeline.receive" src/app/core/services/engagement-*.service.ts` (identify writes to retire)

Include count of grep matches per service to estimate refactoring scope (1-3 refs = small, 4-10 = medium, 10+ = large).
  </action>
  <verify>
    <automated>INVENTORY.md exists with 13+ rows in the service audit table; grep counts present; each service has a disposition (REWRITE/RECONCILE/KEEP/NOT-TOUCHED)</automated>
  </verify>
  <done>All 13 engagement-*.service.ts files classified with grep-backed justification.</done>
</task>

<task type="auto">
  <name>Task 2: MCP describe pins — 6 platform/portal/hydra endpoints</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (append results)</files>
  <read_first>
    @.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md (D-43 MCP targets)
    @.planning/docs/SDK_VERIFICATION_SOURCES.md (MCP describe recipe)
    @.planning/notes/projects-boards-models.md (Nic's quick-ref for expected shapes)
  </read_first>
  <action>
Run `zerobias_describe()` (ZB MCP) for each of the 6 endpoints locked in D-43. Record exact param shapes, required fields, and enum values. Critical questions per brief Open Questions:

1. **`zerobias_describe('portal.Project.search')`** — Does `memberPrincipalId` / member-filter param exist? Exact name?
2. **`zerobias_describe('platform.Project.list')`** — Member-filter param? Exact name? `pageNumber` / `pageSize` naming convention?
3. **`zerobias_describe('platform.Project.create')`** — Confirm required fields are `name`, `status`, `visibility`, `membershipPolicy` per D-31.
4. **`zerobias_describe('platform.Project.addMember')`** — Param shape: `projectId`, `principalId`, `role`? Exact field names and types?
5. **`zerobias_describe('platform.Board.create')`** — Confirm `boardType` enum values include `kanban` and `list`. Confirm `isDefault` field exists.
6. **`zerobias_describe('platform.Task.create')`** — Confirm for vetting Board paired-task shape: `parentId` field exists, `boardId` optional with server fallback chain per brief §"Resolved via MCP describe".

For each call, extract:
- Full request/response schema (copy exact param names, types, enums)
- Required fields list
- Optional field defaults (if any)
- Enum values for Project.status, Project.visibility, Project.membershipPolicy, Board.status, Board.boardType

Append results to INVENTORY.md under "## MCP Describe Pins" section. Format: one subsection per endpoint with YAML-ish schema excerpt.

Reference existing SDK code to verify MCP output matches runtime behavior (check ~/Projects/zb/clients/packages/ source).
  </action>
  <verify>
    <automated>INVENTORY.md contains "## MCP Describe Pins" section with 6 subsections (portal.Project.search, platform.Project.list/create/addMember, platform.Board.create, platform.Task.create); each subsection contains request schema, required fields, enum values</automated>
  </verify>
  <done>All 6 MCP describe pins recorded with exact signatures. Member-filter question (D-23) resolved for both portal.Project.search and platform.Project.list.</done>
</task>

<task type="auto">
  <name>Task 3: Codebase structure audit — other retired classes</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (append results)</files>
  <read_first>
    @.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md (inventory skeleton at section "Inventory bucket")
    @.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md (section "File Classification")
    @src/app/core/services/ (grep for MarketplaceProfileItem, RFP, Bid, Note, Document, Vetting)
  </read_first>
  <action>
Audit the long tail of SMeMart classes beyond the core (Engagement, EngagementVettingItem, SmeMartProject):
- **MarketplaceProfileItem** (vendor profile) — KEEP. Org-anchored identity distinct from engagement metadata. No change needed.
- **RFP, Bid, Note, NoteFolder, Document, Vetting, ServiceOffering** — Grep each class name across src/. Likely all KEEP for v1.4 (marketplace-specific, not engagement-related). Confirm no calls to retire Engagement / SmeMartProject from these services.

Record in INVENTORY.md table:
| Class | Service References | Disposition | Notes |
|-------|-------------------|-------------|-------|
| MarketplaceProfileItem | vendor-profile.service.ts, marketplace-profile.service.ts | KEEP | Org identity, not engagement-related |
| RFP | rfp.service.ts, rfp-wizard.service.ts | KEEP | Marketplace entity, no engagement dependency |
| ... | ... | ... | ... |

If any of the long-tail classes reference retiring Engagement / SmeMartProject, escalate to "RECONCILE" and note for Plan 03.
  </action>
  <verify>
    <automated>INVENTORY.md long-tail section contains 8+ class rows (MarketplaceProfileItem + RFP + Bid + Note + NoteFolder + Document + Vetting + ServiceOffering); each has disposition and notes</automated>
  </verify>
  <done>Long-tail classes audited and partitioned. Confirms scope boundaries for Plan 02-04.</done>
</task>

<task type="auto">
  <name>Task 4: Existing demo data audit + dual-read window spec</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (append results)</files>
  <read_first>
    @.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md (D-13, D-14, D-15 schema retirement; existing W3Geekery + Auditmation Dev demo data)
    @.planning/notes/demo-data-guide.md (current demo data on UAT)
    @.planning/notes/uat-migration-tracker.md (CI→UAT ID mapping)
  </read_first>
  <action>
Document the current demo data state on UAT:
- W3Geekery default engagement (UUID from STATE.md)
- Auditmation Dev engagement (from prior phases)
- Existing SmeMartProject records created by prior phases

Record in INVENTORY.md:
| Demo Data | Created By | Shape | Disposal Plan |
|-----------|-----------|-------|----------------|
| W3Geekery engagement | Phase 26 provisioner | SmeMartProject + Engagement GQL | Dual-read via engagements.service.ts until deprecation |
| Auditmation Dev engagement | Phase XX | SmeMartProject + Engagement GQL | Dual-read via engagements.service.ts until deprecation |

Confirm the dual-read window strategy (D-15):
- Provisioner (Plans 02+) creates **new** records via `platform.Project.create` only (no GQL writes)
- Read paths (Plan 03 engagements.service.ts) consume BOTH `platform.Project.list` AND existing GQL `SmeMartProject` until schema deprecation (D-13)
- Schema PR (Plan 05) deprecates without delete; existing GQL class stays readable for orphan records
- Data ages out organically; no one-shot migration

Add a note to INVENTORY.md: "Dual-read enabled by D-13 deprecate-without-delete. GQL schema classes stay in schema package; app code stops creating new records. Demo data remains readable."
  </action>
  <verify>
    <automated>INVENTORY.md contains "## Existing Demo Data" section with 2-3 demo records listed (W3Geekery, Auditmation); dual-read window strategy described</automated>
  </verify>
  <done>Demo data state documented. Dual-read window spec locked for implementation in Plans 02-03.</done>
</task>

<task type="auto">
  <name>Task 5: Synthesize INVENTORY.md and file backlog entries</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (finalize)</files>
  <read_first>
    @.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md (deferred list: PROVIDER-MY-ENGAGEMENTS-1, PROJECT-SVC-RENAME-1, SCHEMA-RETIREMENT-DELETE-1, [GOVERNANCE-PROJECT-RENDERING])
    INVENTORY.md (draft from Tasks 1-4)
  </read_first>
  <action>
Finalize INVENTORY.md with three top-level sections:

**## Section 1: Codebase Partitioning**
- Service audit table (from Task 1): REWRITE, RECONCILE, KEEP, NOT-TOUCHED services with grep counts
- Summary: "X services REWRITE, Y RECONCILE, Z KEEP, W NOT-TOUCHED"

**## Section 2: MCP Describe Pins**
- 6 platform/portal/hydra endpoint schemas (from Task 2)
- Resolved questions: member-filter param names for portal.Project.search and platform.Project.list

**## Section 3: Schema Retirement & Demo Data**
- Long-tail class audit (from Task 3): MarketplaceProfileItem KEEP, RFP/Bid/Note/Document/Vetting/ServiceOffering KEEP
- Existing demo data (from Task 4): W3Geekery + Auditmation Dev records; dual-read window strategy

**## Deferred Work (File as Backlog)**
Record these as backlog entries (not 29.5 tasks):
- `PROVIDER-MY-ENGAGEMENTS-1` — Medium, v1.5+. Provider-side My Engagements UI (MCP describe shapes spec'd in this INVENTORY; implementation deferred per D-22)
- `PROJECT-SVC-RENAME-1` — Low, organic cleanup. Rename `SmeMartProjectService` → `ProjectService` in v1.5+. Wide cross-cutting diff; defer per D-19
- `SCHEMA-RETIREMENT-DELETE-1` — Medium, post-29.5-closure. Delete `Engagement` / `EngagementVettingItem` / `SmeMartProject` schema classes once dual-read has zero remaining readers
- `[GOVERNANCE-PROJECT-RENDERING]` — Conditional (only files if Plan 06 verification fails). Placeholder Engagement Task + platform-team ask to render platform.Project in Governance UI

Add to INVENTORY.md footer:
```
## Backlog Entries to File (post-29.5-closure)

1. **PROVIDER-MY-ENGAGEMENTS-1** (Medium, v1.5+) — Provider-side My Engagements UI. MCP describe shapes confirmed above; do NOT implement in 29.5 (per D-22). File post-closure.
2. **PROJECT-SVC-RENAME-1** (Low, v1.5+ organic) — Rename SmeMartProjectService → ProjectService (wide diff; defer per D-19).
3. **SCHEMA-RETIREMENT-DELETE-1** (Medium, post-closure) — Delete schema classes once dual-read has zero readers (gated on data age-out, not calendar).
4. **[GOVERNANCE-PROJECT-RENDERING]** (Conditional) — File only if Plan 06 verification fails (Governance UI doesn't surface platform.Project). Includes placeholder Task removal once Governance ships.
```
  </action>
  <verify>
    <automated>INVENTORY.md is complete with 3+ main sections, summary statistics (e.g., "5 services REWRITE, 2 RECONCILE, 6 KEEP"), and "## Backlog Entries" footer with 4 deferred items</automated>
  </verify>
  <done>INVENTORY.md is a comprehensive audit output. Ready for downstream plans (02-08) to consume service disposition, API shapes, and demo-data strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin provisioning → platform APIs | Only Clark + Director invoke provisioning; unauthorized orgs cannot trigger via UI. Verify admin-only guard on Provisioning tab. |
| External service → member-filter shape | MCP describe pins down exact param shapes; no guessing or ad-hoc param invention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.5-01 | Authorization | Provisioning tab | Mitigate | Plan 01 confirms admin guard on SME Mart Provisioning tab (read-only confirmation in INVENTORY.md; no code change required if guard already exists). Plan 02 rewrite must NOT relax this guard. |
| T-29.5-02 | Information Disclosure | platform.Project visibility filter | Accept | `Project.visibility='internal'` exposes project names to org members. Acceptable per D-29 (operator-admins need cross-org read for Governance). No PII in Project.name (locked verbiage uses org name only). |
| T-29.5-03 | Tampering | Schema deprecation without delete | Mitigate | D-13 orphan-read strategy: keep GQL class definition but stop creating new records. Mitigation: verify schema-class-with-no-new-writes remains readable; test dual-read path in Plan 06 verification. |

</threat_model>

<verification>
Plan 01 verification is self-contained (grep output, MCP queries, document generation). No integration with downstream plans required before Plan 02 starts.

Downstream dependency: Plans 02-04 consume INVENTORY.md for service disposition, API shapes, and dual-read strategy. Plan 05 (schema retirement PR) uses INVENTORY.md to confirm class retirement list.
</verification>

<success_criteria>
- INVENTORY.md exists with audit tables for all 13 engagement-*.service.ts files + long-tail classes
- 6 MCP describe pins recorded with exact schemas (no guessing on Project.status enum, Board.boardType options, etc.)
- Member-filter param names resolved for platform.Project.list and portal.Project.search (D-23)
- Demo-data dual-read strategy documented for Plan 03 implementation
- 4 backlog entries identified and ready to file (PROVIDER-MY-ENGAGEMENTS-1, PROJECT-SVC-RENAME-1, SCHEMA-RETIREMENT-DELETE-1, [GOVERNANCE-PROJECT-RENDERING])
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-platform-model-migration/29.5-01-SUMMARY.md` with:
- Audit summary (X services REWRITE, Y RECONCILE, Z KEEP)
- MCP pin results (member-filter param names confirmed)
- Backlog entries ready to file
- Next-plan dependencies (Plans 02-04 ready to start in parallel)
</output>
