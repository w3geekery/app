---
phase: 29.5-platform-model-migration
plan: 04
type: execute
wave: 2
depends_on: [29.5-01]
files_modified:
  - src/app/core/models/resource-type.enum.ts
  - src/app/core/models/index.ts
  - src/app/core/services/engagement-hierarchy.service.ts
  - src/app/core/services/engagement-hierarchy.service.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "ResourceTypeEnum extended with 'project' and 'board' values (D-29, D-30)"
    - "engagement-hierarchy.service correctly returns new resource types in parent/child relationships"
    - "Resource-link rendering handles all resource types including new project/board"
    - "Type checking clean with new enum values"
  artifacts:
    - path: src/app/core/models/resource-type.enum.ts
      provides: "Enum with engagement, provider, project, board, task, document values"
      min_lines: 15
    - path: src/app/core/services/engagement-hierarchy.service.ts
      provides: "Hierarchy traversal returning ProjectExtended + BoardExtended (new types)"
      min_lines: 50
  key_links:
    - from: "resource-type.enum.ts"
      to: "engagement-hierarchy.service.ts"
      via: "enum import + usage in resource type mapping"
      pattern: "ResourceType\\.(project|board)"
    - from: "engagement-hierarchy.service.ts"
      to: "Resource-link rendering (all components)"
      via: "Return type carries resource type in linked object"
      pattern: "linkedResource\\.resourceType|linkedResource\\.type"
---

<objective>
Extend ResourceTypeEnum with new 'project' and 'board' values (D-29, D-30) and update engagement-hierarchy.service to correctly return these types in workspace/vetting hierarchy relationships. This enables downstream components to render new resource types correctly.

Purpose: New platform primitives (Project, Board) must be identifiable by type in all resource-link rendering paths. Enum extension unblocks Phase 30 (Project Board UI) from rendering workspace/vetting hierarchy.

Output: ResourceTypeEnum extended, engagement-hierarchy.service updated with passing specs, type checking clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md
@.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md
@.planning/phases/29.5-platform-model-migration/29.5-01-PLAN.md (INVENTORY.md output)

## Locked decisions
- **D-29:** ResourceTypeEnum adds value 'project' (lowercase, enum discriminator)
- **D-30:** ResourceTypeEnum adds value 'board' (lowercase, enum discriminator)
- **D-31:** workspace = child Project with parentId=engagement.id, resourceType='project'
- **D-32–D-37:** Locked verbiage (not relevant to this plan; see Plan 02)
- **D-04:** Board governance model = org-scoped (ownerId), not boundary-scoped

## Angular 21 Modernization Rules
From CLAUDE.md + .planning/docs/MODERNIZATION_GUIDE.md:
- **NO enum field decorators** (enums are not components/services, so N/A)
- **NO constructor injection in services** → use `inject()` only
- **No @Input/@Output field decorators** → N/A for enum + service pure functions
- **Keep class-level @Injectable({ providedIn: 'root' })** on services
- **Lint gate:** `npm run lint` (runs ESLint with max-warnings=0)

## ZeroBias SDK shapes
- **ProjectExtended** contains `resourceType?: string` (optional); we'll set to 'project' on return
- **BoardExtended** contains `resourceType?: string` (optional); we'll set to 'board' on return
- **Resource-link consumer contracts:** Most components iterate over `linkedResources: { resourceType, id, name }` — enum ensures consistent discriminator
</context>

<tasks>

<task type="auto">
  <name>Task 0: Pre-flight — verify engagement-hierarchy.service partition classification</name>
  <files>.planning/phases/29.5-platform-model-migration/INVENTORY.md (read), .planning/phases/29.5-platform-model-migration/29.5-04-PLAN.md (deviation log if re-scoped)</files>
  <read_first>
    - .planning/phases/29.5-platform-model-migration/INVENTORY.md (Plan 01 deliverable — codebase partition table)
    - src/app/core/services/engagement-hierarchy.service.ts (current implementation; confirms partition decision against real code)
  </read_first>
  <action>
Read INVENTORY.md (Plan 01 deliverable) and locate `engagement-hierarchy.service.ts` in the partition table.

**Decision rule:**

- **If classified REWRITE or RECONCILE** (i.e., currently queries retiring GQL classes `Engagement` / `EngagementVettingItem` / `SmeMartProject`, or otherwise touches the retired surface): proceed with Plan 04 as written — Tasks 1, 2, 3 execute normally (enum extension + service update + renderer verification).

- **If classified NOT-TOUCHED** (platform-already / GQL-non-engagement — doesn't reference retiring classes): re-scope this plan to ResourceTypeEnum changes ONLY (Task 1) and SKIP Task 2 (service file modifications) and any spec changes to engagement-hierarchy.service. Task 3 (renderer verification) still runs but limited to enum-consumer surfaces, not hierarchy-service consumers.

- **If KEEP** (not in any retirement-related bucket and out of 29.5 scope): same as NOT-TOUCHED — re-scope to enum-only.

**Document the re-scope decision (if invoked) in this plan's deviation log:**

If re-scope was triggered, append to the bottom of THIS plan file (`29.5-04-PLAN.md`) under a new `## Deviation Log` section:

```markdown
## Deviation Log

### 2026-MM-DD: Re-scoped Plan 04 to enum-only

**Trigger:** INVENTORY.md classified `engagement-hierarchy.service.ts` as <NOT-TOUCHED|KEEP>.
**Action:** Skipped Task 2 (service file update) and engagement-hierarchy.service spec changes.
**Rationale:** Service does not reference retiring GQL classes; modifying it would be out-of-scope churn under Touch-It-Fix-It (D-40).
**Files no longer modified:** src/app/core/services/engagement-hierarchy.service.ts, src/app/core/services/engagement-hierarchy.service.spec.ts.
**must_haves still satisfied:** ResourceTypeEnum truth (Task 1); resource-link rendering truth (Task 3, narrowed to enum-consumer surfaces).
```

Also update `files_modified` in the frontmatter to drop the engagement-hierarchy.service entries IF re-scope was invoked.
  </action>
  <verify>
    <automated>test -f .planning/phases/29.5-platform-model-migration/INVENTORY.md && grep -E "engagement-hierarchy" .planning/phases/29.5-platform-model-migration/INVENTORY.md</automated>
  </verify>
  <done>
    - INVENTORY.md exists and was read
    - `engagement-hierarchy.service.ts` partition classification identified (REWRITE | RECONCILE | KEEP | NOT-TOUCHED)
    - If REWRITE/RECONCILE: continue with Tasks 1-3 as written
    - If KEEP/NOT-TOUCHED: re-scope applied, deviation log appended to this plan file, `files_modified` frontmatter trimmed
    - Task 0 outcome documented (decision recorded; no silent re-scope)
  </done>
</task>

<task type="auto">
  <name>Task 1: Extend ResourceTypeEnum with 'project' and 'board' values</name>
  <files>src/app/core/models/resource-type.enum.ts, src/app/core/models/index.ts</files>
  <action>
Extend ResourceTypeEnum to include new platform primitives:

1. **Current state (if enum file exists):**
   - Likely contains: engagement, provider, task, document, (possibly others)
   - Is used by resource-link rendering, components, etc.

2. **Required changes:**
   - Add value `project = 'project'` (string literal for discriminator)
   - Add value `board = 'board'` (string literal for discriminator)
   - Keep all existing values intact (no deletions)
   - Update barrel export in `index.ts` if ResourceTypeEnum is exported there

3. **Implementation:**
   ```typescript
   export enum ResourceType {
     engagement = 'engagement',
     provider = 'provider',
     project = 'project',      // NEW D-29
     board = 'board',          // NEW D-30
     task = 'task',
     document = 'document',
     // ... existing values
   }
   ```

4. **No breaking changes to callers:**
   - Existing enum references (engagement, provider, task, document) continue to work
   - New values add options; don't force existing code to change

5. **Export verification:**
   - Ensure ResourceType is exported from src/app/core/models/index.ts
   - Grep for any hardcoded string literals 'engagement' | 'provider' | 'task' that should be enum refs (optional optimization, not blocking)

6. **Angular 21 compliance:**
   - Enum files don't have decorators; N/A here
  </action>
  <verify>
    <automated>npm run lint -- src/app/core/models/resource-type.enum.ts && npx tsc -p tsconfig.spec.json --noEmit | grep -c "resource-type.enum" || echo "0"</automated>
  </verify>
  <done>
    - ResourceTypeEnum contains both 'project' and 'board' values
    - Enum values use lowercase string literals (D-29, D-30)
    - Exported from src/app/core/models/index.ts
    - No type errors in tsconfig.spec.json
    - Linting clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Update engagement-hierarchy.service to return new resource types</name>
  <files>src/app/core/services/engagement-hierarchy.service.ts</files>
  <action>
Update engagement-hierarchy.service to correctly identify and return new ProjectExtended and BoardExtended resource types in hierarchy traversal.

1. **Current state:**
   - Likely has methods like `getHierarchy()`, `getParentEngagement()`, `getWorkspacesForEngagement()`, etc.
   - May currently cast all relationships to generic resource or single type
   - Does not distinguish between Project and Board types

2. **Required changes:**

   a) **In hierarchy traversal methods:**
      - When returning ProjectExtended (workspace), set/verify `.resourceType = ResourceType.project`
      - When returning BoardExtended (vetting board), set/verify `.resourceType = ResourceType.board`
      - Preserve existing method return types; add type unions if needed:
        ```typescript
        getHierarchy(): Promise<(ProjectExtended | BoardExtended)[]>
        getWorkspacesForEngagement(engagementId): Promise<ProjectExtended[]>
        getVettingBoardForProject(projectId): Promise<BoardExtended>
        ```

   b) **Shape mapping logic:**
      - If ProjectExtended/BoardExtended come from platform SDK, they may not have resourceType pre-set
      - Add explicit assignment: `project.resourceType = ResourceType.project`
      - Same for boards: `board.resourceType = ResourceType.board`

   c) **No breaking changes to callers:**
      - Callers likely iterate over `.linkedResources[].resourceType` to drive UI
      - New types enable new rendering branches (phase 30), but don't break existing ones

3. **Angular 21 compliance:**
   - No constructor injection changes needed (service already using inject() pattern, per PATTERNS.md)
   - No @Input/@Output involved (pure service functions)
   - Keep `@Injectable({ providedIn: 'root' })`

4. **Test coverage:**
   - Update existing specs to verify resourceType is set correctly on returned objects
   - Add spec: "getHierarchy() returns projects with resourceType='project' and boards with resourceType='board'"
   - No new test files needed; update existing engagement-hierarchy.service.spec.ts
  </action>
  <verify>
    <automated>npm run lint -- src/app/core/services/engagement-hierarchy.service.ts && npm test -- --include='**/engagement-hierarchy.service.spec.ts'</automated>
  </verify>
  <done>
    - engagement-hierarchy.service methods return ProjectExtended with resourceType='project'
    - engagement-hierarchy.service methods return BoardExtended with resourceType='board'
    - All specs pass (existing + updated for new type verification)
    - Linting clean
    - Type checking clean (return types updated)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify resource-link rendering handles new types (no code changes expected)</name>
  <files>src/app/core/services/engagement-hierarchy.service.spec.ts</files>
  <action>
Light verification task: confirm all resource-link rendering code paths can handle 'project' and 'board' resource types without changes.

1. **Scope:**
   - Search for components that iterate over `.linkedResources` or similar and render based on `.resourceType`
   - Common patterns: `switch(resource.resourceType)` or `*ngIf="resource.resourceType === 'engagement'"` checks
   - Expected files: resource-link.component, resource-card.component, hierarchy-view.component, etc.

2. **Verification steps:**
   - Grep for `resourceType === 'engagement'` or `case ResourceType.engagement:` to find render branches
   - Confirm each branch has `default:` or `else` clause that gracefully handles unknown types
   - No changes needed if render logic is generic (e.g., just displays name + ID)
   - IF specific rendering for 'project'/'board' is needed, that's Phase 30 work (Default Project Board UI), not 29.5

3. **Expected finding:**
   - Most resource-link rendering is generic enough to handle new types without changes
   - If a component explicitly checks `resourceType === 'engagement'` and has no default, add default clause that treats 'project'/'board' as generic resources
   - No major refactoring expected; this is a smoke test

4. **Test coverage:**
   - Add spec in engagement-hierarchy.service.spec.ts: "getHierarchy() includes projects and boards with correct resourceType values"
   - This spec validates the service returns correct types; UI rendering tests are Phase 30's domain

5. **No implementation changes expected:**
   - If rendering already supports generic resource types, pass the test
   - If rendering explicitly checks for known types, add default clause (one-line fix, if needed)
  </action>
  <verify>
    <automated>grep -r "resourceType.*===" src/app --include="*.component.ts" | wc -l && npm test -- --include='**/engagement-hierarchy.service.spec.ts'</automated>
  </verify>
  <done>
    - Verified all resource-link rendering code paths handle unknown resourceType gracefully
    - Either generic rendering (no changes) OR default clause added (if specific branches existed)
    - Spec added: "getHierarchy() returns objects with correct resourceType values"
    - All specs pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engagement-hierarchy.service ↔ platform.Project/Board SDK | Untrusted resource objects; must validate resourceType field before rendering |
| resourceType enum ↔ UI rendering | String discriminator must match renderer expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.5-10 | **I** (Information Disclosure) | resourceType enum values | Mitigate | Enum values are public domain (resource type is displayed in UI); no secrets exposed |
| T-29.5-11 | **T** (Tampering) | engagement-hierarchy.service resource type assignment | Mitigate | resourceType field set explicitly by service, not from untrusted input; enum discriminator prevents invalid values |
| T-29.5-12 | **R** (Repudiation) | Unknown resourceType in rendering | Mitigate | Add default clause to all resource-type switches; log unrecognized types to debug channel for Phase 30 verification |

</threat_model>

<verification>
**Phase 29.5 Wave 2 Completion Checklist (Plan 04):**

1. **Enum extended:**
   ```bash
   grep -E "project.*=|board.*=" src/app/core/models/resource-type.enum.ts
   ```
   Expected: 2+ matches (project = 'project', board = 'board')

2. **Exported from barrel:**
   ```bash
   grep "ResourceType" src/app/core/models/index.ts
   ```
   Expected: ResourceType export present

3. **Service updated:**
   ```bash
   grep -c "resourceType.*=" src/app/core/services/engagement-hierarchy.service.ts
   ```
   Expected: 2+ assignments (one for project, one for board)

4. **Tests pass:**
   ```bash
   npm test -- --include='**/engagement-hierarchy.service.spec.ts' --include='**/resource-type.enum.spec.ts' || true
   ```
   Expected: all green (enum tests if they exist, service tests updated)

5. **Type checking clean:**
   ```bash
   npx tsc -p tsconfig.spec.json --noEmit | grep -i "resource-type\|engagement-hierarchy" || echo "0 errors"
   ```
   Expected: 0 errors

6. **Linting clean:**
   ```bash
   npm run lint -- src/app/core/models/resource-type.enum.ts src/app/core/services/engagement-hierarchy.service.ts
   ```
   Expected: 0 errors, 0 warnings
</verification>

<success_criteria>
Phase 29.5 Wave 2 (Plan 04) is complete when:

- ResourceTypeEnum contains 'project' and 'board' values (lowercase strings, D-29, D-30)
- engagement-hierarchy.service returns ProjectExtended objects with `.resourceType = 'project'`
- engagement-hierarchy.service returns BoardExtended objects with `.resourceType = 'board'`
- All existing resource-link rendering components verified to handle new types (generic rendering OR default clause added)
- All specs pass (engagement-hierarchy service tests + resource-type enum tests if present)
- Linting clean (0 errors, 0 warnings)
- Type checking clean (npx tsc -p tsconfig.spec.json --noEmit)
- Touch-It-Fix-It compliance: any files modified have all modernization violations fixed in same change
- No breaking changes to callers (new enum values add options; don't force refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-platform-model-migration/29.5-04-SUMMARY.md` with:

1. **Enum extended:** ResourceTypeEnum values added (project, board)
2. **Service updated:** engagement-hierarchy.service methods now return correct resourceType values
3. **Renderer verification:** Count of components checked for resourceType handling + any default clauses added
4. **Test coverage:** Specs updated + count of new tests
5. **Modernization compliance:** Lint + tsc output (0 errors)
6. **Blockers:** Any rendering components that fail gracefully with new types identified for Phase 30 work
</output>
