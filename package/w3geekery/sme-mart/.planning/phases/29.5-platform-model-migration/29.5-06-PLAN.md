---
phase: 29.5-platform-model-migration
plan: 06
type: execute
wave: 4
depends_on: [29.5-02, 29.5-05]
files_modified: []
autonomous: false
requirements: []
user_setup:
  - service: UAT ZeroBias environment access
    why: "Verification requires creating test org + provisioning engagement via SME Mart + confirming platform.Project surfaces in ZB Governance UI"
    dashboard_config:
      - task: "Access UAT.zerobias.com (uat.zerobias.com/governance)"
        location: "ZB Governance dashboard"

must_haves:
  truths:
    - "Test engagement provisioned via SME Mart creates a platform.Project"
    - "platform.Project (test engagement) surfaces in ZB Governance UI under Projects list"
    - "platform.Project hierarchy (engagement + workspace) renders correctly in Governance"
    - "ZB Task (Engagement Task, per D-08 speculation) can optionally be omitted from plan (if verified unnecessary)"
  artifacts:
    - path: "(none in code; UAT verification only)"
      provides: "Governance UI smoke test results"
  key_links:
    - from: "SME Mart provisioner.service (Plan 02)"
      to: "ZB Governance Projects view (Platform)"
      via: "provisioning creates platform.Project via SDK → immediately queryable in ZB UI"
      pattern: "platform.Project.*ownerId|boundary"
---

<objective>
Verify that engagement provisioning via SME Mart (Plan 02 rewrite) correctly creates platform.Project resources that are immediately visible + queryable in ZeroBias Governance UI. This is a critical prerequisite for D-08 speculative-drop decision: if platform.Project does NOT surface in Governance, the drop decision must be reversed (Engagement Task retained in provisioner recipe).

Purpose: D-08 proposes dropping Step B (create Engagement Task) based on assumption that platform.Project serves as the governance entity. This checkpoint validates that assumption. If Governance UI cannot find platform.Project, Plan 06 files conditional backlog entry [GOVERNANCE-PROJECT-RENDERING] for Phase 32 (Governance rendering work), and reverses D-08 (step B NOT dropped).

Output: UAT smoke test results (pass/fail) + conditional backlog entry if verification fails + sign-off confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.5-platform-model-migration/29.5-CONTEXT.md
@.planning/phases/29.5-platform-model-migration/29.5-PATTERNS.md
@.planning/phases/29.5-platform-model-migration/29.5-02-PLAN.md (provisioner recipe + constants)

## Locked decisions
- **D-08:** Engagement Task (Step B in provisioner recipe) is speculative-drop: "remove IF platform.Project surfaces in Governance"
- **D-04:** platform.Project governance model: ownerId=org, boundary-scoped via parent link
- **Provisioner output (Plan 02):** Steps A/C/D/F/G create: org + boundary + engagement Project + workspace Project + vetting Board; Step B is NOT created (dropped)

## ZeroBias Governance assumptions
- **Platform.Project visibility:** Should appear in Governance UI under Projects view after creation
- **Query path:** Governance uses platform.getProject() or listProjects() to populate UI
- **Timing:** Immediate (no async delay); project queryable within seconds of creation

## Risk mitigation
- If platform.Project DOES surface in Governance → Plan 06 passes, D-08 confirmed valid (step B delete is safe)
- If platform.Project DOES NOT surface → Plan 06 fails, conditional backlog entry created, D-08 reversed (step B retained in Plan 02 revision)
- Revision cost: revert provisioner Plan 02 to include step B (no other changes)
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Deploy Plan 02 (provisioner rewrite) to UAT environment</name>
  <what-built>
    - Plan 02 provisioner.service rewrite is complete + tested locally
    - Code is committed to `poc/sme-mart` branch
    - Cross-fork PR created: w3geekery/app:poc/sme-mart → zerobias-org/app:uat
  </what-built>
  <how-to-verify>
    1. Check GitHub: https://github.com/zerobias-org/app/pulls (zerobias-org upstream)
    2. Find open PR from w3geekery/app:poc/sme-mart → uat
    3. Verify PR title mentions "Platform Engagement Provisioner Rewrite" or Plan 02
    4. Verify CI passes (all checks green)
    5. Verify build/test logs show 0 failures
  </how-to-verify>
  <resume-signal>
    Once PR is merged to zerobias-org/app:uat, confirm: "PR merged to uat; SME Mart UAT build has latest provisioner rewrite." Then proceed to Task 2.
  </resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: UAT smoke test — provision test engagement via SME Mart</name>
  <what-built>
    - SME Mart UAT deployment is live (uat.zerobias.com/sme-mart)
    - Latest provisioner code deployed
    - User is logged in as admin (required for provisioning access)
  </what-built>
  <how-to-verify>
    Steps:
    1. Navigate to: `https://uat.zerobias.com/sme-mart/provisioning` (or admin panel with provisioning tab)
    2. Click "Provision New Engagement" (or equivalent button)
    3. Fill form: name="Test Engagement 29.5", description="Verification test for D-08", boundary=UAT test boundary
    4. Click "Provision" and watch for success message
    5. After provisioning completes, note:
       - Engagement ID (UUID)
       - Workspace Project ID (UUID)
       - Creation timestamp (for later log inspection)
  </how-to-verify>
  <resume-signal>
    After successful provisioning, report back with:
    - Engagement name created
    - Engagement UUID
    - Workspace Project UUID (if visible in response)
    - Creation success message (screenshot or text)
    Then proceed to Task 3.
  </resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: UAT smoke test — verify platform.Project in Governance UI</name>
  <what-built>
    - Test engagement provisioned (Task 2 complete)
    - ZB Governance UI is accessible
    - User has admin permissions to view all orgs' projects
  </what-built>
  <how-to-verify>
    Steps:
    1. Navigate to: `https://uat.zerobias.com/governance` (or Governance dashboard)
    2. Look for "Projects" section / view
    3. Search or filter for test engagement name ("Test Engagement 29.5")
    4. Verify:
       - **PASS:** Engagement project appears in list with correct name + description
       - **PASS:** Can click into project detail and see hierarchy (engagement + workspace children)
       - **PASS:** Project ownerId matches expected org
       - **FAIL:** Project does not appear at all (after 30-second refresh wait)
       - **FAIL:** Project appears but is not queryable (error when clicking detail)
    5. Screenshot the Governance Projects list (success case)
  </how-to-verify>
  <resume-signal>
    Report back with one of:
    - "PASS: Engagement project visible in Governance Projects list. Name, description, hierarchy all correct. D-08 assumption confirmed." [includes screenshot]
    - "FAIL: Engagement project NOT visible in Governance Projects list after 30s refresh. D-08 assumption INVALID. Reverting provisioner Plan 02 to retain Step B. Creating conditional backlog entry [GOVERNANCE-PROJECT-RENDERING]." [includes screenshot of empty list]
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SME Mart provisioner ↔ platform.Project API | Provisioner creates projects; Governance queries them; must validate shape + visibility |
| UAT environment ↔ prod | Verification on UAT does not guarantee prod behavior; config differences may exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.5-16 | **R** (Repudiation) | governance rendering of platform.Project | Mitigate | If project doesn't surface in Governance, log as known issue (backlog entry [GOVERNANCE-PROJECT-RENDERING]); do not silently drop Engagement Task (revert Plan 02) |
| T-29.5-17 | **T** (Tampering) | provisioner creates correct project shape | Mitigate | Verify platform.Project matches expected fields (name, description, ownerId, boundary); log any shape mismatches to backlog |

</threat_model>

<verification>
**Phase 29.5 Wave 4 Governance Verification Checkpoint:**

After Task 3 (checkpoint:human-action):

**Success path (platform.Project visible in Governance):**
- [ ] Test engagement provisioned via SME Mart provisioning UI
- [ ] platform.Project created with correct name/description
- [ ] Project visible in ZB Governance Projects list
- [ ] Project detail queryable + hierarchy renders correctly
- [ ] D-08 assumption CONFIRMED (step B delete safe)
- [ ] Plan 06 passed; proceed to Plan 07 (Wave 4)

**Failure path (platform.Project NOT visible in Governance):**
- [ ] Test engagement provisioned; platform.Project API calls succeeded (no errors in SME Mart UI)
- [ ] platform.Project NOT visible in Governance Projects list (30+ second refresh)
- [ ] D-08 assumption INVALID; step B must be retained
- [ ] Action: File conditional backlog entry [GOVERNANCE-PROJECT-RENDERING] for Phase 32
- [ ] Action: Revert provisioner Plan 02 to include Step B (Engagement Task creation)
- [ ] Action: Update Plan 02 SUMMARY.md with revision note + backlog entry details
</verification>

<success_criteria>
Phase 29.5 Wave 4 (Plan 06 governance verification) is complete when:

**SUCCESS case:**
- platform.Project created by provisioner is immediately visible in ZB Governance UI
- Project detail view renders hierarchy correctly
- D-08 assumption validated (step B delete is safe)
- Plan 06 SUMMARY.md documents PASS + screenshots

**FAILURE case (with mitigation):**
- platform.Project NOT visible in Governance UI (even after 30s refresh)
- Plan 02 provisioner.service.ts REVERTED to include Step B (Engagement Task creation)
- Conditional backlog entry [GOVERNANCE-PROJECT-RENDERING] created in BACKLOG.md
- Plan 02 SUMMARY.md updated with revision note + reason (governance visibility gap)
- Plan 06 SUMMARY.md documents FAIL + screenshots + revision details
- D-08 decision note updated: "Reversal: Engagement Task (Step B) RETAINED due to Governance rendering gap (Phase 32 work)"

In either case, Plan 06 is complete when verification is done + decision is locked + any revisions are committed.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-platform-model-migration/29.5-06-SUMMARY.md` with:

1. **Verification path:** SUCCESS or FAILURE
2. **Engagement provisioned:** Name, UUID, timestamp
3. **Governance UI result:** Screenshots (success list view OR empty list showing no match)
4. **D-08 decision:** Confirmed safe (project visible) OR Invalid (project not visible, step B reverted)
5. **Revisions applied:** If FAILURE, document Plan 02 reversion details + backlog entry created
6. **Signature:** Verification completed by executor agent with timestamp

**If FAILURE + reversion:** Immediately commit Plan 02 revision + backlog entry update + create errata under `.planning/director/errata/` documenting the governance gap discovery.
</output>
