---
phase: 29.5-platform-model-migration
type: closure
authored: 2026-05-12
authored-by: gsd-execute (Claude Opus 4.7), Plan 08 Wave 5
status: COMPLETE
---

# Phase 29.5: Platform Model Migration — Closure Report

**Goal:** Replace SmeMart GQL classes (`Engagement`, `EngagementVettingItem`, `SmeMartProject`) with `platform.Project` / `platform.Board` / `platform.Task` primitives. Engagement-as-Project hierarchy locked; provisioning recipe collapses 7→5 steps; zero new GQL classes added.

**Status:** COMPLETE — Wave 5 closed 2026-05-12.

---

## Locked Decision Triad (ratified during 29.5)

| Decision | Substance | Where landed |
|----------|-----------|--------------|
| **D-46** | Engagement-as-Project hierarchy (Engagement Project at depth 1; Project tier at depth 2). Phase 30 brief at commit `b7f9b80` predates this and references the prior tier-tag assumption — **brief needs revision post-closure**. | `.planning/director/DECISIONS.md` |
| **D-49** | Tag namespace flip from `sme-mart.eng.*` (legacy) → `sme-mart.engagement.*` (new) for engagement-identity tags. Code still on legacy namespace; out-of-scope for v3 amendment. | `.planning/director/DECISIONS.md`, errata 030 |
| **D-50** | Depth-2 tier is named "Project" (not "Workspace"). v3 amendment surgically flipped the Plan 02 recipe to lock `sme-mart.tier.project`. | `.planning/director/DECISIONS.md`, commit `523e924` |

D-48 (Project Lead cascade) also materially clarified during 29.5 — see errata 033 + DECISIONS.md addendum: cascade is **eager-materialize-per-Project**, not lazy-resolve-via-parent-chain.

---

## Plans 01-08 Status

| Plan | Wave | Status | Key artifact |
|------|------|--------|--------------|
| 01 — INVENTORY audit + MCP endpoint verification | 1 | COMPLETE | `29.5-01-SUMMARY.md`, `INVENTORY.md` |
| 02 — Provisioner.service rewrite (7→5 steps) + v3 amendment | 2 | COMPLETE | `29.5-02-SUMMARY.md`, commit `523e924` (v3) |
| 03 — Service refactors (engagements, sme-mart-project, demo-visibility) + dual-read | 2 | COMPLETE | `29.5-03-SUMMARY.md` |
| 04 — ResourceType enum extension (`project`, `board`) — re-scoped enum-only per Task 0 KEEP classification | 2 | COMPLETE | `29.5-04-SUMMARY.md`, commit `999aeeb` |
| 05 — Schema deprecation PR on `zerobias-org/schema` | 3 | COMPLETE (PR opened, review out-of-band per Director rescind of stall-clock) | `29.5-05-SUMMARY.md` |
| 06 — UAT Engagement/Project SDK round-trip verification | 4 | **PASS** (5/5 SDK assertions) — UI cross-check staged for Clark, deferred (not a 29.5 blocker) | `29.5-06-SUMMARY.md` |
| 07 — UAT Vetting Board smoke | 4 | **PREMISE-OBSOLETE / DEFERRED via Director Option A** — see [errata 031](../../director/errata/031-vetting-platform-board-not-implemented.md) | `29.5-07-SUMMARY.md` |
| 08 — Final cleanup + closure | 5 | COMPLETE | This file, `29.5-08-SUMMARY.md` |

---

## Wave 4 Errata (all linked, all open follow-ups)

| Errata | Title | Disposition |
|--------|-------|-------------|
| [030](../../director/errata/030-d49-namespace-drift-code-vs-uat-artifact.md) | D-49 namespace drift code-vs-uat-artifact | Backlog entry `D-49-NAMESPACE-MIGRATE-1` filed (Platform Alignment); **hard prerequisite for Phase 31** |
| [031](../../director/errata/031-vetting-platform-board-not-implemented.md) | Vetting `platform.Board` not implemented | Backlog entry `VETTING-PLATFORM-MIGRATE-1` filed (Platform Alignment, v1.5+); Plan 07 PREMISE-OBSOLETE |
| [032](../../director/errata/032-platform-project-get-omits-parentid-for-top-level.md) | `platform.Project.get` omits `parentId` for top-level Projects | Touch-It-Fix-It on next `provisioner.spec` edit (assertion drift: `.toBeNull()` → `.toBeFalsy()`/`.toBeUndefined()`) |
| [033](../../director/errata/033-d48-cascade-mechanism-eager-not-lazy.md) | D-48 cascade mechanism is eager-materialize, not lazy | DECISIONS.md D-48 addendum landed; **open question:** does eager-cascade fire on later add-Lead-to-parent? (post-closure investigation when next adding Leads to existing Engagement Projects) |

---

## Gate Results — HEAD (commit `e39b236`)

All gates clean at the time of closure.

| Gate | Command | Result |
|------|---------|--------|
| Lint (29.5 modified files) | `npx eslint --max-warnings=0 <29.5 modified source files>` | **0 errors, 0 warnings** |
| tsc (app config) | `npx tsc -p tsconfig.app.json --noEmit` | **0 errors** (clean) |
| tsc (spec config — canonical) | `npx tsc -p tsconfig.spec.json --noEmit` | **0 errors** (clean) |
| Targeted tests (Plans 02-04 modified specs) | `npm test -- --include='**/platform-engagement-provisioner.service.spec.ts' --include='**/engagements.service.spec.ts' --include='**/sme-mart-project.service.spec.ts'` | **3 test files passed, 50/50 tests passed** (1.75s) |

**Note on full-repo lint:** `npm run lint` (which runs `eslint .` against the entire repo) reports ~1430 errors. These are **pre-existing** modernization-rule violations across files NOT touched by Phase 29.5, tracked under `MODERN-CLEANUP-1` per the Touch-It-Fix-It enforcement model. Diff-based pre-commit + CI lint enforce 0-errors against modified files only. Phase 29.5's own modified files are clean.

**Phase 29.5 modified source files (sealed at this commit, lint-clean):**
- `src/app/core/models/enums.ts`
- `src/app/core/models/index.ts`
- `src/app/core/services/demo-visibility.service.ts`
- `src/app/core/services/engagements.service.ts` (+ spec)
- `src/app/core/services/platform-engagement-provisioner.service.ts` (+ spec)
- `src/app/core/services/sme-mart-project.service.ts` (+ spec)
- `src/app/core/services/wave-1-integration.spec.ts`
- `src/app/pages/admin/tabs/org-provisioning-tab.component.ts`
- `src/app/pages/my-engagements/my-engagement-list.component.spec.ts`

Note: `engagement-hierarchy.service.ts` was NOT modified during Phase 29.5 (Plan 04 was re-scoped to enum-only per Task 0 KEEP classification — INVENTORY.md showed the service does not reference retiring GQL classes). Pre-existing lint errors there belong to `MODERN-CLEANUP-1`, not Touch-It-Fix-It under 29.5.

---

## Plan 06 Disposition — SDK PASS, UI Deferred

Plan 06 verified five SDK round-trip assertions against live UAT artifacts and **all five PASSED**:

1. `platform.Project.get(<Engagement>)` — D-32 name + D-33 description + boardCount=1 + memberCount>=1 + `tag.name = sme-mart.engagement.zerobias-to-w3geekery` (D-49 NEW namespace, per live artifact).
2. `platform.Project.get(<Project tier>)` — D-34 name ("ZeroBias Platform", NOT "Workspace" per D-50) + D-35 description + `tag.name = sme-mart.tier.project` + parentId chains to Engagement.
3. `platform.Project.list({ownerId: W3Geekery})` — both artifacts surface.
4. `platform.Project.listMembers(<Project tier>)` — Clark appears as Project Lead.
5. `platform.Project.listMembers(<Engagement>)` — Clark appears as explicit Project Lead member (eager-cascade per D-48 addendum).

UI cross-check (browser-based: `/my-engagements` list, `/my-engagements/<id>` detail verbiage rendering, depth-2 child surface) is **staged for Clark** with specific URLs and expected D-32/D-33/D-34/D-35 verbiage in `29.5-06-SUMMARY.md`. **This is deferred, not blocking** — Plan 06's substantive verification is the SDK contract, which is GREEN. UI rendering correctness is a separate concern Clark will run when he next sits in front of a browser.

Conditional backlog entry `[GOVERNANCE-PROJECT-RENDERING]` from the Plan 08 pre-authored scope was **dropped** — its filing condition was "Plan 06 verification FAILED", which did not occur.

---

## Plan 07 Disposition — PREMISE-OBSOLETE / DEFERRED via Director Option A

Plan 07 was authored before the v3 amendment landed. Its premise — a vetting `platform.Board` (`boardType="list"`, `isDefault=false`) lazy-created on first vetting requirement per D-12 — does **not** match current SME Mart code architecture. Empirical grep confirmed zero references to `platform.Board` / `platform.Task` anywhere in `vetting.service.ts` or the Vetting view components; SME Mart's vetting flow continues on the legacy GQL `EngagementVettingItem` Pipeline.receive path.

This is **NOT** a SME Mart code defect, NOT a platform bug, NOT an SDK contract failure. It is a plan-vs-current-architecture mismatch: D-12's vetting `platform.Board` pattern is forward-looking and was never implemented; v3 amendment correctly excluded vetting modernization from its surgical scope.

**Director Option A accepted (2026-05-12, errata 031):**
- Plan 07 status recorded here as **PREMISE-OBSOLETE / DEFERRED**.
- Backlog entry `VETTING-PLATFORM-MIGRATE-1` filed by Director with broader scope (both vetting-items-to-Task migration AND vetting-Board lazy-create) at Platform Alignment section of BACKLOG.md.
- Conditional backlog entry `[VETTING-BOARD-RENDERING]` from Plan 08 pre-authored scope is **dropped** as superseded — the rendering-only entry was narrower than what Option A actually requires.
- No Wave 4 blocker. Plan 06 SDK assertions are the substantive 29.5 verification.

---

## Schema PR (Plan 05) Coordination — Director-managed

Per the Director rescind of the prior "Schema PR Stall Escape Clause" (5-day stall threshold, `29.5b` carry-over template, calendar checks) — **there is no stall clock**. Plan 05's closure criterion is "PR opened on `zerobias-org/schema`" plus normal review. Clark manages review cadence out-of-band; merge status at closure time is the Director's call.

At time of writing (2026-05-12 Wave 5 close), the schema PR's merge state is whatever it is. **Phase 29.5 closes on app-side state alone** — the schema deprecation work is upstream and follows its own review cadence. If merge is still pending at the Director's cross-fork PR step (poc/sme-mart → zerobias-org/app:uat), that is handled out-of-band per Clark's escalation.

---

## Backlog Entries Created or Recognized at Closure

Three **organic** entries added to `BACKLOG.md` by this plan (Platform Alignment section):

| ID | Scope | Priority | Target |
|----|-------|----------|--------|
| `PROVIDER-MY-ENGAGEMENTS-1` | Swap provider engagement UI from GQL SmeMartProject reads to `platform.Project.list` | Medium | v1.5+ (organic) |
| `PROJECT-SVC-RENAME-1` | Rename `SmeMartProjectService` → `PlatformProjectService` + swap callers (now safe; service is platform-native) | Low | v1.5+ (organic) |
| `SCHEMA-RETIREMENT-DELETE-1` | Delete `SmeMartProject` + `EngagementVettingItem` schema classes (not just deprecation) | Medium | Post-closure; blocked by dual-read window (after Phase 31) |

Two **conditional** entries from the Plan 08 pre-authored scope are **dropped**:
- `[GOVERNANCE-PROJECT-RENDERING]` — condition (Plan 06 FAILED) not met; Plan 06 PASSED.
- `[VETTING-BOARD-RENDERING]` — superseded by `VETTING-PLATFORM-MIGRATE-1` (Director filed with broader scope per errata 031); narrower rendering-only entry not re-added.

Two **Director-filed** entries recognized but not re-added by this plan (already in Platform Alignment):
- `D-49-NAMESPACE-MIGRATE-1` (commit `e39b236`) — hard prerequisite for Phase 31.
- `VETTING-PLATFORM-MIGRATE-1` (commit `e39b236`) — v1.5+.

---

## Phase 30 Readiness

**Status:** Phase 29.5 closes complete. Phase 30 brief at commit `b7f9b80` predates the D-46 / D-49 / D-50 ratification triad and references prior tier-tag assumptions that were invalidated during 29.5. **The brief needs revision post-closure** — Director will rewrite separately. ROADMAP acknowledges this revision-needed state.

Substantive readiness:
- ✅ Platform primitives (Project, Board, Task) in place + verified at SDK level on UAT (Plan 06).
- ✅ Service layer refactored (engagements + sme-mart-project + demo-visibility on dual-read paths).
- ✅ Provisioner v3 (5-step recipe) validated.
- ✅ ResourceType enum extended (project, board).
- ⚠ Vetting modernization deferred to `VETTING-PLATFORM-MIGRATE-1` (v1.5+) — Phase 30 does not depend on this.
- ⚠ D-49 namespace migration deferred to `D-49-NAMESPACE-MIGRATE-1` — **Phase 31** hard prerequisite, **not** a Phase 30 blocker.
- ⚠ Phase 30 brief rewrite needed (D-46 / D-50 invalidate prior tier-tag assumptions in `b7f9b80`).

Phase 30 can proceed once the brief is rewritten. No 29.5 outstanding work blocks it.

---

## Open Follow-Ups

1. **`D-49-NAMESPACE-MIGRATE-1`** (Medium) — Hard prerequisite for Phase 31. See errata 030 + Platform Alignment entry.
2. **`VETTING-PLATFORM-MIGRATE-1`** (Medium) — Target v1.5+. See errata 031 + Platform Alignment entry.
3. **Errata 032** — Touch-It-Fix-It on next `provisioner.spec` edit: assertion `.toBeNull()` → `.toBeFalsy()` or `.toBeUndefined()` for `parentId` round-trip consistency.
4. **Errata 033 open question** — Does the eager-cascade D-48 mechanism fire on a later `addMember(parent, role=Lead)` call, or only on `Project.create`? Investigate when next adding Leads to existing Engagement Projects.
5. **Plan 06 UI cross-check** (Clark, browser session) — Three staged checks in `29.5-06-SUMMARY.md`; deferred, not blocking.
6. **Phase 30 brief rewrite** (Director, post-closure) — Reflect D-46/D-49/D-50 ratification; rewrite supersedes commit `b7f9b80`.

---

## Sign-Off

- Phase Lead (execution): gsd-execute (Claude Opus 4.7, this terminal)
- Phase Lead (architecture/QA): Director Parks
- Phase Lead (project ownership): Clark Stacer
- Closure date: 2026-05-12
- **Status: COMPLETE — READY FOR PHASE 30** (pending Director rewrite of Phase 30 brief)
