---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/core/constants/tier-tags.ts
  - src/app/core/services/platform-engagement-provisioner.service.ts
autonomous: true
requirements: [PB-01, PB-02, PB-03, PB-04, PB-06, PB-07]
must_haves:
  truths:
    - "The tier-tag identity for the SME Mart Project tier is sourced from one shared module so the dashboard, the provisioner, and future consumers stay in lock-step"
    - "Existing imports of SME_MART_TIER_PROJECT_TAG_ID from the provisioner continue to resolve correctly (caller stability)"
    - "Constant value (UAT UUID) matches D-50 canonical mapping"
  artifacts:
    - path: "src/app/core/constants/tier-tags.ts"
      provides: "Shared tier-tag UUID constants (Phase 30 + 31 consumers)"
      min_lines: 10
    - path: "src/app/core/services/platform-engagement-provisioner.service.ts"
      provides: "Re-export of SME_MART_TIER_PROJECT_TAG_ID for backward compatibility"
      contains: "export { SME_MART_TIER_PROJECT_TAG_ID }"
  key_links:
    - from: "src/app/core/services/engagements.service.ts"
      to: "src/app/core/constants/tier-tags.ts"
      via: "import SME_MART_TIER_PROJECT_TAG_ID"
      pattern: "import.*SME_MART_TIER_PROJECT_TAG_ID"

---

<objective>
Hoist the SME_MART_TIER_PROJECT_TAG_ID constant from platform-engagement-provisioner.service to a shared constants module, and establish re-export stability.

Purpose: Phase 30 (engagements.service helpers) and Phase 31 (auto-reprovision) both need access to the Project-tier identity tag UUID. Moving it to a shared constants file eliminates coupling to the provisioner and creates a single source of truth.

Output: New `src/app/core/constants/tier-tags.ts` with D-50 locked constant; re-export from provisioner for backward compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-default-project-board-coming-soon-placeholders/30-CONTEXT.md
@.planning/phases/30-default-project-board-coming-soon-placeholders/30-PATTERNS.md
@.planning/docs/MODERNIZATION_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tier-tags.ts constants module</name>
  <files>src/app/core/constants/tier-tags.ts</files>
  <read_first>
    - File being created (new)
    - Analog: `src/app/core/constants/demo-tags.ts` (existing constants file with comment block pattern)
    - Source: `src/app/core/services/platform-engagement-provisioner.service.ts:19` — current home of `SME_MART_TIER_PROJECT_TAG_ID`
    - Locked source: CONTEXT.md `<canonical_refs>` section "Tag Constant Source + Hoist Target"
  </read_first>
  <action>
Create new file `src/app/core/constants/tier-tags.ts` with the following structure:

1. **Header comment block** — cite D-50 canonical tier mapping and reserved UUID for future Hierarchy Editor (copy verbatim from PATTERNS.md line 227-233):
   ```typescript
   /**
    * SME Mart Project-tier identity tag.
    *
    * Per D-50 canonical tier mapping: depth 2 = Project tier (FIXED), tag = sme-mart.tier.project.
    * The sme-mart.tier.workspace UUID (UAT 2d7e6b6d-62e1-4691-958c-41cd1b8de043) is reserved
    * for customer-renameable depth-3+ tiers via the future Hierarchy Editor; v1.4 does not use it.
    */
   ```

2. **Export the constant** — UAT UUID from provisioner line 19:
   ```typescript
   export const SME_MART_TIER_PROJECT_TAG_ID = '420b0753-e72c-4b81-8929-70508a119bf0'; // UAT
   ```

3. **Do NOT add environment-specific variants** — that's TIER-TAG-ENV-BOOTSTRAP-1 (Phase 31+ backlog item). Single UAT constant for this phase.

4. **Co-locate with `demo-tags.ts`** — the constants directory is the single home for tag UUIDs. Verify that `src/app/core/constants/demo-tags.ts` exists before creating this file.

**Anti-patterns to avoid:**
- Do NOT add comments like "this is a constant" — state the tier mapping (D-50) and canonical source
- Do NOT hardcode the UUID string inline without the constant export
- Do NOT comment with "UAT only" — let the D-50 comment explain the design
  </action>
  <verify>
    <automated>test -f src/app/core/constants/tier-tags.ts && grep -c "SME_MART_TIER_PROJECT_TAG_ID" src/app/core/constants/tier-tags.ts</automated>
  </verify>
  <done>File `tier-tags.ts` created with D-50 header comment and exported constant. Grep count = 1 (single export declaration).</done>
</task>

<task type="auto">
  <name>Task 2: Add re-export to platform-engagement-provisioner.service.ts</name>
  <files>src/app/core/services/platform-engagement-provisioner.service.ts</files>
  <read_first>
    - File being modified: `src/app/core/services/platform-engagement-provisioner.service.ts` (existing)
    - Current state: `SME_MART_TIER_PROJECT_TAG_ID` declared at line 19 (per PATTERNS.md line 494-503)
    - Analog for re-export pattern: PATTERNS.md line 516-520 (re-export one-liner at end of exports)
    - Locked decision: CONTEXT.md G2 "Discovery Helpers — LIVE IN `engagements.service`" — provisioner stays the provisioner; re-export makes its constant available to new consumers
  </read_first>
  <action>
1. **Read the entire provisioner file** — locate the `SME_MART_TIER_PROJECT_TAG_ID` declaration at line ~19.

2. **Leave the file-local constant in place** — do NOT remove it from the provisioner. It's used locally within the provisioning recipe.

3. **Add a re-export at the END of the file** (after all local constants and functions, before/after any other exports), on a new line:
   ```typescript
   // Re-export SME_MART_TIER_PROJECT_TAG_ID from new constants file for caller stability
   // (engagements.service and feature-coming-soon need this constant)
   export { SME_MART_TIER_PROJECT_TAG_ID } from '../constants/tier-tags';
   ```

4. **Verify import paths are correct** — relative path `../constants/tier-tags` should resolve from `src/app/core/services/` → `src/app/core/constants/tier-tags.ts`.

5. **Apply Touch-It-Fix-It** — while modifying this file, check lines 1–19 for any modernization-rule violations (old-style const patterns, etc.). If violations exist, fix them in the same commit:
   - Check if any multi-line const objects use `const { ... } =` syntax — modernization doesn't change these.
   - Check if there are comments in violation of the style guide — unlikely but possible.
   - **Do NOT change the file's overall structure or logic** — only fix violation patterns on already-present lines.

**Why the re-export?**
- Existing callers of `import { SME_MART_TIER_PROJECT_TAG_ID } from '...provisioner'` continue to work (backward compatibility).
- New callers (Phase 30 engagements.service) can import from the constants module.
- If one consumer wants ONLY the constant without the provisioner's function overhead, they import from constants.
  </action>
  <verify>
    <automated>grep -n "export.*SME_MART_TIER_PROJECT_TAG_ID.*tier-tags" src/app/core/services/platform-engagement-provisioner.service.ts</automated>
  </verify>
  <done>Re-export line added to platform-engagement-provisioner.service.ts. Grep confirms the export statement references `../constants/tier-tags`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File-local to re-export | Constant is read-only UUID; re-export does not change its value or visibility. |
| Provisioner to consumers | Provisioner logic is unchanged; only the export surface is extended. Existing callers are unaffected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | **I** (Information Disclosure) | Tier-tag UUID hardcoded in source | **mitigate** | UUID is a tag identifier in the ZB platform, not a secret. It's already visible in the provisioner.service file and in UAT databases. No new disclosure surface. Treat as public constant. |
| T-30-02 | **I** (Information Disclosure) | Re-export increases constant visibility | **mitigate** | The constant's value is unchanged; only the import path is added. Existing security posture is unchanged. |

**Assessment:** This is a constants-only, read-only phase. No write operations, no new API surfaces, no authentication/authorization changes. **Risk level: LOW.** Threat surface is purely organizational (code visibility).
</threat_model>

<verification>
1. **File existence:** `test -f src/app/core/constants/tier-tags.ts` — file created.
2. **Constant export:** `grep "export const SME_MART_TIER_PROJECT_TAG_ID" src/app/core/constants/tier-tags.ts` — constant exported.
3. **Re-export presence:** `grep "export.*SME_MART_TIER_PROJECT_TAG_ID.*tier-tags" src/app/core/services/platform-engagement-provisioner.service.ts` — re-export line added.
4. **TypeScript clean:** `npx tsc -p tsconfig.app.json --noEmit` — no type errors.
5. **Lint clean (on touched files):** `npx eslint src/app/core/constants/tier-tags.ts src/app/core/services/platform-engagement-provisioner.service.ts --max-warnings=0` — no violations.
6. **Manual verification:** Open both files in editor; visually confirm constant value matches UAT UUID `420b0753-e72c-4b81-8929-70508a119bf0`.
</verification>

<success_criteria>
- New `tier-tags.ts` file created in `src/app/core/constants/` directory.
- File exports `SME_MART_TIER_PROJECT_TAG_ID` with UAT UUID value.
- D-50 canonical comment block present with exact verbiage from CONTEXT.md.
- Platform-engagement-provisioner re-exports the constant from tier-tags.ts on a single line.
- Both files pass `tsc --noEmit` type-checking (no errors).
- Both files pass lint with `--max-warnings=0` (diff-based pre-commit scope).
- Constant value is NOT changed; it's only moved and re-exported (zero runtime change).
</success_criteria>

<output>
After completion, create `.planning/phases/30-default-project-board-coming-soon-placeholders/30-01-SUMMARY.md`
</output>
