---
phase: 30
plan: 05
type: execute
wave: 3
depends_on: [30-01, 30-02, 30-03, 30-04]
files_modified:
  - src/app/app.routes.ts
autonomous: false
requirements: [PB-01, PB-02, PB-03, PB-04, PB-05, PB-06, PB-07]
user_setup: []
must_haves:
  truths:
    - "/projects route mounts DefaultProjectBoardComponent (replaces ComingSoon)"
    - "/org-documents, /engagement-dashboard, /message-center routes mount FeatureComingSoonComponent"
    - "All three placeholder routes receive route.data with title, description, featureKey"
    - "All routes inherit onboardingGuard from AppShell parent (no new guards needed)"
    - "No circular imports; components are declared before routes consume them"
  artifacts:
    - path: "src/app/app.routes.ts"
      provides: "Updated route declarations"
      contains: "DefaultProjectBoardComponent"
    - path: "src/app/app.routes.ts"
      provides: "Updated route declarations"
      contains: "FeatureComingSoonComponent"
  key_links:
    - from: "src/app/app.routes.ts"
      to: "src/app/pages/default-project-board/default-project-board.component.ts"
      via: "route component declaration"
      pattern: "DefaultProjectBoardComponent"
    - from: "src/app/app.routes.ts"
      to: "src/app/pages/default-project-board/feature-coming-soon.component.ts"
      via: "route component declaration"
      pattern: "FeatureComingSoonComponent"

---

<objective>
Wire Phase 30 routes in app.routes.ts: replace /projects ComingSoon placeholder with DefaultProjectBoardComponent, add three new placeholder routes (/org-documents, /engagement-dashboard, /message-center) mounting FeatureComingSoonComponent with route-data config, verify all dependencies are wired correctly, run comprehensive phase verification (tsc, lint, tests, D-32..D-35 verbiage cross-check).

Purpose: Connect all components from Plans 01-04 into the app router. Phase 30 ships when this plan completes: users land on /projects, see their default engagement + project tier, and navigate to three informative coming-soon placeholders.

Output: app.routes.ts updated with 4 new/modified route entries; manual verification checkpoint ensuring D-32..D-35 verbiage is correct and all gates pass (tsc + lint + tests + UAT manual check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONTEXT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-default-project-board-coming-soon-placeholders/30-CONTEXT.md
@.planning/phases/30-default-project-board-coming-soon-placeholders/30-PATTERNS.md
@.planning/phases/30-default-project-board-coming-soon-placeholders/30-UI-SPEC.md
@.planning/docs/MODERNIZATION_GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Phase 30 routes in app.routes.ts</name>
  <files>src/app/app.routes.ts</files>
  <read_first>
    - File being modified: `src/app/app.routes.ts` (existing)
    - Current /projects route: line 52 (`{ path: 'projects', component: ComingSoon, ... }`)
    - Import statements location: lines 1-30 (add DefaultProjectBoardComponent + FeatureComingSoonComponent)
    - Pattern source: PATTERNS.md lines 532-600 (route config, lazy-load variant, data structures)
    - Placeholder route data schema: PATTERNS.md lines 562-585 (title, description, featureKey structure)
    - Locked decision: CONTEXT.md G3 "feature-coming-soon.component co-located"
  </read_first>
  <action>
1. **Add imports at the top of app.routes.ts** (after existing imports, around line 30):
   ```typescript
   import { DefaultProjectBoardComponent } from './pages/default-project-board/default-project-board.component';
   import { FeatureComingSoonComponent } from './pages/default-project-board/feature-coming-soon.component';
   ```

2. **Find and replace the /projects route** (line 52):
   
   **Current state (to be replaced):**
   ```typescript
   { path: 'projects', component: ComingSoon, data: { title: 'Projects' } },
   ```

   **New state:**
   ```typescript
   { path: 'projects', component: DefaultProjectBoardComponent },
   ```

3. **Add three new placeholder routes** after /projects route (before legacy redirects like /engagements → /rfps):
   ```typescript
   {
     path: 'org-documents',
     component: FeatureComingSoonComponent,
     data: {
       title: 'Org Documents — Coming Soon',
       description: 'Centralized document management and sharing for your organization is on the roadmap. Once available, you\'ll be able to upload, organize, and share documents across engagements.',
       featureKey: '046',
     },
   },
   {
     path: 'engagement-dashboard',
     component: FeatureComingSoonComponent,
     data: {
       title: 'Engagement Dashboard — Coming Soon',
       description: 'Aggregated metrics and progress views across all your engagements are coming soon. You\'ll see status, milestones, and key activity at a glance.',
       featureKey: '066',
     },
   },
   {
     path: 'message-center',
     component: FeatureComingSoonComponent,
     data: {
       title: 'Message Center — Coming Soon',
       description: 'Cross-party messaging across all your engagements is coming soon. Today, conversations live within individual engagements.',
       featureKey: '065',
     },
   },
   ```

4. **Verify route order** — ensure /projects, /org-documents, /engagement-dashboard, /message-center are mounted BEFORE any wildcard redirects or legacy redirects (e.g., `{ path: 'engagements', redirectTo: 'rfps', pathMatch: 'full' }`).

5. **Apply Touch-It-Fix-It** — if the routes file has any modernization violations while modifying, fix them in the same commit (e.g., old-style arrow functions, non-const exports, etc.).

**Anti-patterns to avoid:**
- Do NOT mount the 3 placeholder routes inside a lazy-loaded children array — keep them as top-level siblings
- Do NOT use string component names (deprecated Angular pattern) — import actual component classes
- Do NOT remove or comment out the ComingSoon import (it may be used elsewhere)
- Do NOT change the order of existing routes (only insert new ones in the designated location)
  </action>
  <verify>
    <automated>grep -c "DefaultProjectBoardComponent" src/app/app.routes.ts && grep -c "FeatureComingSoonComponent" src/app/app.routes.ts && grep -c "org-documents" src/app/app.routes.ts && grep -c "engagement-dashboard" src/app/app.routes.ts && grep -c "message-center" src/app/app.routes.ts</automated>
  </verify>
  <done>Routes file modified. Grep count = 1 for DefaultProjectBoardComponent (import + route). Grep count = 1 for FeatureComingSoonComponent (import + route). Grep count = 3 for placeholder routes (org-documents, engagement-dashboard, message-center). /projects route replaced with DefaultProjectBoardComponent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
- Phase 30 is now fully wired: /projects lands on DefaultProjectBoardComponent (shows user's engagement + project tier); three new routes /org-documents, /engagement-dashboard, /message-center show honest coming-soon placeholders.
- All 4 component files created (Plans 01-04): tier-tags.ts constant, engagements.service helpers, feature-coming-soon component, default-project-board component.
- Routes connected in app.routes.ts (Plan 05, Task 1).
  </what-built>
  <how-to-verify>
1. **Build and serve locally:**
   - Run `ng serve --configuration development` (or `npm run dev`)
   - Navigate to http://localhost:4200/projects
   - Verify the page loads without errors (no blank page, no TypeScript compilation errors in DevTools console)

2. **Verify engagement + project-tier cards appear:**
   - If authenticated and onboarded, you should see two Material cards on the /projects page
   - First card shows engagement title + description (D-32/D-33 verbiage with buyer org name)
   - Second card shows project-tier title ("ZeroBias Platform") + description (D-34/D-35 verbiage)
   - Both cards are styled with Material Design 3 (check theming is applied, cards are properly spaced)

3. **Verify placeholders work:**
   - Click or navigate to http://localhost:4200/org-documents
   - Verify "Org Documents — Coming Soon" title + description render via ZbEmptyStateContainer
   - Repeat for /engagement-dashboard (title "Engagement Dashboard — Coming Soon", feature code 066)
   - Repeat for /message-center (title "Message Center — Coming Soon", feature code 065)

4. **Verify error/loading states:**
   - Open DevTools Network tab, throttle connection to "Slow 3G"
   - Reload /projects, verify loading spinner appears briefly
   - If you're not authenticated, verify you're redirected to /login (onboarding guard works)
   - If you're authenticated but data is unavailable, verify error message appears with "Retry" button

5. **Verify deep-link parity (per PB-06):**
   - From the board, right-click a card → Copy Link
   - Open that URL in a new tab → page should load without state loss
   - Verify no session/routing re-initialization happens

6. **Verify D-32..D-35 verbiage (exit criterion G7):**
   - Check engagement card title matches pattern: "[OrgName] <- ZeroBias" (D-32: ASCII reverse-arrow)
   - Check engagement description starts with "Platform Services Engagement: ZeroBias ➡️" (D-33: forward arrow + org name)
   - Check project-tier card title is exactly "ZeroBias Platform" (D-34: no variation)
   - Check project-tier description contains "gateway into ZeroBias" (D-35: org-name interpolated)
   - If verbiage is incorrect, send "CHANGES_NEEDED" signal and describe discrepancies

7. **Verify no TypeScript / lint errors:**
   - In terminal: `npx tsc -p tsconfig.app.json --noEmit` (should produce no errors)
   - In terminal: `npx tsc -p tsconfig.spec.json --noEmit` (should produce no errors)
   - In terminal: `npx eslint src/app/pages/default-project-board/ src/app/core/services/engagements.service* src/app/app.routes.ts --max-warnings=0` (should pass)
   - In terminal: `npm test` — run full test suite, verify all tests pass (including new Phase 30 tests)

8. **Report findings:**
   - If all checks pass: type "APPROVED"
   - If any check fails or verbiage is wrong: type "CHANGES_NEEDED: [describe issues]"
   - If UAT environment unavailable: type "UAT_UNAVAILABLE: [reason], approving based on local build success"
  </how-to-verify>
  <resume-signal>Type "APPROVED", "CHANGES_NEEDED: [issues]", or "UAT_UNAVAILABLE: [reason]"</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run comprehensive phase verification gates</name>
  <files>src/app/app.routes.ts</files>
  <read_first>
    - Verification context: Plans 01-04 artifacts (all .ts, .html, .scss, .spec.ts files)
    - Exit criteria source: CONTEXT.md G7 "D-32..D-35 verbiage verification only as exit criterion" + UI-SPEC.md D-32..D-35
    - G6 stop-the-line rule: default-project-board.component.ts must be < 150 lines
    - Modernization rules: MODERNIZATION_GUIDE.md (enforce via eslint gate)
  </read_first>
  <action>
After checkpoint approval (Task 2), run the following verification sequence:

1. **TypeScript strict mode (app config):**
   ```bash
   npx tsc -p tsconfig.app.json --noEmit
   ```
   - Expected: 0 errors
   - If errors: stop, fix them, re-run

2. **TypeScript strict mode (spec config):**
   ```bash
   npx tsc -p tsconfig.spec.json --noEmit
   ```
   - Expected: 0 errors
   - If errors: stop, fix them, re-run

3. **ESLint on Phase 30 files (diff-based, per pre-commit hook):**
   ```bash
   npx eslint \
     src/app/pages/default-project-board/default-project-board.component.ts \
     src/app/pages/default-project-board/default-project-board.component.html \
     src/app/pages/default-project-board/default-project-board.component.scss \
     src/app/pages/default-project-board/default-project-board.component.spec.ts \
     src/app/pages/default-project-board/feature-coming-soon.component.ts \
     src/app/pages/default-project-board/feature-coming-soon.component.html \
     src/app/pages/default-project-board/feature-coming-soon.component.scss \
     src/app/pages/default-project-board/feature-coming-soon.component.spec.ts \
     src/app/core/services/engagements.service.ts \
     src/app/core/services/engagements.service.spec.ts \
     src/app/core/constants/tier-tags.ts \
     src/app/core/services/platform-engagement-provisioner.service.ts \
     src/app/app.routes.ts \
     --max-warnings=0
   ```
   - Expected: 0 errors, 0 warnings
   - If failures: fix violations (see MODERNIZATION_GUIDE.md Troubleshooting section)

4. **Run all Phase 30 unit tests:**
   ```bash
   npm test -- \
     --include="**/default-project-board.component.spec.ts" \
     --include="**/feature-coming-soon.component.spec.ts" \
     --include="**/engagements.service.spec.ts" \
     --watch=false
   ```
   - Expected: all tests pass (0 failures)
   - If test failures: debug test vs implementation mismatch, fix, re-run

5. **Verify G6 stop-the-line rule (150-line ceiling on component):**
   ```bash
   wc -l src/app/pages/default-project-board/default-project-board.component.ts
   ```
   - Expected: output shows line count < 150
   - If >= 150: component is too large; refactor before proceeding

6. **Verify D-32..D-35 verbiage in UAT (manual step, part of checkpoint approval):**
   - See Task 2 "Verify D-32..D-35 verbiage" — this is confirmed during checkpoint
   - Document: observed verbiage matches D-32 ("W3Geekery <- ZeroBias"), D-33 ("Platform Services Engagement: ZeroBias ➡️ ..."), D-34 ("ZeroBias Platform"), D-35 (org-name interpolation)

7. **Summary log:**
   - All gates must pass before final commit
   - If any gate fails, stop and resolve before retrying
   - On full pass: log "PHASE 30 VERIFICATION COMPLETE ✓"

**Exit Criterion (G7 lock):**
- [ ] tsc app config: 0 errors
- [ ] tsc spec config: 0 errors
- [ ] eslint Phase 30 files: 0 errors, 0 warnings
- [ ] Unit tests: all pass
- [ ] G6 ceiling: default-project-board.component.ts < 150 lines
- [ ] D-32..D-35 verbiage: confirmed correct (from checkpoint approval)
- [ ] Manual verification: engagement card, project-tier card, three placeholder routes all render correctly in UAT
  </action>
  <verify>
    <automated>echo "Phase 30 verification gates completed. Check manual verification checkpoint (Task 2) for UAT approval."</automated>
  </verify>
  <done>All phase verification gates executed. No output artifact — all checks are procedural and reported via checkpoint (Task 2). If checkpoint approved and verification gates pass, Phase 30 is complete and ready for git commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router → Components | Route declarations are defined in code. No user input to router. Components receive only route.data (static). No cross-site request issues. |
| AppShell → Routed Components | All routes inherit onboardingGuard from AppShell parent. No new auth checks needed — children enforce same guard. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | **T** (Tampering) | Route configuration in app.routes.ts | **accept** | Route declarations are in source code, not user-controllable. Tampering requires code change, not runtime attack. |
| T-30-10 | **E** (Elevation of Privilege) | Three new routes inherit onboardingGuard | **mitigate** | All routes (existing + new) inherit parent AppShell guard. No elevation surface created. Guard enforces authentication before any route renders. |

**Assessment:** Route wiring is code-only. No new API surfaces, no new auth checks needed (inherited). **Risk level: MINIMAL.**
</threat_model>

<verification>
1. **Build succeeds:** `ng build --configuration development` (no TS compilation errors)
2. **Routes mounted:** `grep -c "DefaultProjectBoardComponent" src/app/app.routes.ts` = 1 (import + route)
3. **Placeholders mounted:** `grep -c "org-documents\|engagement-dashboard\|message-center" src/app/app.routes.ts` = 3
4. **Routes compile:** `npx tsc -p tsconfig.app.json --noEmit` — 0 errors
5. **Lint clean:** `npx eslint src/app/app.routes.ts --max-warnings=0` — 0 errors
6. **UI verification:** Manual verification via checkpoint (Task 2)
7. **D-32..D-35 verbiage:** Confirmed via checkpoint approval (Task 2)
</verification>

<success_criteria>
- app.routes.ts updated with 4 new/modified route entries
- DefaultProjectBoardComponent imported and mounted at /projects path
- FeatureComingSoonComponent imported and mounted at /org-documents, /engagement-dashboard, /message-center
- All three placeholder routes have route.data with title, description, featureKey
- Routes inherit onboardingGuard from AppShell parent (no explicit guards needed)
- No circular imports; all component imports resolve correctly
- tsc -p tsconfig.app.json --noEmit: 0 errors
- tsc -p tsconfig.spec.json --noEmit: 0 errors
- eslint on all Phase 30 files: 0 errors, 0 warnings
- npm test (full suite): all tests pass
- default-project-board.component.ts line count < 150 (G6 ceiling)
- D-32..D-35 verbiage confirmed correct in UAT (checkpoint approval)
- Manual verification: engagement card, project-tier card, three placeholders render correctly
- Phase 30 ready for git commit and merge to upstream
</success_criteria>

<output>
After completion, create `.planning/phases/30-default-project-board-coming-soon-placeholders/30-05-SUMMARY.md`
</output>
