---
phase: 33
plan: 06
type: execute
wave: 1
depends_on: ["33-01"]
files_modified:
  - pages/home/home.component.ts
  - pages/home/home.component.html
  - shared/components/provider-card/provider-card.component.ts
  - shared/components/provider-card/provider-card.component.html
  - pages/my-profile/my-profile-overview.component.ts
  - pages/my-profile/my-profile-overview.component.html
  - core/services/impersonation.service.ts
  - pages/engagements/engagement-detail.component.ts
  - pages/engagements/engagement-list.component.ts
  - pages/rfps/rfp-detail.component.ts
  - pages/rfps/bid-wizard/bid-wizard.component.ts
  - pages/my-profile/my-profile-services.component.ts
  - pages/my-profile/my-profile-reviews.component.ts
  - pages/my-profile/my-profile-moderate-reviews.component.ts
  - pages/services/service-catalog.component.ts
  - core/services/bid-ai.service.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "All deleted types (`ProviderProfile`, `ProviderDirectoryRow`, `ProviderDetailRow`) are removed from imports across 15 consumer files"
    - "All dropped fields (`hourly_rate`, `availability_status`, `response_time` per §7 #3) are deleted from form controls, displays, and service methods across my-profile-overview and impersonation.service"
    - "Return-shape consumers (engagement, rfps, bid-wizard, my-profile-services/reviews, service-catalog, bid-ai) compile against new `getProvider`/`getProviderByUserId` return types that expose `id`/`orgId` or are updated to use explicit org ID"
    - "`display_name` access in service-catalog.component is migrated from `provider.display_name` to `dana.Org` read (real name source per migration-mapping §6)"
    - "`provider-card.component` (still uses `@Input`) is modernized to Angular 21 (input/output, inject, signals) per Touch-It-Fix-It"
    - "TypeScript compiles cleanly with 33-02 (`npx tsc -p tsconfig.spec.json --noEmit` passes at Wave-1 exit)"
  artifacts:
    - path: "pages/home/home.component.ts"
      provides: "Removes ProviderDirectoryRow import, re-points featuredProviders signal to new shape from 33-02"
      contains: "listProviders"
    - path: "pages/my-profile/my-profile-overview.component.ts"
      provides: "Removes dropped-field form controls (hourly_rate, availability_status, response_time); re-points profile signal to new OrgProfile shape"
      contains: "profile"
    - path: "core/services/impersonation.service.ts"
      provides: "Removes hourly_rate declaration and read per §7 #3"
      contains: "effectiveUserId()"
    - path: "core/services/bid-ai.service.ts"
      provides: "Migrates to new getProvider return type; KEEPS parseViewJson (do NOT delete)"
      contains: "getProvider"
  key_links:
    - from: "home.component"
      to: "provider-profiles.service"
      via: "listProviders call"
      pattern: "this\\.providerProfiles\\.listProviders"
    - from: "my-profile-overview.component"
      to: "profile signal"
      via: "ProviderProfile type (deleted)"
      pattern: "profile.*ProviderProfile"
    - from: "service-catalog.component"
      to: "dana.Org"
      via: "display_name property"
      pattern: "provider\\.display_name.*dana\\.Org"
---

<objective>
Migrate all consumers of deleted types (`ProviderProfile`, `ProviderDirectoryRow`, `ProviderDetailRow`) and dropped fields (`hourly_rate`, `availability_status`, `response_time`) to compile against the new org-scoped GQL shapes from 33-02 Half-A reads. This is a Wave-1 gate: 33-02 + 33-06 run in parallel and together must green the `tsc` gate at Wave-1 exit.

**Purpose:** 33-01 deleted the consumer types, causing tsc failures in 15 files. 33-02 provides the new shapes. 33-06 migrates all 15 consumers to use them. Together, Wave-1 achieves clean compilation.

**Output:** 15 consumer files updated to use new types, dropped fields deleted, `display_name` migrated to dana.Org, provider-card modernized, TypeScript compiles cleanly (Wave-1 exit gate with 33-02).

**Wave-1 Exit Gate:** This plan + 33-02 together must achieve `npx tsc -p tsconfig.spec.json --noEmit` PASS before Wave-2 (33-03/33-04/33-05) proceeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (section "2.7 OrgProfile" for new shapes)
@core/models/provider.model.ts (new types from 33-01)
@core/services/provider-profiles.service.ts (new read methods from 33-02, return shapes)
@.planning/docs/MODERNIZATION_GUIDE.md
@.planning/docs/SDK_VERIFICATION_SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate deleted-type consumers (home, provider-card, my-profile-overview) — remove dropped fields</name>
  <files>
    pages/home/home.component.ts
    pages/home/home.component.html
    shared/components/provider-card/provider-card.component.ts
    shared/components/provider-card/provider-card.component.html
    pages/my-profile/my-profile-overview.component.ts
    pages/my-profile/my-profile-overview.component.html
    core/services/impersonation.service.ts
  </files>
  <read_first>
    @pages/home/home.component.ts (imports ProviderDirectoryRow, signal `featuredProviders`)
    @shared/components/provider-card/provider-card.component.ts (imports ProviderDirectoryRow via @Input; still uses @Input/@Output — modernize)
    @pages/my-profile/my-profile-overview.component.ts (imports ProviderDetailRow; form controls for dropped fields at lines ~48-50, 64-66)
    @pages/my-profile/my-profile-overview.component.html (displays dropped fields at lines ~26, 31, 40)
    @core/services/impersonation.service.ts (declares hourly_rate at line 17; reads pp.hourly_rate at line 116)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md (satisfy the architect-skill-gate; provider-card modernization)
  </read_first>
  <action>
Migrate three key consumer files:

1. **pages/home/home.component.ts:**
   - Remove `import { ProviderDirectoryRow }` from old provider.model.ts (deleted)
   - Re-point `featuredProviders` signal to new return type from 33-02's `listProviders()` (still row-shaped, just new type)
   - Call `this.providerProfiles.listProviders()` if not already; ensure it uses new GQL method

2. **shared/components/provider-card/provider-card.component.ts (MODERNIZE):**
   - Remove `import { ProviderDirectoryRow }` 
   - Replace `@Input() provider: ProviderDirectoryRow` with `provider = input<NewProviderRowType>()`  per Angular 21 modernization (Touch-It-Fix-It)
   - Replace `@Output() onViewProviderProfile` with `output()` 
   - Change from constructor injection to `inject()` where applicable
   - Mark as standalone + OnPush if not already
   - No logic changes, just modernization

3. **pages/my-profile/my-profile-overview.component.ts:**
   - Remove `import { ProviderDetailRow }`
   - Delete form controls for `hourly_rate` (lines ~48-50) per §7 #3
   - Delete form controls for `availability_status` (lines ~64-66) per §7 #3
   - Delete form controls for `response_time` per §7 #3
   - Re-point `profile` signal from `ProviderDetailRow` to new `OrgProfile` type
   - Validate the form structure compiles without the dropped fields

4. **pages/my-profile/my-profile-overview.component.html:**
   - Delete display of `hourly_rate` (lines ~26, ~31, ~40) per §7 #3
   - Delete display of `availability_status` 
   - Delete display of `response_time`
   - Validate template compiles without the dropped-field bindings

5. **core/services/impersonation.service.ts:**
   - Delete `hourly_rate: string` declaration (line 17) per §7 #3
   - Delete `pp.hourly_rate` read (line 116) per §7 #3
   - Verify the service compiles without the field

**Angular 21 compliance (modernization):**
- provider-card: `input()`/`output()`, `inject()`, standalone, OnPush
- Home: standalone if not already, use modern patterns
- Overview: same
</action>
  <verify>
    <automated>
      grep -c "ProviderDirectoryRow\|ProviderDetailRow" pages/home/home.component.ts pages/my-profile/my-profile-overview.component.ts && echo "✗ FAIL: Deleted types still imported" || echo "✓ PASS: Deleted types removed" && \
      grep -c "hourly_rate\|availability_status\|response_time" pages/my-profile/my-profile-overview.component.ts core/services/impersonation.service.ts && echo "✗ FAIL: Dropped fields still present" || echo "✓ PASS: Dropped fields deleted" && \
      grep -c "@Input\|@Output" shared/components/provider-card/provider-card.component.ts && echo "⚠️  Old decorators in provider-card (Touch-It-Fix-It modernization)" || echo "✓ PASS: Modernized to input/output" && \
      npx tsc -p tsconfig.spec.json --noEmit 2>&1 | grep -c "error" | grep "^0" || echo "⚠️  tsc errors remain (dependencies from other consumers in Task 2)"
    </automated>
  </verify>
  <done>
    pages/home/home.component, shared/components/provider-card/provider-card.component (MODERNIZED to Angular 21), pages/my-profile/my-profile-overview.component, and core/services/impersonation.service.ts are migrated to new types. Deleted types (`ProviderProfile`/`ProviderDirectoryRow`/`ProviderDetailRow`) removed from imports. Dropped fields (`hourly_rate`, `availability_status`, `response_time`) deleted from form controls, displays, and service code per §7 #3. provider-card modernized (input/output, inject, standalone, OnPush). Files compile individually; Task 2 completes Wave-1 exit gate.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate return-shape consumers (engagements, rfps, bid-wizard, my-profile-services/reviews, service-catalog, bid-ai)</name>
  <files>
    pages/engagements/engagement-detail.component.ts
    pages/engagements/engagement-list.component.ts
    pages/rfps/rfp-detail.component.ts
    pages/rfps/bid-wizard/bid-wizard.component.ts
    pages/my-profile/my-profile-services.component.ts
    pages/my-profile/my-profile-reviews.component.ts
    pages/my-profile/my-profile-moderate-reviews.component.ts
    pages/services/service-catalog.component.ts
    core/services/bid-ai.service.ts
  </files>
  <read_first>
    @pages/engagements/engagement-detail.component.ts (calls `getProvider`, uses `provider.id`)
    @pages/engagements/engagement-list.component.ts (calls `getProvider`, uses `provider.id`)
    @pages/rfps/rfp-detail.component.ts (calls `getProvider`, uses `provider.id`)
    @pages/rfps/bid-wizard/bid-wizard.component.ts (calls `getProvider` or `getProviderByUserId`)
    @pages/my-profile/my-profile-services.component.ts (calls `getProvider`, uses `detail.id`)
    @pages/my-profile/my-profile-reviews.component.ts (calls `getProvider`, uses `detail.id`)
    @pages/my-profile/my-profile-moderate-reviews.component.ts (calls `getProvider`, uses `provider.id`)
    @pages/services/service-catalog.component.ts (calls `getProvider`, accesses `provider.display_name` — MIGRATE to dana.Org)
    @core/services/bid-ai.service.ts (calls `getProvider`, KEEP parseViewJson do NOT delete)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md
  </read_first>
  <action>
Migrate 9 consumer files that call 33-02's new `getProvider`/`getProviderByUserId` methods and depend on the return shape.

**Common pattern for most consumers (engagement, rfp, bid-wizard, my-profile-services/reviews, my-profile-moderate-reviews):**
1. Call `this.providerProfiles.getProvider(orgId)` (or `getProviderByUserId(userId)`)
2. Receive the new return shape (org-scoped, Catalog FKs, verified/verificationSource)
3. If the return shape exposes `id` / `orgId` (per 33-02's design decision), use it as-is
4. If the return shape does NOT expose `id`, update the code to use explicit org ID:
   - Change `.id` to `.orgId` or receive orgId separately from caller context
   - Document this choice in the task completion

**Special case: service-catalog.component.ts (display_name migration)**
- Current: accesses `provider.display_name` (on old MarketplaceProfileItem / MPI)
- Migration: `display_name` is NOT a field on new OrgProfile; it's READ from platform `dana.Org`
- Action: Fetch `dana.Org` separately and extract the name, OR use `org.name` if the provider read includes the linked org
- Implement the cleaner path (likely: fetch brief org info alongside provider, use org.name)

**Special case: bid-ai.service.ts (parseViewJson safety)**
- KEEP `parseViewJson<T>` function — it has a live consumer (bid-ai.service.ts line 104)
- Just re-point the service's `getProvider` call to the new shape
- Do NOT touch parseViewJson itself; ensure it compiles unchanged

**For all 9 files:**
- No architectural changes, just type updates and name-resolution wiring
- Verify each one compiles against the new shapes

**Angular 21 modernization (optional unless touching logic):**
- If modifying a component for another reason, modernize it (input/output, inject, signals)
- Do NOT force modernization on all 9 — focus on getting them to compile first
</action>
  <verify>
    <automated>
      grep -c "getProvider\|getProviderByUserId" pages/engagements/engagement-detail.component.ts pages/rfps/rfp-detail.component.ts pages/services/service-catalog.component.ts && \
      grep -c "display_name.*dana\\.Org\|org\\.name.*display_name" pages/services/service-catalog.component.ts && echo "✓ display_name migrated to dana.Org" || echo "⚠️  display_name migration needs review" && \
      grep -c "parseViewJson" core/services/bid-ai.service.ts | grep -v "^0" && echo "✓ PASS: parseViewJson kept (do NOT delete)" || echo "⚠️  parseViewJson check" && \
      npx tsc -p tsconfig.spec.json --noEmit
    </automated>
  </verify>
  <done>
    All 9 return-shape consumer files (engagement-detail/list, rfp-detail, bid-wizard, my-profile-services/reviews/moderate-reviews, service-catalog, bid-ai) are migrated to call 33-02's new `getProvider`/`getProviderByUserId` methods and compile against the new return shapes. `display_name` access in service-catalog is migrated to dana.Org (real name source). `parseViewJson` in bid-ai.service.ts is KEPT (not deleted). **Wave-1 exit gate: Task 1 + Task 2 achieve `npx tsc -p tsconfig.spec.json --noEmit` PASS.**
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → Service | Components call new GQL read methods; return shapes are org-scoped |
| Service → External (dana.Org) | service-catalog accesses organization name from platform org entity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-14 | Information Disclosure | Dropped field removal | mitigate | Dropped fields (hourly_rate, availability_status, response_time) per §7 #3 are deleted. These were personal/operational fields; their removal is intentional. No PII lost; fields were optional. |
| T-33-15 | Authorization | Return-shape consumers | mitigate | All consumers now use org-scoped return shapes (orgId FK). Service enforces org scope. No cross-org data access possible. |

</threat_model>

<verification>
1. `npx tsc -p tsconfig.spec.json --noEmit` passes (Wave-1 exit gate).
2. Deleted types (`ProviderProfile`, `ProviderDirectoryRow`, `ProviderDetailRow`) removed from all 15 consumer files.
3. Dropped fields (`hourly_rate`, `availability_status`, `response_time`) deleted from my-profile-overview and impersonation.service.
4. All 9 return-shape consumers compile against new shapes; `.id` / `.orgId` usage clarified.
5. service-catalog.component `display_name` migrated to dana.Org read.
6. bid-ai.service.ts `parseViewJson` KEPT (not deleted).
7. provider-card.component modernized to Angular 21 (input/output, inject).
8. No tsc errors remaining.
</verification>

<success_criteria>
- All 15 consumer files migrated to new types and compiles cleanly.
- Deleted types removed from all imports.
- Dropped fields deleted per §7 #3.
- Return-shape consumers use new shapes correctly.
- `display_name` migrated to dana.Org.
- `parseViewJson` preserved.
- provider-card modernized.
- Wave-1 exit gate: tsc PASS with 33-02.
</success_criteria>

<output>
Create `.planning/phases/33-profile-expertise-company-info-re-home/33-06-SUMMARY.md` when complete.
</output>
