---
phase: 33
plan: 04
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified:
  - onboarding/company-info-sections.ts
  - onboarding/company-profile-form.component.ts
  - onboarding/company-profile-form.component.html
  - onboarding/company-profile-form.component.scss
  - core/models/company-info.model.ts
  - seed-zb-provider.ts
autonomous: true
requirements: [PROF-05, PROF-09, PROF-10]
user_setup: []

must_haves:
  truths:
    - "Onboarding company-info sections write to OrgProfile + Address + PKV (not MPI); businessClassification + employeeCount picklists use LOCKED 7 + 7 values per D-57 + Director precision"
    - "Corporate-profile form (6 sections: insurance, reference, personnel, financial, corporate-identity, attestation) writes to typed classes (InsuranceCoverage, ClientReference, Personnel, FinancialProfile, OrgProfile); attestation section is RETIRED per §7 #8"
    - "OrgSegment onboarding step added — user selects universal buyer/provider classification (Domain/Category from Catalog); one OrgSegment row created per org per selected segment"
    - "businessClassification field updated in 3 locations: company-profile-form.html:210-211 (old bands), company-info.model.ts, seed-zb-provider.ts; employeeCount re-banded to '51-100'/'101-500' (replacing '51-200'/'201-500')"
    - "Corporate-profile section rows write via PipelineWriteService with org scope, verify defaults, error handling"
    - "businessClassification (7 LOCKED values) + employeeCount (7 re-banded values) picklists deployed in all forms"
    - "All forms use modern Angular 21 (input/output, @if/@for, inject, signals)"
  artifacts:
    - path: "onboarding/company-info-sections.ts"
      provides: "Company-info KV sections write helpers, OrgProfile + Address + PKV writes"
      contains: "saveCompanyInfo"
    - path: "onboarding/company-profile-form.component.ts"
      provides: "Corporate-profile form component with 6 sections (or 5 if attestation retired), writes to typed classes, businessClassification + employeeCount picklists"
      contains: "businessClassification enum choices, employeeCount re-banded choices, BUSINESS_CLASSIFICATION_OPTIONS, EMPLOYEE_COUNT_OPTIONS"
    - path: "core/models/company-info.model.ts"
      provides: "Company-info + corporate-profile TypeScript types, enum values for businessClassification (7) + employeeCount (7 re-banded)"
  key_links:
    - from: "company-profile-form.component"
      to: "provider-profiles.service.ts"
      via: "updateProfile + corporate-profile write methods"
      pattern: "this\\.providerProfiles\\.(updateProfile|add.*Corporate)"
    - from: "company-info-sections.ts"
      to: "pipeline-write.service.ts"
      via: "pushEntity calls for OrgProfile/Address"
      pattern: "this\\.pipelineWrite\\.pushEntity.*OrgProfile\\|Address"
---

<objective>
Retarget onboarding company-info sections and the corporate-profile form to write to typed GQL classes (OrgProfile, Address, InsuranceCoverage, ClientReference, Personnel, FinancialProfile, OrgSegment) instead of MPI blobs. Add the OrgSegment classification step to onboarding. Update businessClassification + employeeCount picklists with LOCKED values (D-57 + Director precision).

**Purpose:** Onboarding + corporate-profile form are key user-facing surfaces where buyers/providers enter company/expertise data. Retargeting them to typed classes completes the write path and enables end-to-end Phase 33 validation.

**Output:** Rewritten onboarding company-info + corporate-profile form with typed-class writes, OrgSegment classification step, updated businessClassification (7 values) + employeeCount (7 re-banded values) picklists integrated into all forms, error handling, TypeScript clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (section "1.2 Implementation Pattern" for corporate-profile write shapes)
@.planning/phases/33-profile-expertise-company-info-re-home/33-UI-SPEC.md (picklist specifications)
@onboarding/company-profile-form.component.ts (current implementation)
@.planning/docs/MODERNIZATION_GUIDE.md
@.planning/docs/SDK_VERIFICATION_SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget company-info sections + add OrgSegment classification step</name>
  <files>
    onboarding/company-info-sections.ts
    onboarding/company-profile-form.component.ts
    onboarding/company-profile-form.component.html
    core/models/company-info.model.ts
    seed-zb-provider.ts
  </files>
  <read_first>
    @onboarding/company-info-sections.ts (current KV helper functions)
    @onboarding/company-profile-form.component.ts (current form logic)
    @.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (section "1.2 Pattern C: Update profile")
    @.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md (D-57 Director precisions)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md (satisfy the architect-skill-gate)
  </read_first>
  <action>
Retarget onboarding company-info form to write OrgProfile + Address + PKV instead of MPI, and add OrgSegment classification step.

**Changes to company-info-sections.ts:**
1. Replace MPI KV section writes with OrgProfile + Address writes via provider-profiles.service.ts.updateProfile().
2. The updateProfile method (from Plan 03) accepts a profileData object with fields: legalName, dba, tagline, shortDescription, longDescription, website, logoUrl, employeeCount, businessClassification, foundedYear, primaryContactUserId, hqLocation (for Address).
3. Map legacy company-info KV keys to OrgProfile/Address fields per migration-mapping §5:
   - `org_name` → `legalName`
   - `hq_location` → Address row with ownerType='org', addressType='registered'
   - `founded_year` → `foundedYear`
   - `website` → `website`
   - `logo_url` → `logoUrl`
   - `primary_contact_user_id` → `primaryContactUserId`
   - `business_type` → `businessClassification` (enum; see picklist below)
   - `employee_count` → `employeeCount` (enum, re-banded per D-57)
   - `onboarding_complete` stays in PKV (not moved to OrgProfile)
4. Add OrgSegment classification step (new): After company-info save, the form/flow prompts user to select universal buyer/provider classification (Catalog Domain or Category). On save, write an OrgSegment row via provider-profiles.service.ts (if addOrgSegment method exists, or via generic pushEntity('OrgSegment', {...}) in company-info-sections.ts).

**Changes to company-profile-form.component:**
The form has 6 sections (or 5 if attestation is retired per §7 #8). The sections are:
- Insurance (→ InsuranceCoverage rows)
- Reference (→ ClientReference rows)
- Personnel (→ Personnel rows)
- Financial (→ FinancialProfile rows)
- Corporate-identity (→ OrgProfile fields; credentials handled separately)
- Attestation (→ RETIRED, skip rendering per §7 #8)

For each section, form users enter data and save. The component calls the corresponding write method in provider-profiles.service.ts (e.g., `pushEntity('InsuranceCoverage', {...})` for insurance rows).

**⚠ Pipeline.receive is FULL-REPLACE (Director note 2026-06-26).** Each `pushEntity` write **nulls every field absent from the payload** — a partial OrgProfile/section save silently wipes omitted fields. Send the **complete** record (all current values), or read-merge the existing record before writing; never a sparse patch. This matters most for OrgProfile (the company-info save and the businessClassification/employeeCount picklist save in Task 3 both touch OrgProfile — they must not blank each other's fields). The write-through cache+merge mitigates this only when warm. (The prior "fire-and-forget silent failure" concern was already remediated under FF-03 — failures re-throw; this full-replace behavior is the remaining footgun.)

**Angular 21 compliance:**
Use modern syntax (@if/@for, input/output, inject). Mark component as standalone, OnPush. Remove any ngModule/CommonModule imports. Validate TypeScript types (no any).
  </action>
  <verify>
    <automated>
      grep -c "this\.providerProfiles\.updateProfile" onboarding/company-info-sections.ts && \
      grep -c "pushEntity.*OrgProfile\|pushEntity.*Address" onboarding/company-info-sections.ts && \
      grep -c "provider_type\|MarketplaceProfileItem" onboarding/company-info-sections.ts && echo "✗ FAIL: Legacy MPI reference found" || echo "✓ PASS: No MPI references" && \
      npx tsc -p tsconfig.spec.json --noEmit
    </automated>
  </verify>
  <done>
    Company-info sections write to OrgProfile + Address + PKV via provider-profiles.service.ts.updateProfile(). OrgSegment classification step added to onboarding. Corporate-profile form sections (insurance, reference, personnel, financial, corporate-identity; attestation retired) write to typed classes. Seed data and company-info.model.ts ready for picklist updates. TypeScript compile passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement corporate-profile form write paths for 5 typed classes</name>
  <files>onboarding/company-profile-form.component.ts</files>
  <read_first>
    @onboarding/company-profile-form.component.ts (current form structure)
    @.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (section "1.2 Pattern D: Write corporate-profile form sections to typed classes")
    @core/services/pipeline-write.service.ts (pushEntity API)
    @core/services/notes.service.ts (error handling pattern)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md (satisfy the architect-skill-gate)
  </read_first>
  <behavior>
    - Test 1: Insurance section rows write to InsuranceCoverage class via pushEntity with fields: coverageType, carrier, policyNumber, coverageAmount, currency, effectiveDate, expiresAt, certificateUrl
    - Test 2: Reference section rows write to ClientReference class via pushEntity with fields: clientName, projectName, contactName, contactEmail, contactPhone, relationship, summary, startDate, endDate
    - Test 3: Personnel section rows write to Personnel class via pushEntity with fields: userId, fullName, title, specialization, bio, email, linkedinUrl, isKeyPersonnel, backgroundCheckStatus
    - Test 4: Financial section rows write to FinancialProfile class via pushEntity with fields: annualRevenue, revenueCurrency, creditScore, creditRatingAgency, bankName, dunsNumber, yearEndMonth
    - Test 5: Corporate-identity section writes to OrgProfile + optional credentials
    - Test 6: All writes default verified=false / verificationSource=null per D-53
    - Test 7: All writes scoped to orgId, include callSiteTag for telemetry, wrapped in try/catch with snackBar error surface
    - Test 8: Attestation section is skipped (not rendered, not written) per §7 #8
  </behavior>
  <action>
Implement the corporate-profile form write paths. The form has 5–6 sections; each section writes rows to a typed class via PipelineWriteService.

**For each section (pattern from RESEARCH.md §1.2 Pattern D):**

1. Insurance (→ InsuranceCoverage):
   - User enters: coverage type, carrier name, policy number, coverage amount, currency, effective date, expires at, certificate URL
   - Write payload: orgId, coverageType, carrier, policyNumber, coverageAmount, currency, effectiveDate, expiresAt, certificateUrl, verified=false, verificationSource=null
   - Call: `this.pipelineWrite.pushEntity('InsuranceCoverage', payload, [], 'corporate-profile.insurance')`

2. Reference (→ ClientReference):
   - User enters: client name, project name, contact name, email, phone, relationship, summary, start date, end date
   - Write payload: orgId, clientName, projectName, contactName, contactEmail, contactPhone, relationship, summary, startDate, endDate, verified=false, verificationSource=null
   - Call: `this.pipelineWrite.pushEntity('ClientReference', payload, [], 'corporate-profile.reference')`

3. Personnel (→ Personnel):
   - User enters: user ID, full name, title, specialization, bio, email, LinkedIn URL, is-key-personnel flag, background check status
   - Write payload: orgId, userId, fullName, title, specialization, bio, email, linkedinUrl, isKeyPersonnel, backgroundCheckStatus, verified=false, verificationSource=null
   - Call: `this.pipelineWrite.pushEntity('Personnel', payload, [], 'corporate-profile.personnel')`

4. Financial (→ FinancialProfile):
   - User enters: annual revenue, revenue currency, credit score, credit rating agency, bank name, DUNS number, year-end month
   - Write payload: orgId, annualRevenue, revenueCurrency, creditScore, creditRatingAgency, bankName, dunsNumber, yearEndMonth, verified=false, verificationSource=null
   - Call: `this.pipelineWrite.pushEntity('FinancialProfile', payload, [], 'corporate-profile.financial')`

5. Corporate-identity (→ OrgProfile):
   - Shares businessClassification + employeeCount with company-info onboarding.
   - Write via updateProfile (from Plan 03).

6. Attestation (→ RETIRED, skip):
   - Do not render this section in the form template.
   - Do not write any data.

**Error handling (from notes.service.ts):**
```typescript
try {
  for (const row of insuranceRows) {
    const payload: Record<string, unknown> = {
      id: uuid(),
      name: `${className}-${orgId}-${index}`,
      orgId,
      ...row,
      verified: row.verified ?? false,
      verificationSource: row.verificationSource ?? null,
    };
    await this.pipelineWrite.pushEntity('InsuranceCoverage', payload, [], 'corporate-profile.insurance');
  }
} catch (err) {
  this.snackBar.open(
    `Failed to save insurance coverage: ${(err as Error).message}`,
    'Dismiss',
    { duration: 5000 },
  );
  throw err;
}
```

**Form structure:**
The form likely uses Material mat-tab-group or a stepper to navigate between sections.

**Angular 21 compliance:**
Use inject() for PipelineWriteService, MatSnackBar. Use signal() for form state. Use @if/@for in template. Mark component as standalone, OnPush. No @Input/@Output (use input/output if needed). No any types.
  </action>
  <verify>
    <automated>
      grep -c "InsuranceCoverage.*pushEntity\|ClientReference.*pushEntity\|Personnel.*pushEntity\|FinancialProfile.*pushEntity" onboarding/company-profile-form.component.ts && \
      grep -c "attestation" onboarding/company-profile-form.component.ts && echo "⚠️  WARNING: Attestation section still referenced (§7 #8 → should be retired)" || echo "✓ PASS: Attestation section handling clear" && \
      grep -c "verified.*false\|verificationSource.*null" onboarding/company-profile-form.component.ts && \
      grep -c "snackBar.open.*Failed" onboarding/company-profile-form.component.ts && \
      npx tsc -p tsconfig.spec.json --noEmit
    </automated>
  </verify>
  <done>
    Corporate-profile form sections write to typed classes (InsuranceCoverage, ClientReference, Personnel, FinancialProfile, OrgProfile). All writes use PipelineWriteService.pushEntity with org scope, default verified=false/verificationSource=null, include callSiteTag, and wrap in try/catch with snackBar. Attestation section is not rendered/written (§7 #8 retired). TypeScript compile passes. Tests cover all 5 section write paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate businessClassification + employeeCount picklists in all forms</name>
  <files>
    onboarding/company-profile-form.component.ts
    onboarding/company-profile-form.component.html
    core/models/company-info.model.ts
    seed-zb-provider.ts
  </files>
  <read_first>
    @.planning/phases/33-profile-expertise-company-info-re-home/33-UI-SPEC.md (Picklist Specifications)
    @onboarding/company-profile-form.component.html (current form template)
    @.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md (Director precisions: 7 businessClassification values, 7 employeeCount re-banded values)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md (satisfy the architect-skill-gate)
  </read_first>
  <action>
Integrate `businessClassification` (7 values) and `employeeCount` (7 re-banded values) picklists into company-profile form. Both use `mat-select` (single-select, standard Material).

**businessClassification enum (LOCKED per Director precision 2026-06-25, D-57):**
The 7 values (key → display label, verbatim from Director precision):
```
NONPROFIT → "Nonprofit / Not-for-profit"
GOVERNMENT → "Government"
HOSPITAL_HEALTHCARE → "Hospital/Healthcare Institution"
PUBLICLY_TRADED → "Publicly-traded Company"
PE_BACKED → "PE-backed Company"
PRIVATELY_HELD → "Privately-held Company"
INDIVIDUAL_SOLE_PROPRIETOR → "Individual / Sole Proprietor"
```

**employeeCount enum (7 re-banded values, locked in published 2.0.6 schema, per D-57):**
The 7 values (exact strings as picklist options):
```
"1-10"
"11-50"
"51-100"        ← re-banded (legacy was "51-200")
"101-500"       ← re-banded (legacy was "201-500")
"501-1000"
"1001-5000"
"5000+"
```

**Form integration:**
1. In company-profile-form.component.html, ensure the businessClassification field is present in the corporate-identity section (or company-info section, depending on form structure).
2. Bind the mat-select to the form control for businessClassification.
3. Render the 7 `<mat-option>` items with exact keys and labels from above.
4. Repeat for employeeCount field (7 options, exact string values).
5. In company-profile-form.component.ts, export the enum/options constant:
   ```typescript
   export const BUSINESS_CLASSIFICATION_OPTIONS = [
     { key: 'NONPROFIT', label: 'Nonprofit / Not-for-profit' },
     { key: 'GOVERNMENT', label: 'Government' },
     { key: 'HOSPITAL_HEALTHCARE', label: 'Hospital/Healthcare Institution' },
     { key: 'PUBLICLY_TRADED', label: 'Publicly-traded Company' },
     { key: 'PE_BACKED', label: 'PE-backed Company' },
     { key: 'PRIVATELY_HELD', label: 'Privately-held Company' },
     { key: 'INDIVIDUAL_SOLE_PROPRIETOR', label: 'Individual / Sole Proprietor' },
   ] as const;
   
   export const EMPLOYEE_COUNT_OPTIONS = [
     '1-10', '11-50', '51-100', '101-500', '501-1000', '1001-5000', '5000+'
   ] as const;
   ```
6. Use `@for` (not `*ngFor`) to loop over options in the template.

**Touch-it-fix-it check:**
If company-info.model.ts or seed-zb-provider.ts still reference the old bands ("51-200", "201-500") or incorrect businessClassification values, update them to the LOCKED values above (the 7 businessClassification keys above, the 7 employeeCount values above).

**Angular 21 compliance:**
- Use `@for` (not `*ngFor`) in the template.
- Bind to Reactive Form control (FormControl or FormGroup).
- No `any` types on the options array.
  </action>
  <verify>
    <automated>
      grep -c "NONPROFIT\|GOVERNMENT\|HOSPITAL_HEALTHCARE" core/models/company-info.model.ts && \
      grep -c "51-100\|101-500" core/models/company-info.model.ts && \
      grep -c "51-200\|201-500" core/models/company-info.model.ts && echo "⚠️  WARNING: Legacy employee-count bands still present in model" || echo "✓ PASS: New bands in model" && \
      grep -c "NONPROFIT\|GOVERNMENT\|HOSPITAL_HEALTHCARE" onboarding/company-profile-form.component.html && \
      grep -c "mat-select.*businessClassification\|mat-select.*employeeCount" onboarding/company-profile-form.component.html && \
      grep -c "51-100\|101-500" onboarding/company-profile-form.component.html && \
      grep -c "51-200\|201-500" onboarding/company-profile-form.component.html && echo "⚠️  WARNING: Legacy bands still in template" || echo "✓ PASS: New bands in template" && \
      grep -c "@for.*BUSINESS_CLASSIFICATION\|@for.*EMPLOYEE_COUNT\|\*ngFor" onboarding/company-profile-form.component.html && \
      npx tsc -p tsconfig.spec.json --noEmit
    </automated>
  </verify>
  <done>
    businessClassification (7 LOCKED values per Director precision) and employeeCount (7 re-banded values) picklists integrated into company-profile form. Both use mat-select with modern Angular 21 syntax (@for). businessClassification constant exported with exact key-label pairs. employeeCount constant exported with 7 re-banded string values. Legacy bands ("51-200", "201-500") removed from company-info.model.ts and seed-zb-provider.ts (touch-it-fix-it). businessClassification and employeeCount enums in company-info.model.ts updated. TypeScript compile passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form → Service | User submits company-info + corporate-profile data; service enforces org scope (orgId FK) |
| Service → Pipeline | Writes to typed classes via PipelineWriteService; class IDs must resolve |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Authorization | Company-info + corporate-profile form | mitigate | All writes scoped to orgId (form's org context). Form component must enforce that the user can only edit their own org (guard gate). Once orgId is supplied, writes are org-scoped via FK. |
| T-33-10 | Information Disclosure | Corporate-profile sensitive sections | mitigate | Financial, insurance, reference sections may contain sensitive data. These are gated separately (vetting track, Phase 033, disclosure-gating). For v1, all sections are readable by the org. No PII-leakage risk; data is org-internal. |
| T-33-11 | Input Validation | Form field values | mitigate | Form must validate field shapes before calling write methods (e.g., email format in reference contact, numeric fields for financial). Angular Reactive Forms validators handle most cases. Server-side (PipelineWriteService) performs final validation. |

</threat_model>

<verification>
1. `npx tsc -p tsconfig.spec.json --noEmit` passes with no errors.
2. Company-info sections write to OrgProfile + Address via updateProfile.
3. OrgSegment classification step is integrated into onboarding.
4. businessClassification uses 7 LOCKED values (D-57 + Director precision).
5. employeeCount uses 7 re-banded values ("51-100", "101-500", etc.).
6. Corporate-profile form sections write to typed classes (InsuranceCoverage, ClientReference, Personnel, FinancialProfile).
7. All writes default verified=false / verificationSource=null.
8. Attestation section is not rendered or written (§7 #8 retired).
9. Error handling surfaces failures to user via snackBar.
10. All writes include callSiteTag for telemetry.
11. Picklists rendered with mat-select using @for (not *ngFor).
</verification>

<success_criteria>
- Onboarding company-info retargeted to OrgProfile + Address writes.
- OrgSegment classification step added.
- businessClassification (7 values) + employeeCount (7 re-banded values) picklists deployed in all forms with LOCKED values per D-57 + Director precision.
- Corporate-profile form (5 sections) write to typed classes via PipelineWriteService.
- All writes org-scoped, default asserted (verified=false), error handling present.
- Attestation section retired.
- TypeScript compile passes.
- Tests cover all write paths.
</success_criteria>

<output>
Create `.planning/phases/33-profile-expertise-company-info-re-home/33-04-SUMMARY.md` when complete.
</output>
