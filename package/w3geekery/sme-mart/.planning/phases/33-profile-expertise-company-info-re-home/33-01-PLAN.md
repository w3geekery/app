---
phase: 33
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - core/models/provider.model.ts
  - core/models/marketplace-profile-item.model.ts
  - core/services/pipeline-write.service.ts
autonomous: true
requirements: [PROF-01, PROF-02]
user_setup: []

must_haves:
  truths:
    - "All `Provider*` expertise junctions (Skill, Role, Product, Framework, Segment, ServiceSegment) use `orgId` scope, Catalog FK names (`skillId` not `zerobias_skill_id`), and carry `verified`/`verificationSource` per D-53"
    - "OrgProfile replaces ProviderProfile; fields scatter per migration-mapping §3 (headline→tagline, about→shortDescription/longDescription, zerobias_org_id→orgId)"
    - "`MarketplaceProfileItem` model and all Neon-backed constants are deleted; no consumers remain"
    - "SME_MART_CLASS_IDS includes the 13 Phase-33 class IDs (6 Provider* + 4 corporate-profile + 1 OrgSegment + Address + OrgProfile); all deterministic UUIDs from published schema"
    - "ESLint + modernization rules pass on rewritten files (type-suffixed, no unused imports, no deprecated patterns)"
  artifacts:
    - path: "core/models/provider.model.ts"
      provides: "Org-scoped expertise junction types (ProviderSkill, ProviderRole, ProviderProduct, ProviderFramework, ProviderSegment, ProviderServiceSegment), OrgSegment, typed-class field shapes with verified/verificationSource"
      min_lines: 150
    - path: "core/services/pipeline-write.service.ts"
      provides: "SME_MART_CLASS_IDS constant with 13 Phase-33 additions registered"
      contains: "ProviderSkill, ProviderRole, ProviderProduct, ProviderFramework, ProviderSegment, ProviderServiceSegment, OrgProfile, Address, InsuranceCoverage, ClientReference, Personnel, FinancialProfile, OrgSegment"
  key_links:
    - from: "provider.model.ts"
      to: "provider-profiles.service.ts"
      via: "type imports"
      pattern: "import.*Provider(Skill|Role|Product|Framework|Segment|ServiceSegment|Profile)"
    - from: "pipeline-write.service.ts"
      to: "provider-profiles.service.ts (Half B)"
      via: "SME_MART_CLASS_IDS lookup"
      pattern: "SME_MART_CLASS_IDS\\[(Provider|OrgProfile|Address|Insurance)"
---

<objective>
Lay the foundation for Phase 33 by rewriting the provider expertise model for org-scope + Catalog FKs, deleting the legacy MarketplaceProfileItem model and its Neon-backed constants, and registering the 13 missing class IDs in `SME_MART_CLASS_IDS` so Half-B writes can execute.

**Purpose:** The legacy provider model centers on `ProviderProfile` (a pseudo-entity) with provider-scoped expertise junctions carrying denormalized names. Phase 33 dissolves ProviderProfile, org-scopes all expertise, and drops denormalization. Model rewrites and class-ID registration must complete before any Half-B write task executes.

**Output:** Rewritten `provider.model.ts` (org-scoped, Catalog FKs, verified/verificationSource on every junction), deleted `marketplace-profile-item.model.ts`, updated `pipeline-write.service.ts` with 13 Phase-33 class IDs, ESLint + modernization clean.

**⚠️ WAVE-0 GATE (RED-BY-DESIGN):** This is a foundation wave. Deleting the types `ProviderProfile`/`ProviderDirectoryRow`/`ProviderDetailRow` and the dropped fields (`hourly_rate`/`availability_status`/`response_time`) will cause `tsc` failures in existing consumers (33-02 + 33-06 migrate them). **GREEN `tsc -p tsconfig.spec.json --noEmit` is NOT asserted per-task; it is the WAVE-1 EXIT gate after all consumer migrations (33-02 + 33-06) land.** Per-task gates: (1) class-ID registration via SME_MART_CLASS_IDS grep, (2) ESLint + modernization on rewritten files. Wave-1 must immediately follow Wave-0; 33-02 + 33-06 run in parallel to green the tsc gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md
@.planning/phases/33-profile-expertise-company-info-re-home/33-PATTERNS.md
@.planning/docs/MODERNIZATION_GUIDE.md
@.planning/docs/SDK_VERIFICATION_SOURCES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite provider.model.ts — org-scoped expertise junctions + OrgProfile types</name>
  <files>core/models/provider.model.ts</files>
  <read_first>
    @.planning/phases/33-profile-expertise-company-info-re-home/33-PATTERNS.md (section "core/models/provider.model.ts")
    @.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (sections 2.1–2.8: field shapes for all 8 classes)
    @.planning/docs/MODERNIZATION_GUIDE.md
    @.claude/skills/sme-mart-architect.md (satisfy the architect-skill-gate)
  </read_first>
  <action>
Rewrite provider.model.ts to define org-scoped types per the migration-mapping design (CONTEXT.md §"The structural shift"). The file must export interfaces for 8 typed classes:

1. **Expertise junctions (6 types, each org-scoped):** `ProviderSkill`, `ProviderRole`, `ProviderProduct`, `ProviderFramework`, `ProviderSegment`, `ProviderServiceSegment`. For each:
   - Rename `provider_id` → `orgId` (scope key)
   - Rename Catalog FKs: `zerobias_*_id` → `*Id` (per RESEARCH.md §2.1–2.6; e.g., `zerobias_skill_id` → `skillId`)
   - Delete denormalized names (`*_name` fields)
   - Add `verified: boolean` (defaults false per D-53)
   - Add `verificationSource: string | null` (defaults null per D-53)
   - Preserve existing non-denorm fields per published schema (e.g., `proficiencyLevel`, `yearsExperience`, `certified`, `certificationDetails`, etc.)
2. **OrgProfile (replaces ProviderProfile, 1:1 per org):** Fields per RESEARCH.md §2.7: `orgId`, `legalName`, `dba`, `tagline`, `shortDescription`, `longDescription`, `website`, `logoUrl`, `employeeCount` (re-banded enum), `businessClassification` (7-value enum per D-57), `foundedYear`, `primaryContactUserId`, `verified`, `verificationSource`.
3. **OrgSegment (universal classification, new):** Fields per RESEARCH.md §2.9 if present, or minimal: `orgId`, `segmentId` (Catalog Domain/Category FK), `isPrimary`, `verified`, `verificationSource`.

All interfaces extend the Object base class (id, name, created_at, updated_at are inherited). Use camelCase throughout (no snake_case). Preserve the enum type definitions if any (e.g., `ProficiencyLevel`). Do NOT define OrgProfile methods—types only.

**Field reference:** Use RESEARCH.md §2.1–2.8 as the authoritative field shape (published schema, verified 2026-06-25). Reconcile against any pre-2.0.6 field names in the current file; published 2.0.6 takes precedence.

**Angular 21:** Use readonly properties. No constructor injection (will use inject() in components). Remove unused import statements. Add type-suffixed filename validation (should already be `.ts`, not another suffix).

**File flow:**
1. Open current provider.model.ts and read line-by-line to identify all interface definitions.
2. For each interface, apply the structural shifts per migration-mapping (rename provider_id→orgId, zerobias_*_id→*Id, drop *_name, add verified/verificationSource).
3. Add OrgProfile interface (new, replaces ProviderProfile; fields per RESEARCH.md §2.7).
4. Add OrgSegment interface (new, per PROF-09).
5. Delete ProviderProfile, ProviderDirectoryRow, ProviderDetailRow interfaces (these will cause tsc failures in 33-02 + 33-06; those plans migrate the consumers).
6. Verify all field names match RESEARCH.md published shapes; reconcile any drift toward schema.
7. Commit: file is modern-clean and ESLint passes. Consumers migrate in Wave-1.
  </action>
  <verify>
    <automated>
      grep -c "ProviderSkill {" core/models/provider.model.ts && \
      grep -c "orgId: string" core/models/provider.model.ts && \
      grep -c "skillId: string" core/models/provider.model.ts && \
      grep -c "verified: boolean" core/models/provider.model.ts && \
      grep -c "zerobias_skill_id" core/models/provider.model.ts && echo "✗ FAIL: Found legacy zerobias_skill_id" || echo "✓ PASS: No legacy zerobias_ prefixes" && \
      grep -c "OrgProfile {" core/models/provider.model.ts && \
      grep -c "ProviderProfile\|ProviderDirectoryRow\|ProviderDetailRow" core/models/provider.model.ts && echo "✓ NOTE: Deleted types removed (consumers migrate in Wave-1)" || echo "✓ PASS: Deleted types absent" && \
      npx eslint core/models/provider.model.ts --max-warnings=0 2>&1 | grep -c "error\|Error" || echo "✓ PASS: ESLint clean"
    </automated>
  </verify>
  <done>
    provider.model.ts exports 8 org-scoped interfaces (`ProviderSkill`, `ProviderRole`, `ProviderProduct`, `ProviderFramework`, `ProviderSegment`, `ProviderServiceSegment`, `OrgProfile`, `OrgSegment`), all with `orgId` scope, Catalog `*Id` FKs (no `zerobias_` prefixes), and `verified`/`verificationSource` fields. ProviderProfile, ProviderDirectoryRow, ProviderDetailRow are deleted. Field shapes match RESEARCH.md §2.1–2.8 (published schema 2.0.6). ESLint + modernization clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete marketplace-profile-item.model.ts and retarget all imports</name>
  <files>
    core/models/marketplace-profile-item.model.ts
  </files>
  <read_first>
    @.planning/phases/33-profile-expertise-company-info-re-home/33-CONTEXT.md (sections "Out of scope" and "The 6 expertise junctions")
    @.planning/docs/SDK_VERIFICATION_SOURCES.md (ground truth discipline)
  </read_first>
  <action>
Identify all imports of `marketplace-profile-item.model.ts` in the codebase and determine their migration path, then delete the file.

**Grep for all imports:**
Use `grep -r "marketplace-profile-item" src/ --include="*.ts"` to find every reference. For each hit:
1. If it's an import statement (e.g., `import { MarketplaceProfileItem }...`), note the consumer file and the type being imported.
2. Determine the replacement type from the Phase 33 migration:
   - `MarketplaceProfileItem` → split across `OrgProfile` + the 6 expertise junction types (per migration-mapping §4).
   - `MpiRow` → `GqlOrgProfileResponse` (in the provider-profiles.service.ts Half-A reads).
   - Any MPI-backed constants (field lists, VIEW names) → deleted (Half-A re-points to GQL queries, which reference the 6 junctions directly by name).
3. If the consumer is out-of-scope for Phase 33 (e.g., a legacy read path in a different service), **do NOT modify it** — instead, create a stub or leave the import in place if the file still exists after Half-A completes. Consumers are migrated in Wave-1 by 33-02 + 33-06.
4. Consumers are migrated in Wave-1; this task just deletes the file.

**Deletion:**
After confirming the migration path above, delete the file:
```bash
rm core/models/marketplace-profile-item.model.ts
```

Verify the file is gone:
```bash
ls core/models/marketplace-profile-item.model.ts 2>&1 | grep "No such file"
```

**Impact check:**
Deleting the file WILL cause tsc failures in existing consumers (my-profile-expertise.component, provider-detail.component, provider-list.component, home.component, provider-card.component). These are migrated in Wave-1 by 33-02 + 33-06. The per-task gate is file deletion, not tsc pass.
  </action>
  <verify>
    <automated>
      ls core/models/marketplace-profile-item.model.ts 2>&1 | grep -q "No such file\|cannot access" && echo "✓ PASS: File deleted" || echo "✗ FAIL: File still exists" && \
      grep -r "marketplace-profile-item" src/ --include="*.ts" | wc -l && echo "⚠️  Wave-1 consumers will import from deleted file (migrate in 33-02/33-06)"
    </automated>
  </verify>
  <done>
    marketplace-profile-item.model.ts is deleted. File no longer exists on disk. Existing imports will cause tsc failures (expected; consumers migrate in Wave-1 via 33-02 + 33-06). Per-task gate: file deleted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Register 13 Phase-33 class IDs in SME_MART_CLASS_IDS</name>
  <files>core/services/pipeline-write.service.ts</files>
  <read_first>
    @.planning/phases/33-profile-expertise-company-info-re-home/33-PATTERNS.md (section "core/services/pipeline-write.service.ts")
    @.planning/phases/33-profile-expertise-company-info-re-home/33-RESEARCH.md (section "1.3 Class ID Registration — CRITICAL BLOCKER")
    @.planning/director/profile-migration-mapping-2026-06-08.md (canonical reference for class names)
  </read_first>
  <action>
Add 13 missing class IDs to the `SME_MART_CLASS_IDS` constant (pipeline-write.service.ts lines 10–47). The 13 classes are:

**6 expertise junctions (org-scoped):**
1. ProviderSkill
2. ProviderRole
3. ProviderProduct
4. ProviderFramework
5. ProviderSegment
6. ProviderServiceSegment

**4 corporate-profile typed classes:**
7. OrgProfile
8. Address
9. InsuranceCoverage
10. ClientReference

**2 additional classes:**
11. Personnel
12. FinancialProfile
13. OrgSegment

**ID source — RESOLVED. Director Parks MCP-verified all 13 on BOTH uat-zb AND prod-zb (`platform.Class.getClass`) 2026-06-26 — byte-identical across envs.** These are the REAL values — register them verbatim. NO placeholders, NO execute-time MCP dependency. All verified `packageCode: w3geekery.smemart.schema`; all UUIDv5 (`-5xxx-` nibble) → deterministic, env-stable (proven identical UAT==prod), so do NOT build a per-env switch. `ProviderServiceSegment` matches the D-56 anchor exactly.

**Register these EXACT values** into `SME_MART_CLASS_IDS` (append after the existing 16 entries; keep those as-is):
```typescript
  // Phase 33 — Provider expertise junctions (org-scoped; UUIDv5, env-stable UAT==prod; MCP-verified 2026-06-26)
  ProviderSkill:          '91a32787-5d86-5d58-9143-152d1bc5dad2',
  ProviderRole:           '6098fe68-f656-51fe-87d8-dde87b50efc6',
  ProviderProduct:        '63cd2a00-1dc8-5152-9d36-eaf98abfa8ee',
  ProviderFramework:      '47a4ce15-d87c-5eb7-b4ce-b5057da882be',
  ProviderSegment:        '1b211929-39af-5d81-b205-3bf2c23d45d6',
  ProviderServiceSegment: '5d698106-3a8a-530d-9060-52e1aa7ab134',  // matches D-56 anchor

  // Phase 33 — Corporate-profile + classification typed classes (MCP-verified UAT==prod 2026-06-26)
  OrgProfile:             '8001e339-2609-54ae-b407-9c2ab7ebf413',
  Address:                'cce4037a-ed62-5aec-bcd3-cbc8afb0546d',
  InsuranceCoverage:      '7b0b6b97-b99e-5267-9cf0-2dd3f5888997',
  ClientReference:        '632ced7f-85d5-5f6e-9f99-8258001d14cc',
  Personnel:              '23dd60e6-100d-5d65-bc09-af594215e27e',
  FinancialProfile:       '5b4cba20-be1c-5747-b355-5ff18b7b76b5',
  OrgSegment:             '780543c7-fbe5-5e8e-8bd5-5361689c1ce6',
```

**Execution note:** Wave-0 foundation task. Half-B writes (33-03/33-04) resolve these via `new UUID(classId)`, which throws synchronously on a bad value → a missing/placeholder ID fails LOUD at the first write, not silently (the prior "fire-and-forget silent failure" was already remediated under FF-03 — `pushEntities`/`deleteEntities` catch-blocks log `[PIPELINE_WRITE_FAILURE]` and re-throw). Registering the real IDs above retires the class-ID blocker; NO executor MCP step needed.
  </action>
  <verify>
    <automated>
      ! grep -qE "UUID-FROM-ZB-MCP|to-be-verified|xxxxxxxx|AWAITING EXECUTOR" core/services/pipeline-write.service.ts && \
      grep -qE "ProviderSkill: *'91a32787-5d86-5d58-9143-152d1bc5dad2'" core/services/pipeline-write.service.ts && \
      grep -qE "ProviderServiceSegment: *'5d698106-3a8a-530d-9060-52e1aa7ab134'" core/services/pipeline-write.service.ts && \
      grep -qE "OrgProfile: *'8001e339-2609-54ae-b407-9c2ab7ebf413'" core/services/pipeline-write.service.ts && \
      grep -qE "OrgSegment: *'780543c7-fbe5-5e8e-8bd5-5361689c1ce6'" core/services/pipeline-write.service.ts && \
      for k in ProviderRole ProviderProduct ProviderFramework ProviderSegment Address InsuranceCoverage ClientReference Personnel FinancialProfile; do grep -qE "$k: *'[0-9a-f]{8}-[0-9a-f]{4}-5[0-9a-f]{3}-[0-9a-f]{4}-[0-9a-f]{12}'" core/services/pipeline-write.service.ts || exit 1; done && \
      echo "✓ PASS: all 13 real MCP-verified class IDs registered, no placeholders"
    </automated>
  </verify>
  <done>
    SME_MART_CLASS_IDS includes all 13 Phase-33 class IDs registered with the EXACT MCP-verified UUIDs above (6 expertise junctions + OrgProfile/Address/InsuranceCoverage/ClientReference/Personnel/FinancialProfile/OrgSegment), under phase comments. No placeholder strings remain (grep gate passes). All 13 match the UUIDv5 regex; ProviderServiceSegment === D-56 anchor. Class-ID blocker RETIRED — Wave-2 Half-B writes resolve every target class. (Per-task gate is grep-only — green `tsc` is the Wave-1-exit gate, this is a red-by-design foundation wave.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Pipeline | Writes to AuditgraphDB via Pipeline.receive; class IDs must resolve correctly |
| Component → Service | Components pass org ID + expertise data; service validates org scope before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Authorization | Expertise mutation methods | mitigate | Service methods enforce `orgId` scope — writes are org-scoped (orgId FK, not provider_id). No cross-org write possible if service rewrite enforces scope per D-23 org-scoping audit. |
| T-33-02 | Tampering | Class ID constant | mitigate | Class IDs are deterministic UUIDs from schema (env-stable). Register exact IDs from ZB MCP verification step; do not guess or use stale IDs. Mismatched IDs cause runtime pushEntity failure, which surfaces as an error (not silent corruption). |
| T-33-03 | Information Disclosure | verified/verificationSource fields | mitigate | New fields expose claim provenance to the UI (asserted vs vetted). This is intentional per D-53. Field values are non-sensitive (boolean + enum string). No PII leakage. |
| T-33-SC | Supply Chain | Published schema classes | mitigate | Schema defines 8 typed classes; field shapes verified via npm pack (published 2.0.6). Model types in provider.model.ts must match published shapes exactly. Deviation → rewrite per RESEARCH.md reconciliation rule. |

</threat_model>

<verification>
1. ESLint + modernization clean on rewritten files (no errors on `npm run lint` diff).
2. No legacy `zerobias_*_id`, `provider_id`, or `*_name` fields in the rewritten model types.
3. `marketplace-profile-item.model.ts` file does not exist on disk.
4. SME_MART_CLASS_IDS constant includes all 13 Phase-33 class IDs with phase-comment headers.
5. No grep matches for `marketplace-profile-item` in src/ (or only in Wave-1 consumer files being migrated).
6. Per-task gates pass (class-ID grep + ESLint clean). **tsc GREEN is the Wave-1 EXIT gate, not a per-task gate.**
</verification>

<success_criteria>
- Org-scoped provider types (ProviderSkill, etc.) use `orgId` scope, Catalog `*Id` FKs, and verified/verificationSource fields per D-53.
- OrgProfile replaces ProviderProfile; ProviderDirectoryRow/ProviderDetailRow are deleted.
- MarketplaceProfileItem model and file are deleted.
- SME_MART_CLASS_IDS includes 13 Phase-33 class IDs (verified or placeholders pending executor confirmation).
- ESLint + modernization clean.
- **WAVE-0 is red-by-design. Wave-1 (33-02 + 33-06 parallel) migrates consumers and greens tsc.**
</success_criteria>

<output>
Create `.planning/phases/33-profile-expertise-company-info-re-home/33-01-SUMMARY.md` when complete.
</output>
