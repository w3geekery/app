---
phase: "32"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/shared/components/boards-grid.component.ts
  - src/app/shared/components/boards-grid.component.html
  - src/app/shared/components/boards-grid.component.scss
  - src/app/shared/components/board-card.component.ts
  - src/app/shared/components/board-card.component.html
  - src/app/shared/components/board-card.component.scss
  - src/app/shared/components/board-card-pinned-preview.component.ts
  - src/app/shared/components/board-card-pinned-preview.component.html
  - src/app/shared/components/boards-grid.component.spec.ts
  - src/app/shared/components/board-card.component.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "boards-grid component renders a responsive card grid with CSS Grid `repeat(auto-fill, minmax(320px, 1fr))`"
    - "Pinned cards span 2 columns; unpinned cards span 1 (L-5, Sketch 001 Variant A)"
    - "Full-width single-column layout below 760px breakpoint (mobile fallback per L-5)"
    - "Pin button emits output signal with board ID and isPinned boolean (L-4)"
    - "Drill button emits output signal with board ID (L-4)"
    - "Component zero dependencies on SME-Mart-specific services (L-12)"
    - "All styling uses theme tokens (CF-1: `--zb-background`, `--zb-background-card`, `--zb-text`, `--mat-sys-*`), no hardcoded colors"
    - "Pin and drill buttons are keyboard-accessible and screen-reader-labeled (CF-2: `aria-expanded`, `aria-label`)"
    - "Board view-model consumed by cards is typed and atomic (no mixed-axis strings, no positional ordering); RDF-serializable per C-2 (Director-enforced Holon constraint)"
  artifacts:
    - path: src/app/shared/components/boards-grid.component.ts
      provides: "Shared board grid container; accepts Board[], pinnedBoardIds, emits cardClicked + pinToggled"
      min_lines: 80
    - path: src/app/shared/components/board-card.component.ts
      provides: "Individual card presentation (name, description, type, status, default-chip)"
      min_lines: 60
    - path: src/app/shared/components/board-card-pinned-preview.component.ts
      provides: "Task count label for pinned-state cards (≤25 tasks, paged)"
      min_lines: 30
    - path: src/app/shared/components/boards-grid.component.spec.ts
      provides: "Unit tests: pin toggle, drill click, grid layout, no SME-Mart service injection"
      min_lines: 120
  key_links:
    - from: "src/app/shared/components/boards-grid.component.ts"
      to: "src/app/shared/components/board-card.component.ts"
      via: "board-card child component"
      pattern: "imports.*BoardCardComponent"
    - from: "board-card component"
      to: "pin toggle + drill button"
      via: "@Output signals emitted to parent"
      pattern: "pinToggled.emit|cardClicked.emit"
    - from: "CSS Grid styling"
      to: "pinned card layout"
      via: "`[class.pinned]` binding + `grid-column: span 2`"
      pattern: "grid-column: span 2"

---

## ANGULAR 21 MODERNIZATION RULES — NON-NEGOTIABLE (pasted verbatim per Director governance)

```
ANGULAR 21 MODERNIZATION RULES — NON-NEGOTIABLE

Every new and touched file in this phase MUST follow these rules. Pre-commit hook is diff-based, --max-warnings=0, blocks commit on violation. Touch-it=fix-it on every modified file.

1. STANDALONE COMPONENTS ONLY. No NgModules.
2. SIGNAL-BASED I/O: input() / output() — NEVER @Input() / @Output().
3. DEPENDENCY INJECTION via inject() — NEVER constructor parameter injection.
4. SIGNALS for state: signal() / computed() / effect() — not BehaviorSubject for component-local state.
5. VIEW QUERIES: viewChild() / viewChildren() — NEVER @ViewChild() / @ViewChildren().
6. CHANGE DETECTION: ChangeDetectionStrategy.OnPush on every component.
7. TEMPLATE CONTROL FLOW: @if / @for / @switch — NEVER *ngIf / *ngFor / *ngSwitch.
8. TYPE SAFETY: no `: any`. Narrow types. No empty catch {} blocks (comment + log or rethrow).
9. NGX-LIBRARY REUSE: prefer zb-remote-table, zb-simple-panel, zb-button-label, zb-dialog, zb-resource-status, zb-chip-colors-directive, snake-to-spaces pipe, etc. BEFORE building custom.
10. THEME TOKENS: --zb-background, --zb-background-card, --zb-text, --mat-sys-* — NEVER hardcoded colors. See BACKLOG-103 for what hardcoded colors cost.
11. FILE NAMING: keep type suffixes (`foo.component.ts`, `foo.service.ts`) — project convention overrides Angular 21's drop-the-suffix default.
12. SHARED-COMPONENT DEPENDENCY RULE: any component that ends up in `src/app/shared/components/` MUST be free of SME-Mart-specific services (engagements.service, sme-mart-project.service, etc.) so it can be reused on Project > Boards + hoisted to ngx-library later.

INVOKE the `sme-mart-architect` skill (and/or `/angular-architect`) at plan-phase for any decomposition question on shared components, signal-input shape, ngx-library reuse, or change-detection patterns. Don't guess.

Reference: .planning/docs/MODERNIZATION_GUIDE.md (canonical rules + troubleshooting). Eslint config at eslint.config.js encodes the rules; pre-commit hook at ../../../.husky/pre-commit + .lintstagedrc.json enforces them.
```

---

<objective>
Deliver the shared, dependency-free `boards-grid` component substrate: a responsive card grid with pin-to-expand-in-place inline preview affordance (Sketch 001 Variant A, L-5). This component accepts board data as input and emits pin/drill events to the parent, enabling reuse across engagement-scoped and project-scoped contexts without SME-Mart-specific service coupling (L-12). Board view-model is typed and atomic per C-2 RDF Compass constraint (Director-enforced for Holon projection support).

Purpose: Unblock parallel development of engagement/project board surfaces while maintaining a single, testable, reusable card grid implementation. The component is the foundation for the Boards feature — all subsequent board UX builds on this. Board data shape is locked to support future RDF/OWL/SHACL audit-trail container.

Output: Three production components (`boards-grid`, `board-card`, `board-card-pinned-preview`) + full unit test suite covering grid layout, pin/drill affordances, keyboard accessibility, zero-injection validation, and RDF-serializable board model structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-boards-engagement-project-cross-org/32-CONTEXT.md
@.planning/phases/32-boards-engagement-project-cross-org/32-RESEARCH.md
@.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md
@.planning/sketches/001-boards-pin-expand/index.html
@.planning/sketches/001-boards-pin-expand/README.md
@.planning/docs/RDF-COMPASS.md
@.planning/docs/MODERNIZATION_GUIDE.md
@src/app/pages/project/tabs/project-boards-tab.component.ts
@src/app/pages/org/tabs/vendor-profile-form.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared board-card sub-component (presentation-only, no state)</name>
  <files>
    src/app/shared/components/board-card.component.ts
    src/app/shared/components/board-card.component.html
    src/app/shared/components/board-card.component.scss
  </files>
  <read_first>
    @src/app/pages/project/tabs/project-boards-tab.component.ts (card template pattern, lines 48-70; analog for board data shape + Material card wiring)
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (Pattern: boards-grid sub-component layout, lines 28-117; signal I/O shape per modernization rule 2)
    @.planning/sketches/001-boards-pin-expand/index.html (card visual: name, description, type, status, isDefault chip, pin + drill buttons)
  </read_first>
  <action>
Create standalone component `BoardCardComponent` that renders a single board card. No data-fetching, no SME-Mart service injection.

**Input signals (L-12 dependency-free, C-2 RDF constraint):**
- `board` (required): Board object with `{ id, name, description, boardType, status, isDefault }` — all fields typed; no mixed-axis strings
- `isPinned` (optional, default false): boolean
- `maxPreviewTasks` (optional, default 25): number for preview label

**Note on C-2 (RDF Compass):** Board object passed as input is typed and atomic:
- Each field is a discrete property (no positional ordering)
- No encoded state in strings (e.g., "name:project:status" concatenated)
- Board data is round-trippable to RDF triples (e.g., `board:id rdf:type Board; board:name ".."; board:boardType ".."`
- This ensures future Holon/RDF projection doesn't require decoding proprietary string formats

**Output signals (L-4 pin + drill affordances):**
- `pinToggled`: emits `{ boardId: string; isPinned: boolean }`
- `cardClicked`: emits `boardId: string`

**Template structure:**
- `mat-card` with `[class.pinned]="isPinned"` (for CSS Grid span control from parent)
- `mat-card-header`: board name + isDefault chip (if true)
- `mat-card-subtitle`: boardType (titlecase) + status (titlecase)
- `mat-card-content`: description (if present)
- `mat-card-actions`: two icon-buttons
  - Pin button: icon `favorite` (filled) or `favorite_border` (empty); click → emit pinToggled event
  - Drill button: icon `open_in_new`; click → emit cardClicked event
- Pinned preview section (if isPinned): static label showing `${maxPreviewTasks} tasks` (child task-list will be wired by parent in Plan 02)

**CSS (L-5 + CF-1 token sourcing):**
- Card styling via theme tokens: `--zb-background-card`, `--zb-text`, `--mat-sys-outline`, `--mat-sys-on-surface-variant` (NOT hardcoded colors)
- Pin/drill button styling via Material's default button colors
- Responsive padding + gap via rem units

**Accessibility (CF-2):**
- Pin button: `[aria-label]="'Pin ' + board.name"` and `[aria-expanded]="isPinned"` (screen reader announces expanded state)
- Drill button: `[aria-label]="'Open ' + board.name"`
- All text via Angular binding (no innerHTML; Material sanitizes)

**Per modernization rules:**
- Rule 1: Standalone component
- Rule 2: Signal-based I/O (input/output, no @Input/@Output)
- Rule 3: No service injection (zero inject() calls)
- Rule 6: ChangeDetectionStrategy.OnPush
- Rule 7: @if / @for template syntax
- Rule 10: Theme tokens only, no hardcoded colors
- Rule 11: File suffix `.component.ts` (naming convention)
  </action>
  <verify>
    <automated>npm test -- --include='**/board-card.component.spec.ts' --watch=false</automated>
  </verify>
  <done>
    Component created and unit-tested. Tests cover: (1) Input signal bindings render correctly, (2) Pin toggle emits pinToggled with correct boardId + isPinned, (3) Drill click emits cardClicked with boardId, (4) isDefault chip renders conditionally, (5) Accessibility attributes (aria-label, aria-expanded) present and correct, (6) No SME-Mart services injected (grep validates zero `inject(Engagement*Service)` / `inject(SmeMart*Service)`), (7) Theme tokens used in SCSS (no hardcoded colors like #fff or rgb()), (8) Board input is typed (no `: any`); C-2 constraint verified: fields are discrete properties, not encoded strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create board-card-pinned-preview sub-component (task count label stub)</name>
  <files>
    src/app/shared/components/board-card-pinned-preview.component.ts
    src/app/shared/components/board-card-pinned-preview.component.html
    src/app/shared/components/board-card-pinned-preview.component.scss
  </files>
  <read_first>
    @.planning/phases/32-boards-engagement-project-cross-org/32-RESEARCH.md (Example 2: boards-grid code, lines 570-619; pinned-preview stub placeholder comment)
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (board-card-pinned-preview sub-component, line 15; role = pinned task preview display)
  </read_first>
  <action>
Create standalone component `BoardCardPinnedPreviewComponent` that displays a simple label for pinned board preview. This is a **Foundation-Phase stub**; the actual task-list table wiring happens in Plan 02 (engagement-boards-tab parent will pass task data).

**Input signals (L-12 dependency-free):**
- `maxTasks` (optional, default 25): number
- `pinnedTaskCount` (optional, default 0): number of tasks currently shown

**Output signals:**
- None (read-only label component)

**Template:**
- Static background container with border-top separator
- Text: `"${pinnedTaskCount} tasks"` (or "No tasks" if count is 0)
- Placeholder comment: "Task list will be wired in Plan 02 (engagement-boards-tab + board-detail parent)"

**CSS (CF-1):**
- Background: `--zb-background-card`
- Border: `1px solid --mat-sys-outline`
- Padding: 0.75rem
- Text size: 0.85rem; color: `--mat-sys-on-surface-variant`

**Accessibility (CF-2):**
- No interactive elements; informational label only
- Text readable by screen readers (no aria-hidden)

**Per modernization rules:**
- Rule 1: Standalone
- Rule 2: Signal-based I/O
- Rule 6: OnPush
- Rule 7: @if template syntax
- Rule 10: Theme tokens only
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit && npm test -- --include='**/board-card-pinned-preview.component.spec.ts' --watch=false</automated>
  </verify>
  <done>
    Component created and type-checked. Unit tests cover: (1) Input signal maxTasks + pinnedTaskCount render in label text, (2) Zero tasks shows "No tasks", (3) Multiple tasks shows count correctly, (4) No service injection, (5) Theme tokens used.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create boards-grid parent component (responsive card grid container)</name>
  <files>
    src/app/shared/components/boards-grid.component.ts
    src/app/shared/components/boards-grid.component.html
    src/app/shared/components/boards-grid.component.scss
  </files>
  <read_first>
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (boards-grid pattern, lines 28-117; full template + CSS Grid example; signal shape lines 44-55)
    @src/app/pages/project/tabs/project-boards-tab.component.ts (grid template structure + API call pattern for reference, lines 1-50, 118-169)
    @.planning/sketches/001-boards-pin-expand/index.html (visual: CSS Grid layout, pinned card span 2, responsive fallback < 760px)
  </read_first>
  <action>
Create standalone component `BoardsGridComponent` that renders board cards in a responsive CSS Grid layout with pin-to-expand-in-place affordance (L-5, Sketch 001 Variant A).

**Input signals (L-12 dependency-free):**
- `boards` (optional, default []): Board[]
- `pinnedBoardIds` (optional, default []): string[] (board IDs currently pinned)
- `maxPreviewTasks` (optional, default 25): number

**Output signals (L-4 pin + drill affordances):**
- `cardClicked`: emits `string` (boardId)
- `pinToggled`: emits `{ boardId: string; isPinned: boolean }`

**Template structure:**
- Wrapper div: `.boards-grid` (CSS Grid container)
- `@for (board of boards(); track board.id)`:
  - Import + render `<app-board-card>` with all inputs + output event bindings
  - `[class.pinned]="pinnedBoardIds().includes(board.id)"` (CSS class for grid-column span control)
  - Forward `(pinToggled)="pinToggled.emit($event)"` + `(cardClicked)="cardClicked.emit($event)"`
- Empty state: `@if (boards().length === 0)` → show "No boards" message

**CSS (L-5 + CF-1):**
```scss
.boards-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
  gap: 1.5rem;
  padding: 1.5rem;
}

.board-card {
  // board-card component styles live in board-card.component.scss
}

.board-card.pinned {
  grid-column: span 2;
}

@media (max-width: 760px) {
  .boards-grid {
    grid-template-columns: 1fr; /* Full-width on mobile */
  }
  
  .board-card.pinned {
    grid-column: span 1; /* Override: pinned=full-width on mobile */
  }
}
```

**Per modernization rules:**
- Rule 1: Standalone
- Rule 2: Signal-based I/O (input/output)
- Rule 3: NO service injection (zero inject() calls; parent owns data + pin state)
- Rule 6: OnPush
- Rule 7: @if / @for syntax
- Rule 10: Theme tokens (padding, gap via CSS custom properties if needed)
- Rule 12: No SME-Mart-specific services (L-12 verification: grep shows zero `inject(Engagement*Service)`, `inject(SmeMart*Service)`, `inject(PinStorage)` — parent owns pin-state)

**Design notes (L-4, L-5):**
- Pin and drill are coexistent affordances (not alternatives); both buttons always visible
- Pinned cards expand in-place via grid-column span + inline task preview (child content, wired by parent)
- Full URL navigation happens in parent (boards-grid emits event; parent decides action)
  </action>
  <verify>
    <automated>npm test -- --include='**/boards-grid.component.spec.ts' --watch=false && npx eslint src/app/shared/components/boards-grid.component.ts --rule 'no-restricted-properties'</automated>
  </verify>
  <done>
    Component created and tested. Spec tests cover: (1) CSS Grid renders with correct columns (minmax 320px), (2) Pinned card gets `grid-column: span 2` class, (3) Mobile breakpoint (< 760px) falls back to single column + pinned cards override to span 1, (4) Cards @for loop tracks by board.id, (5) Pin/drill events emitted correctly, (6) Empty state renders when boards array is empty, (7) Zero SME-Mart service injection (ESLint check passes), (8) Theme tokens sourced, no hardcoded colors. Accessibility: All interactive elements labeled (inherited from board-card).
  </done>
</task>

<task type="auto">
  <name>Task 4: Unit test suite for boards-grid + sub-components</name>
  <files>
    src/app/shared/components/boards-grid.component.spec.ts
    src/app/shared/components/board-card.component.spec.ts
    src/app/shared/components/board-card-pinned-preview.component.spec.ts
  </files>
  <read_first>
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (signal I/O pattern testing, line 545-566; Material card testing patterns; board-card testing from project-boards-tab analog)
    @src/app/shared/components/create-subtask-dialog/create-subtask-dialog.component.spec.ts (standalone component + signal-based testing patterns; Material dialog interaction tests)
  </read_first>
  <action>
Create comprehensive Jasmine spec suite covering all three components. Tests run on every commit via `npm test -- <spec-file>` (watched in dev, headless in CI).

**Test structure per component:**

*boards-grid.component.spec.ts:*
- Setup: create component instance, set input signals, verify rendering
- Test grid layout (CSS class `.boards-grid` present, grid-template-columns property checked via computed style)
- Test pinned-card span: for each pinned board ID, verify `grid-column: span 2` applied (computed style)
- Test mobile breakpoint: mock window resize to <760px, verify `grid-template-columns` becomes `1fr` (CSS media query applied)
- Test pin/drill event emission: click pin button → verify `pinToggled` emits correct { boardId, isPinned }; click drill button → verify `cardClicked` emits boardId
- Test empty state: set boards to [], verify "No boards" message renders
- Test no-injection validation: grep component source for `inject(Engagement*Service)`, `inject(SmeMart*Service)` → test fails if found

*board-card.component.spec.ts:*
- Setup: create component, set board input + isPinned input
- Test card renders with board.name, board.boardType (titlecase), board.status (titlecase)
- Test isDefault chip: when board.isDefault=true, chip renders; when false, chip hidden (@if)
- Test description: when board.description present, renders in mat-card-content; when null, hidden (@if)
- Test pin button: click → emits { boardId: board.id, isPinned: !isPinned }; aria-label + aria-expanded present
- Test drill button: click → emits cardClicked with board.id
- Test accessibility: verify aria-labels on both buttons, aria-expanded on pin button matches isPinned state
- Test no-injection: grep validates zero `inject()` calls except never-run calls (only setup-time Angular, no service DI)
- Test C-2 constraint: board input type is enforced (no `: any`); field values are atomic (no string encoding)

*board-card-pinned-preview.component.spec.ts:*
- Setup: create component, set maxTasks + pinnedTaskCount inputs
- Test label rendering: text shows "${pinnedTaskCount} tasks"
- Test zero tasks: pinnedTaskCount=0 → "No tasks" renders
- Test no-injection: grep validates zero `inject()` calls

All tests: Use Jasmine `expect().toBeTrue()` / `toContainText()` / `toHaveClass()` / `emitted()` patterns. No reliance on deprecated `*ngIf` / `*ngFor` (use `@if` / `@for` in templates, tests check rendered DOM).

**Coverage targets:**
- Lines: 100% for component logic (input signal processing, event emission)
- Branches: 100% for conditionals (@if directives)
- Functions: 100% (if any helper functions like `isPinned(id)`)

**Run command:**
```bash
npm test -- --include='src/app/shared/components/**/*.spec.ts' --watch=false --code-coverage
```
  </action>
  <verify>
    <automated>npm test -- --include='src/app/shared/components/boards-grid.component.spec.ts' 'src/app/shared/components/board-card.component.spec.ts' 'src/app/shared/components/board-card-pinned-preview.component.spec.ts' --watch=false --code-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    All specs passing (100% coverage for component logic). Tests verify: (1) Grid layout + responsive breakpoint, (2) Pin/drill events emitted with correct payloads, (3) Accessibility attributes present, (4) No SME-Mart service injection (L-12 validated), (5) Material card structure correct, (6) Signal-based I/O wired correctly per modernization rules, (7) C-2 RDF Compass constraint: board fields are typed and atomic (no string encoding). Ready for parent components to consume in Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component input (Board[]) | Board data sourced from SDK; component never mutates input |
| Pin state input (pinnedBoardIds) | Parent owns state; component emits toggle event; parent persists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Injection | boards-grid, board-card | mitigate | No service injection (L-12 enforced via grep + ESLint); component is pure UI renderer |
| T-32-02 | Tampering | pinToggled output | mitigate | Parent validates boardId + isPinned before persisting; no direct DOM manipulation of pin state |
| T-32-03 | Tampering | Board data binding | mitigate | Board data from SDK (trusted); component uses property binding (not innerHTML); Material sanitizes |

</threat_model>

<verification>
## Phase 32 Plan 01 — Shared Component Foundation

**Wave 1 completion checklist:**
- [ ] All three components created (boards-grid, board-card, board-card-pinned-preview)
- [ ] All specs passing: `npm test -- --include='src/app/shared/components/**/*.spec.ts' --watch=false` (exit code 0)
- [ ] ESLint passes diff-based gate: `npx eslint src/app/shared/components/boards-grid.component.ts --rule 'no-restricted-properties'` (zero SME-Mart service inject calls found)
- [ ] Theme tokens used throughout SCSS; no hardcoded colors (CF-1)
- [ ] Accessibility attributes present (aria-label, aria-expanded on interactive elements; CF-2)
- [ ] Components isolated and testable; zero service dependencies (L-12 verified)
- [ ] Board input is typed and atomic; RDF-serializable per C-2 (Director-enforced Holon constraint)

**Integration readiness:**
- [ ] Spec file stubs ready for engagement-boards-tab parent (Plan 02) to import + wire
- [ ] PinStorage interface + localStorage implementation wired by Plan 05 (no blocking)
- [ ] Board-detail page (Plan 03) can import + reuse boards-grid without modification

</verification>

<success_criteria>
- All unit tests pass (100% coverage for component logic)
- ESLint diff-based gate passes (pre-commit hook validation)
- No hardcoded colors; theme tokens sourced (CF-1)
- Accessibility attributes present and correct (CF-2)
- Zero SME-Mart service injection verified via grep
- Component can be imported by engagement-detail + project-detail without rewrite (L-12 proven)
- Pin/drill affordances emit correct event payloads (L-4)
- CSS Grid renders pinned cards at span 2; mobile fallback to span 1 (L-5)
- Board view-model typed and atomic (no string encoding); round-trippable to RDF triples per C-2

</success_criteria>

<output>
After completion, create `.planning/phases/32-boards-engagement-project-cross-org/32-01-SUMMARY.md` with:
- Components created (3): boards-grid, board-card, board-card-pinned-preview
- Test coverage: 100% for component logic; file paths for spec coverage report
- ESLint validation: zero SME-Mart service inject calls detected
- C-2 RDF Compass validation: board fields typed and atomic; RDF-serializable structure confirmed
- Artifacts ready for Plan 02 (engagement-boards-tab parent wiring)
</output>
