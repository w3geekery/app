---
phase: "32"
plan: "03"
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src/app/pages/boards/board-detail.component.ts
  - src/app/pages/boards/board-detail.component.html
  - src/app/pages/boards/board-detail.component.scss
  - src/app/pages/boards/board-switcher.component.ts
  - src/app/pages/boards/board-switcher.component.html
  - src/app/pages/boards/board-switcher.component.scss
  - src/app/pages/boards/boards.routes.ts
  - src/app/pages/boards/board-detail.component.spec.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Board-detail route `/boards/:boardId` loads and renders board + breadcrumb + tasks (L-2)"
    - "Breadcrumb derived from `board.projectId → project → project.parentId` chain on load (L-2)"
    - "Tasks list renders via `zb-remote-table` with column config mirroring zb-ui reference (L-7, L-8)"
    - "Board switcher widget shows sibling boards; selection navigates via full URL (L-13, D-Q8)"
    - "Cog config panel (mat-sidenav end-drawer) surface is open/closeable from button; keyboard-dismissible; internal form content deferred to Phase 33 (L-9, D-Q9)"
    - "Admin 'Open in ZB Platform' link in triple-dot menu, gated by `getPrincipal().isAdmin` (L-9)"
    - "Tasks list is read-only; no task creation/edit UI in Foundation (hard out-of-scope wall)"
    - "Party UUID (ownerId / partyId from API response) is read through for Holon projection in Phase 34+ (C-4 RDF Compass check per Director governance)"
  artifacts:
    - path: src/app/pages/boards/board-detail.component.ts
      provides: "Top-level route handler; owns board + breadcrumb + tasks fetch; mat-sidenav + switcher wiring"
      min_lines: 150
    - path: src/app/pages/boards/board-switcher.component.ts
      provides: "Board selector dropdown (sibling boards); emits boardChanged callback; ported from zb/ui or custom"
      min_lines: 80
    - path: src/app/pages/boards/board-detail.component.html
      provides: "Header + breadcrumb + mat-sidenav (end-drawer config panel) + board-switcher + zb-remote-table + menu"
      min_lines: 40
    - path: src/app/pages/boards/boards.routes.ts
      provides: "Route config: `/boards/:boardId` → board-detail component"
      min_lines: 10
    - path: src/app/pages/boards/board-detail.component.spec.ts
      provides: "Unit + integration tests: board fetch, breadcrumb derivation, switcher nav, admin gating, tasks list"
      min_lines: 150
  key_links:
    - from: "board-detail route"
      to: "board fetch + breadcrumb chain"
      via: "await clientApi.platformClient.getBoardApi().get() → getProjectApi().get()"
      pattern: "getBoardApi.*get|getProjectApi.*get"
    - from: "board-detail template"
      to: "zb-remote-table"
      via: "[columns] array, [columnLabels] map, (rowClick) output, <ng-container matColumnDef> projected children"
      pattern: "zb-remote-table|matColumnDef|columnLabels"
    - from: "admin menu item"
      to: "getPrincipal().isAdmin gate"
      via: "@if (isAdmin())"
      pattern: "getPrincipal.*isAdmin|@if.*isAdmin"
    - from: "board-switcher"
      to: "board-detail"
      via: "(boardChanged) callback → router.navigate(['/boards', newBoardId])"
      pattern: "boardChanged|router.navigate"
    - from: "board API response"
      to: "Party UUID / ownerId tracking"
      via: "board.ownerId (or inferred from board.projectId chain) read-through for RDF scoping"
      pattern: "ownerId|partyId"

---

## ANGULAR 21 MODERNIZATION RULES — NON-NEGOTIABLE (pasted verbatim per Director governance)

```
ANGULAR 21 MODERNIZATION RULES — NON-NEGOTIABLE

Every new and touched file in this phase MUST follow these rules. Pre-commit hook is diff-based, --max-warnings=0, blocks commit on violation. Touch-it=fix-it on every modified file.

1. STANDALONE COMPONENTS ONLY. No NgModules.
2. SIGNAL-BASED I/O: input() / output() — NEVER @Input() / @Output().
3. DEPENDENCY INJECTION via inject() — NEVER constructor parameter injection.
4. SIGNALS for state: signal() / computed() / effect() — not BehaviorSubject for component-local state.
5. VIEW QUERIES: viewChild() / viewChildren() — NEVER @ViewChild() / @ViewChildren().
6. CHANGE DETECTION: ChangeDetectionStrategy.OnPush on every component.
7. TEMPLATE CONTROL FLOW: @if / @for / @switch — NEVER *ngIf / *ngFor / *ngSwitch.
8. TYPE SAFETY: no `: any`. Narrow types. No empty catch {} blocks (comment + log or rethrow).
9. NGX-LIBRARY REUSE: prefer zb-remote-table, zb-simple-panel, zb-button-label, zb-dialog, zb-resource-status, zb-chip-colors-directive, snake-to-spaces pipe, etc. BEFORE building custom.
10. THEME TOKENS: --zb-background, --zb-background-card, --zb-text, --mat-sys-* — NEVER hardcoded colors. See BACKLOG-103 for what hardcoded colors cost.
11. FILE NAMING: keep type suffixes (`foo.component.ts`, `foo.service.ts`) — project convention overrides Angular 21's drop-the-suffix default.
12. SHARED-COMPONENT DEPENDENCY RULE: any component that ends up in `src/app/shared/components/` MUST be free of SME-Mart-specific services (engagements.service, sme-mart-project.service, etc.) so it can be reused on Project > Boards + hoisted to ngx-library later.

INVOKE the `sme-mart-architect` skill (and/or `/angular-architect`) at plan-phase for any decomposition question on shared components, signal-input shape, ngx-library reuse, or change-detection patterns. Don't guess.

Reference: .planning/docs/MODERNIZATION_GUIDE.md (canonical rules + troubleshooting). Eslint config at eslint.config.js encodes the rules; pre-commit hook at ../../../.husky/pre-commit + .lintstagedrc.json enforces them.
```

---

<objective>
Deliver the board-detail page: a top-level route (`/boards/:boardId`) that displays a single board with header, breadcrumb (derived from project hierarchy), tasks list (via `zb-remote-table`), board switcher (sibling board selector), and config panel (mat-sidenav end-drawer). The page supports admin-only "Open in ZB Platform" link and read-only task display (task CRUD is out of scope for Foundation). **The cog config panel SURFACE (open/close button, keyboard dismissal, visual panel wrapper) is delivered in Foundation per D-Q9; the internal form content (config menu items, field wiring, any setting controls) is deferred to Phase 33.** Party UUID (ownerId) is read from API for Holon projection scoping in Phase 34+ (C-4).

Purpose: Enable deep-link access to individual boards, breadcrumb-driven navigation through the engagement/project hierarchy, and side-by-side board switching without losing context. Establishes the top-level routing pattern for Phase 33+ features. Party UUID (ownerId) is read from API for Holon projection scoping in Phase 34+ (C-4).

Output: board-detail component with breadcrumb derivation, board switcher widget, zb-remote-table tasks list, config panel scaffold, and full test coverage. Routes file updated with `/boards/:boardId` handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-boards-engagement-project-cross-org/32-CONTEXT.md (L-2, L-7, L-8, L-9, D-Q8, D-Q9, L-13, C-4)
@.planning/phases/32-boards-engagement-project-cross-org/32-RESEARCH.md (board-detail breadcrumb pattern, lines 259-286; board switcher wiring, lines 292-304)
@.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (board-detail pattern, lines 228-348)
@.planning/notes/zb-ui-tasks-list-reference.md (column shapes + row actions; zb-remote-table API: matColumnDef projection pattern, lines 75-103)
@.planning/notes/board-switcher-reference.md (zb/ui board-context-selector-mvp shape, dependencies, callback)
@.planning/docs/RDF-COMPASS.md (C-4 check: Party UUID scoping for Holon projection)
@src/app/pages/org/tabs/vendor-profile-form.component.ts (mat-sidenav end-drawer pattern, lines visible)
@src/app/pages/project/tabs/project-boards-tab.component.ts (board fetch API reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create board-detail component with header, breadcrumb, and layout</name>
  <files>
    src/app/pages/boards/board-detail.component.ts
    src/app/pages/boards/board-detail.component.html
    src/app/pages/boards/board-detail.component.scss
  </files>
  <read_first>
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (board-detail pattern, lines 228-348; breadcrumb derivation, sidenav wiring)
    @src/app/pages/org/tabs/vendor-profile-form.component.ts (mat-sidenav end-drawer pattern; scaffold for cog config panel)
    @.planning/phases/32-boards-engagement-project-cross-org/32-RESEARCH.md (breadcrumb code example lines 259-286)
  </read_first>
  <action>
Create standalone component `BoardDetailComponent` that handles the `/boards/:boardId` route, fetches board + breadcrumb, and lays out the page structure.

**Component structure:**

Selector: `app-board-detail`
Standalone: true
Imports: Material modules (Toolbar, Sidenav, Icon, Button, Menu), Angular framework
Change detection: `ChangeDetectionStrategy.OnPush`

**Signals:**
- `board` = signal<Board | null>(null)
- `breadcrumb` = signal<BreadcrumbItem[]>([]) (items: { label, url })
- `tasks` = signal<Task[]>([])
- `loading` = signal<boolean>(true)
- `configPanelOpen` = signal<boolean>(false)
- `boardScope` = computed(() => board()?.projectId ? { type: 'project', id: board().projectId } : null)
- `partyUUID` = signal<string | null>(null) (read from board.ownerId or project chain for C-4 RDF scoping)

**Services injected:**
- `ActivatedRoute` — boardId from params
- `Router` — navigation
- `ZerobiasClientApi` — board + breadcrumb + tasks fetch

**ngOnInit sequence:**

1. Extract boardId from route.snapshot.params['boardId']
2. Fetch board:
   ```
   const board = await this.clientApi.platformClient
     .getBoardApi()
     .get(boardId);
   this.board.set(board);
   this.partyUUID.set(board.ownerId || null);  // C-4: track ownerId for RDF scoping
   ```
3. Derive breadcrumb:
   ```
   const project = await this.clientApi.platformClient
     .getProjectApi()
     .get(board.projectId);
   
   const engagement = project.parentId
     ? await this.clientApi.platformClient.getProjectApi().get(project.parentId)
     : null;
   
   this.breadcrumb.set([
     { label: engagement?.name || 'Engagement', url: `/engagements/${engagement?.id}` },
     { label: project.name, url: `/projects/${project.id}` },
     { label: board.name, url: null }, // current page
   ]);
   ```
4. Fetch tasks:
   ```
   const tasksResult = await this.clientApi.platformClient
     .getBoardApi()
     .listTasks(boardId, 1, 25);
   this.tasks.set(tasksResult.items ?? []);
   ```
5. Set loading=false in finally block

**Event handlers:**

`onBoardSelected(newBoardId: string):`
- Full URL nav: `this.router.navigate(['/boards', newBoardId]);`

`onTaskClick(task: Task):`
- Stub for now (task-detail page deferred to Phase 33)
- Log task ID; optionally navigate to `/tasks/:taskId` if route exists

`openInZbPlatform():`
- Open new window: `window.open('https://app.zerobias.com/app/boards/' + board().id, '_blank');`

`toggleConfigPanel():`
- `this.configPanelOpen.update(v => !v);`
- **Important:** This method opens/closes the config panel SURFACE ONLY (the mat-sidenav container, cog icon button, and empty drawer wrapper). The internal form content (setting controls, config menu items, field bindings) is deferred to Phase 33 per D-Q9. The drawer is keyboard-dismissible (mat-sidenav handles ESC key automatically).

`goBack():`
- Router back or navigate to parent engagement

**Template structure:**
- `<mat-toolbar>` header with back button + breadcrumb nav
- `<mat-sidenav-container>`
  - End-position `<mat-sidenav #configPanel [opened]="configPanelOpen()">`: config form scaffold (empty for Foundation; Phase 33 wires internal content). Must be keyboard-dismissible (mat-sidenav handles ESC key automatically).
  - `<mat-sidenav-content>`:
    - Board switcher widget (Plan 03 Task 2)
    - `<zb-remote-table>` tasks list (Plan 03 Task 3)
    - Triple-dot menu with "Open in ZB Platform" link gated by `getPrincipal().isAdmin` (Task 1 completion)

**Error handling:**
- Try/catch around all API calls
- Set error signal; render error message
- loading.set(false) in finally block

**Per modernization rules:**
- Rule 1: Standalone
- Rule 2: Signal-based (input from route, state via signal())
- Rule 3: inject() for DI
- Rule 4: Signals for state
- Rule 6: OnPush
- Rule 7: @if / @else syntax
- Rule 8: Type safety (no `: any`)
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit && npm test -- --include='**/board-detail.component.spec.ts' --watch=false | head -30</automated>
  </verify>
  <done>
    Component created, type-checked, and tested. Tests verify: (1) Board fetch on ngOnInit with correct boardId, (2) Breadcrumb chain derivation (board → project → engagement), (3) Tasks fetch, (4) Layout renders with correct structure (toolbar, sidenav, content), (5) Admin gating on menu item (if isAdmin), (6) Config panel SURFACE opens/closes and is keyboard-dismissible (content deferred to Phase 33), (7) Error/loading states, (8) partyUUID (ownerId) read and stored for C-4 RDF scoping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create board-switcher component (sibling board selector)</name>
  <files>
    src/app/pages/boards/board-switcher.component.ts
    src/app/pages/boards/board-switcher.component.html
    src/app/pages/boards/board-switcher.component.scss
  </files>
  <read_first>
    @.planning/notes/board-switcher-reference.md (zb/ui board-context-selector-mvp @ 77444a82d; input/output shape, dependencies, D-Q8 callback pattern)
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (board switcher L-13, D-Q8 callback + full-URL nav, lines 292-304)
  </read_first>
  <action>
Create or vendor `BoardSwitcherComponent` that displays sibling boards in a dropdown/autocomplete, emits selection callback, and allows parent to handle navigation.

**Option A (if zb/ui branch not yet merged):** Vendor a pinned copy of zb/ui `feat/board-context-selector-mvp @ 77444a82d` to `src/app/pages/boards/board-switcher.component.*` (copy TS, HTML, SCSS; update imports to remove zb/ui namespace).

**Option B (if zb/ui merged or unavailable):** Create minimal standalone component:

Selector: `app-board-switcher`
Standalone: true
Imports: Material (Dropdown, Icon, Button), Angular
Change detection: OnPush

**Input signals:**
- `scope` (optional): { type: 'project' | 'engagement', id: string } — used to fetch sibling boards
- `selectedBoardId` (optional): string — highlights active board

**Output signals:**
- `boardChanged`: emits string (new boardId)

**Logic:**
1. Accept scope input (project/engagement ID)
2. Fetch sibling boards via `getBoardApi().list(..., projectId=scope.id)`
3. Render dropdown with board names
4. Highlight currently selected boardId
5. On selection: emit boardChanged(newBoardId)
6. Parent handles full-URL navigation

**Template:**
```html
@if (boards().length > 1) {
  <div class="board-switcher">
    <button mat-button [matMenuTriggerFor]="boardMenu">
      {{ currentBoardName() }}
      <mat-icon>expand_more</mat-icon>
    </button>
    <mat-menu #boardMenu="matMenu">
      @for (board of boards(); track board.id) {
        <button mat-menu-item
                [attr.aria-current]="board.id === selectedBoardId() ? 'page' : null"
                (click)="onBoardSelect(board.id)">
          {{ board.name }}
        </button>
      }
    </mat-menu>
  </div>
}
```

**Accessibility (CF-2):**
- `[aria-current]="selectedBoardId() === id ? 'page' : null"` on active board item
- Button labels clear (`mat-button` shows board name)
- Keyboard-accessible menu (Material handles Tab/Enter)

**Per modernization rules:**
- Rule 1: Standalone
- Rule 2: Signal-based I/O
- Rule 6: OnPush
- Rule 7: @if syntax
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit && npm test -- --include='**/board-switcher.component.spec.ts' --watch=false 2>&1 | tail -5</automated>
  </verify>
  <done>
    Component created. Tests verify: (1) Boards fetched for scope (project), (2) Current board highlighted, (3) Selection emits boardChanged with correct ID, (4) Accessibility attributes present (aria-current), (5) Menu opens/closes, (6) Single board: switcher hidden.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire zb-remote-table for board-detail tasks list (read-only, mirroring zb-ui)</name>
  <files>
    src/app/pages/boards/board-detail.component.ts (modifications: add column config + row-click handler)
    src/app/pages/boards/board-detail.component.html (modifications: add zb-remote-table template section with matColumnDef projections)
  </files>
  <read_first>
    @.planning/notes/zb-ui-tasks-list-reference.md (column shapes + row actions + zb-remote-table API, lines 14-103; zb-remote-table uses matColumnDef projection pattern, NOT config-driven columns; lines 75-103 show exact wiring)
    @.planning/phases/32-boards-engagement-project-cross-org/32-RESEARCH.md (zb-remote-table wiring reference)
    ~/Projects/zb/zerobias-org/ngx-library/projects/ngx-library/src/lib/components/zb-remote-table/zb-remote-table.component.ts (API: @Input columns: string[], @Input columnLabels, @Input data, @Output rowClick; column definitions are projected as <ng-container matColumnDef> children)
  </read_first>
  <action>
Add `zb-remote-table` wiring to board-detail component to display tasks in a column-based, sortable, read-only table.

**Key insight:** `zb-remote-table` uses Angular Material's table architecture. Columns are configured via:
1. `[columns]` input: array of column key strings (e.g., ['priority', 'activities', 'name', 'status', 'assigned'])
2. `[columnLabels]` input: map of column key → i18n translation key (e.g., { priority: 'Generic.Priority', name: 'Generic.Name', ... })
3. `[data]` input: array of task objects
4. Column definitions: `<ng-container matColumnDef="columnKey">` projected as children, defining header + cell templates per Material table API
5. `(rowClick)` output: emits task object on row click

**Component modifications (board-detail.ts):**

Add signals/computed:
```typescript
readonly displayColumns = ['priority', 'activities', 'name', 'status', 'assigned'];
readonly columnLabels = {
  priority: 'Generic.Priority',
  activities: 'Generic.Code',
  name: 'Generic.Name',
  status: 'Generic.Status',
  assigned: 'Generic.Assigned',
};
```

Add handler:
```typescript
onTaskClick(task: Task): void {
  // Stub: Phase 33 will navigate to task-detail or open modal
  console.log('Task clicked:', task.id);
}
```

**Template modifications (board-detail.html):**

Replace tasks-list stub with:
```html
<div class="tasks-section">
  <h2>Tasks</h2>
  @if (loading()) {
    <mat-spinner diameter="32" />
  } @else if (tasks().length === 0) {
    <div class="empty-state">No tasks in this board</div>
  } @else {
    <zb-remote-table
      [columns]="displayColumns"
      [columnLabels]="columnLabels"
      [data]="tasks()"
      [loading]="loading()"
      [rowClickEnabled]="true"
      [selectableRows]="false"
      [sortDisabled]="false"
      (rowClick)="onTaskClick($event)"
    >
      <!-- Column definitions: projected as children of zb-remote-table -->
      <!-- Material table matColumnDef pattern: define once, zb-remote-table displays based on [columns] input -->

      <ng-container matColumnDef="priority">
        <th mat-header-cell *matHeaderCellDef>Priority</th>
        <td mat-cell *matCellDef="let task">
          <zb-severity-indicator [severity]="task.priority?.label" [showText]="false" />
        </td>
      </ng-container>
      
      <ng-container matColumnDef="activities">
        <th mat-header-cell *matHeaderCellDef>Activity</th>
        <td mat-cell *matCellDef="let task">
          <span class="zb-ui-chip square">{{ task.code }}</span>
        </td>
      </ng-container>
      
      <ng-container matColumnDef="name">
        <th mat-header-cell *matHeaderCellDef>Name</th>
        <td mat-cell *matCellDef="let task">
          {{ task.name }}
        </td>
      </ng-container>
      
      <ng-container matColumnDef="status">
        <th mat-header-cell *matHeaderCellDef>Status</th>
        <td mat-cell *matCellDef="let task">
          <span class="zb-ui-chip square" [ngClass]="'task-status-' + (task.status | lowercase)">
            {{ task.status | snakeToSpaces }}
          </span>
        </td>
      </ng-container>
      
      <ng-container matColumnDef="assigned">
        <th mat-header-cell *matHeaderCellDef>Assigned</th>
        <td mat-cell *matCellDef="let task">
          <zb-avatar
            class="dense small"
            [label]="task.assigned?.contactName"
            [imageUrl]="task.assigned?.imageUrl"
            [useInitials]="!task.assigned?.imageUrl"
            [labelOn]="true"
          />
        </td>
      </ng-container>
    </zb-remote-table>
  }
</div>
```

**Per zb-ui reference (L-7, L-8):**
- Column order: priority, activities, name, status, assigned (matching zb-ui tasks-list default)
- Priority: zb-severity-indicator component (mapped from task.priority.label)
- Activities: task code in square chip (zb-ui-chip class)
- Name: plain text
- Status: snakeToSpaces pipe + status-class binding (task-status-{status|lowercase}; chip styling colors per reference)
- Assigned: zb-avatar component with initials fallback (if no imageUrl)
- Row click: read-only, emits rowClick event (no inline edit)
- No triple-dot row menu (actions in side drawer Phase 33)

**Accessibility (CF-2):**
- Table headers properly marked with `<th mat-header-cell>` (Material handles ARIA)
- ARIA labels on icons (zb-severity-indicator has built-in a11y; matTooltip or aria-label fallback if needed)
- Row click: cursor pointer; keyboard accessible (Material table handles Tab/Enter)

**Error handling:**
- Table handles loading state via [loading] input
- Empty state rendered via @if tasks().length === 0

**Note on Material table directives:** `<ng-container matColumnDef>`, `<th *matHeaderCellDef>`, `<td *matCellDef>` are legitimate Angular Material APIs. They do NOT violate modernization rule 7 (which bans *ngIf / *ngFor / *ngSwitch). Material's *matCellDef is a structural directive for table-specific use and is the canonical pattern. All 5 column definitions use matColumnDef projections correctly per Material table architecture.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit && npm test -- --include='**/board-detail.component.spec.ts' --watch=false | grep -E "TOTAL|passing"</automated>
  </verify>
  <done>
    zb-remote-table wired into board-detail. Tests verify: (1) Column config matches zb-ui reference (priority, activities, name, status, assigned), (2) Task data populates table, (3) Row click emits taskClick event, (4) Empty state renders when no tasks, (5) Loading state shows spinner, (6) Table renders read-only (no inline edit), (7) Accessibility attributes present (headers via Material, severity/avatar components handle labels), (8) matColumnDef projections correctly define all 5 columns per Material table pattern.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create boards.routes.ts and register `/boards/:boardId` route</name>
  <files>
    src/app/pages/boards/boards.routes.ts
    src/app/app.routes.ts (modification: import + add board-detail route)
  </files>
  <read_first>
    @src/app/app.routes.ts (route file structure; find where other top-level pages are registered)
    @src/app/pages/engagements/engagements.routes.ts (example route file pattern; children routes structure)
  </read_first>
  <action>
Create route configuration for the boards feature and register it in the app root routes.

**boards.routes.ts:**
```typescript
import { Routes } from '@angular/router';
import { BoardDetailComponent } from './board-detail.component';

export const BOARDS_ROUTES: Routes = [
  {
    path: ':boardId',
    component: BoardDetailComponent,
    data: { title: 'Board' },
  },
];
```

**app.routes.ts modification:**
Find the root routes array and add:
```typescript
{
  path: 'boards',
  loadChildren: () => import('./pages/boards/boards.routes').then(m => m.BOARDS_ROUTES),
},
```

This registers the feature route so `/boards/abc-123` resolves to board-detail component.

Per modernization rules: Use lazy-loaded route children (loadChildren) to keep root route file clean.
  </action>
  <verify>
    <automated>npx tsc -p tsconfig.spec.json --noEmit && grep -n "BOARDS_ROUTES\|path.*boards" src/app/pages/boards/boards.routes.ts src/app/app.routes.ts</automated>
  </verify>
  <done>
    Route file created and registered. Type-check passes. Route `/boards/:boardId` resolves to board-detail component.
  </done>
</task>

<task type="auto">
  <name>Task 5: Unit test suite for board-detail + board-switcher</name>
  <files>
    src/app/pages/boards/board-detail.component.spec.ts
    src/app/pages/boards/board-switcher.component.spec.ts
  </files>
  <read_first>
    @.planning/phases/32-boards-engagement-project-cross-org/32-PATTERNS.md (board-detail test patterns; API mocking, breadcrumb derivation)
    @src/app/shared/components/create-subtask-dialog/create-subtask-dialog.component.spec.ts (Material dialog + form testing)
  </read_first>
  <action>
Create comprehensive spec suite for board-detail and board-switcher.

**board-detail.component.spec.ts:**

Setup:
- Mock ZerobiasClientApi + getBoardApi + getProjectApi methods
- Mock ActivatedRoute.snapshot.params['boardId']
- Mock Router.navigate()
- Mock getPrincipal() for admin gating

Test cases:
1. Board fetch on ngOnInit (correct boardId)
2. Breadcrumb derivation (board → project → engagement chain)
3. Tasks fetch (correct boardId)
4. Admin gating: "Open in ZB Platform" link visible only if getPrincipal().isAdmin=true
5. Board switcher emits boardChanged → parent navigates
6. Task row click handled (stub for Phase 33)
7. Config panel SURFACE open/close and keyboard-dismissible (content deferred to Phase 33)
8. partyUUID (ownerId) read and stored (C-4 RDF scoping check)
9. Error/loading states
10. zb-remote-table wired with correct column config

Coverage: 100% for board-detail logic (breadcrumb, admin gating, event handling, ownerId tracking).

**board-switcher.component.spec.ts:**

Setup:
- Mock sibling boards array
- Set scope input
- Set selectedBoardId input

Test cases:
1. Boards fetched for scope (project)
2. Current board highlighted (aria-current)
3. Selection emits boardChanged with correct ID
4. Single board: switcher hidden
5. Multiple boards: dropdown visible
6. Keyboard accessible (Material menu)

Coverage: 100% for switcher logic (board fetch, selection, aria-current).

All tests: Use Jasmine patterns (expect, spyOn, etc.). Mock promises with Promise.resolve/reject.

Run command:
```bash
npm test -- --include='src/app/pages/boards/**/*.spec.ts' --watch=false --code-coverage
```
  </action>
  <verify>
    <automated>npm test -- --include='src/app/pages/boards/board-detail.component.spec.ts' 'src/app/pages/boards/board-switcher.component.spec.ts' --watch=false --code-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>
    Spec suites created and passing (100% coverage). Tests verify: (1) Board + breadcrumb + tasks fetch, (2) Admin gating on menu, (3) Board switcher emits selection, (4) zb-remote-table column config, (5) Error/loading states, (6) Accessibility attributes, (7) Config panel SURFACE opens/closes (content deferred), (8) partyUUID (ownerId) read and stored, (9) Route integration.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin gating on "Open in ZB Platform" link | `getPrincipal().isAdmin` is UI-only; server validates on ZB side |
| Board data from API | Trusted source; uses Material binding (not innerHTML) |
| Breadcrumb derivation from API chain | Each API response trusted; no user input in chain |
| partyUUID (ownerId) for RDF scoping | Read from API; scoping constraint for Phase 34+ (no validation in this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Spoofing | Admin "Open in ZB Platform" link | mitigate | Link UI-gated by getPrincipal().isAdmin; server-side ZB validates permission on click |
| T-32-08 | Injection | Breadcrumb URL construction | mitigate | URL built from API-sourced IDs (not user input); Router.navigate() uses route params |
| T-32-09 | Information Disclosure | Task-detail stub | mitigate | Stub page (Phase 33) logs task ID; no sensitive data exposed in console |
| T-32-10 | Information Disclosure | partyUUID tracking | accept | ownerId read from API for RDF projection; no new exposure (already in API response); scoping only (no privilege escalation) |

</threat_model>

<verification>
## Phase 32 Plan 03 — Board Detail Page + Switcher

**Wave 2 completion checklist:**
- [ ] board-detail component created with breadcrumb derivation + layout
- [ ] board-switcher component created (or vendored from zb/ui)
- [ ] zb-remote-table wired with column config matching zb-ui reference (matColumnDef projections)
- [ ] Admin "Open in ZB Platform" link gated by getPrincipal().isAdmin
- [ ] Config panel (mat-sidenav) SURFACE open/closeable; keyboard-dismissible; content deferred to Phase 33 per D-Q9
- [ ] partyUUID (ownerId) read and stored for C-4 RDF Compass check
- [ ] boards.routes.ts created; route registered in app.routes.ts
- [ ] All specs passing: `npm test -- --include='src/app/pages/boards/**/*.spec.ts' --watch=false` (exit code 0)
- [ ] ESLint passes: `npx eslint src/app/pages/boards/**/*.ts`

**Integration readiness:**
- [ ] Route `/boards/:boardId` resolves to board-detail component
- [ ] Breadcrumb links navigate correctly (engagement / project)
- [ ] Board switcher integrates with board-detail
- [ ] zb-remote-table displays task data (5 columns: priority, activities, name, status, assigned)
- [ ] Config panel (mat-sidenav) SURFACE opens/closes (content scaffold for Phase 33)
- [ ] ownerId available for Phase 34+ Holon projection wiring

</verification>

<success_criteria>
- All unit tests pass (100% coverage for board-detail + switcher logic)
- ESLint diff-based gate passes
- Board fetch + breadcrumb derivation working (verified via test)
- zb-remote-table column config matches zb-ui reference (priority, activities, name, status, assigned)
- matColumnDef projections correctly define all 5 columns per Material table API
- Board switcher emits boardChanged callback; parent navigates (full URL per D-Q8)
- Admin gating on "Open in ZB Platform" link working (visible only if isAdmin)
- Config panel (mat-sidenav end-drawer) SURFACE open/closeable and keyboard-dismissible; CONTENT deferred to Phase 33 per D-Q9
- Tasks list read-only (no creation/edit UI)
- Accessibility: breadcrumb links, table headers, aria-labels, aria-current on active board, Material table keyboard nav
- Error/loading/empty states render gracefully
- Route `/boards/:boardId` registered and resolved
- partyUUID (ownerId) read from API and available for RDF Compass C-4 validation in Phase 34+

</success_criteria>

<output>
After completion, create `.planning/phases/32-boards-engagement-project-cross-org/32-03-SUMMARY.md` with:
- Components created: board-detail, board-switcher
- Routes registered: `/boards/:boardId` → board-detail
- API patterns verified: board fetch, breadcrumb chain (project → engagement), board-switcher fetch
- zb-remote-table config: columns match zb-ui reference; matColumnDef pattern verified
- Test coverage: 100% for board-detail + switcher logic; partyUUID tracking validated
- Admin gating: "Open in ZB Platform" link visible only if isAdmin
- Config panel: SURFACE (open/close, keyboard dismissal) delivered; CONTENT deferred to Phase 33 per D-Q9
- C-4 RDF Compass: ownerId read and stored for Phase 34+ Holon projection
- Ready for Plan 04 (Create Board dialog) and Plan 05 (PinStorage + E2E)
</output>
