# SME Mart — Backlog

**Source of truth for FEATURE WORK pending milestone planning.** New feature ideas, meeting-driven stubs, and gap analysis items live here. When starting a new GSD milestone, pull items from this backlog into REQUIREMENTS.md.

**Flow:** Backlog item → `/gsd:new-milestone` → REQUIREMENTS.md → ROADMAP phases → plan → execute → archive

**Last updated:** 2026-04-29

## Two backlog systems — know the difference

| System | Where | For | Tooling |
|---|---|---|---|
| **GSD backlog** (this file) | `.planning/BACKLOG.md` — single-file table | Feature work awaiting milestone planning. Items become phases. | `/gsd:add-backlog`, `/gsd:review-backlog`, `/gsd:plan-milestone-gaps` |
| **Director backlog** | `.planning/director/backlog/{NNN}-{slug}.md` — per-item files | Architecture decisions, cross-milestone observations, patterns to revisit. May or may not become feature work. | `/meta:backlog`, reviewed in `/meta:director design` |

**Rule of thumb:** "this might become code" → here. "architectural decision / observation / pattern concern" → director backlog.

---

## How to Use

- **Add stubs:** Drop new ideas as a row in the appropriate section. Minimum: name, one-line description, source.
- **Select for milestone:** Move items from here into the next `/gsd:new-milestone` requirements conversation.
- **After milestone ships:** Remove completed items. Update status of partially-done items.
- **Deferred items:** Stay here with a note on what's blocking them.

---

## Active Marketplace Plans (Unblocked)

These can be selected for the next milestone. Ordered roughly by business value.

| # | Plan | Description | Est. | Source |
|---|------|-------------|------|--------|
| **104** | Phase 24 Plan 04 — Admin Delete-Demo UI | **Deferred from v1.4 2026-05-18.** Original Phase 24 Plan 04 scope: standalone `/admin/demo-data` route + `DemoDataComponent` with MatDialog confirmation + bulk delete via `Pipeline.deleteEntities()` for demo-tagged class-Objects + hydra Resource deletion + Phase-20 error handling (`await` + try/catch + MatSnackBar + explicit `callSiteTag` + re-throw). Skipped in v1.4 because the read-side filter (Plans 01-03) is already gating non-admin views; admin demo deletion is currently manageable via MCP / SQL. Brian's-Org orphan recovery (Phase 31-C) is the real "admin escape hatch" need this milestone, and that's already wired through the provisioner. **Re-promote** when an admin UI use case actually surfaces (e.g., demo-data churn on UAT becomes burdensome, or another phase needs the deletion plumbing). Files originally scoped: `src/app/features/admin/demo-data.component.{ts,html,scss}`, `demo-data.service.ts`, `app.routes.ts`. Plan brief at `.planning/phases/24-demo-data-visibility-gate/24-04-PLAN.md`. | 4-6 hrs | Phase 24 partial-close decision 2026-05-18 |
| **054** | RFP Package Builder & Access Controls | **v1.2 IN PROGRESS.** Phase 13 (pilot) complete. Phase 14 (invitations) code complete, UAT pending GQL schema. Phase 15 research done (template syntax design). D2 (document templates) = Phase 15. D3 (form builder) = Phase 16. Demo scripts = Phase 17. S2 deferred to v1.3. | 30–40 hrs | Gap analysis D1, D2, D3, S2; design session 2026-03-30 |
| **055** | Advanced Pricing & Evaluation | Complex pricing models: NRC/ARC, recurring/one-time, milestone payments, per-unit, multi-year (D4). Evaluation criteria builder with weighted scoring matrix (D5). Structured bid response templates (S3). Bid validity/expiration dates (S4). | 25–35 hrs | Gap analysis D4, D5, S3, S4 |
| **056** | Engagement Roles & Communication | Third-party facilitator role (D7). Mediated communication channels (D6). NDA/confidentiality tracking with per-vendor status, access logging, destruction attestation (P2). | 20–25 hrs | Gap analysis D6, D7, P2 |
| **065** | Message Center (Engagement-Scoped) | Engagement-level message center. Project-level messaging deferred to Project App. **Notes:** (1) Investigate [PromptQL](https://promptql.io) (Hasura) for pattern inspiration — thread-per-resource model, shared context, inline data refs, AI agent threads. 2026-04-01. (2) Slack API connector concept: create dedicated Slack channels per Project/Engagement, slurp conversations into SME Mart messaging center. Slack has full dev API (`api.slack.com`) — channel creation, webhooks, Slack Connect for cross-org. Could replace custom messaging UI entirely. 2026-04-02. | 8–12 hrs | Marketplace scope |
| **066** | Engagement Dashboard | Configurable widgets for cross-project visibility at the engagement level. | 12–16 hrs | Marketplace scope |
| **033 P5** | LLM-Assisted Bid Generation | Claude Agent SDK packaging, bundled ZB MCP tools, cost model (~$0.56/interaction). Phases 1–4 of Plan 033 complete. | ~8 hrs | Plan 033 |
| **046 remaining** | Org Document Management (remaining phases) | Phase 6: org switcher (deferred). Phase 8: external storage imports (deferred). Phase 9 roadmap: folders, colors, tagging UI, archive browser, versioning, PDF conversion, preview, bulk ops, templates. Expand with 19 new document types from gap analysis (E1). **Note:** Phase 15 (v1.2) cherry-picks template→instance workflow from 046. | ~20 hrs | Gap analysis E1 |
| **047** | Shared Notes & Versioning | Per-user sharing, note versioning (version browser/search/copy), Shared Notebook, timeline integration (share/pin events), pinned notes, task-linked checkboxes. 8 phases. | 32–40 hrs | Plan 047 |
| **052** | Playwright E2E Smoke Tests | **✅ DONE 2026-04-09** (Phases 1-3). 7 specs, 15/15 tests passing. Phase 4 (CI integration) deferred. See Completed Plans section. |  | Meeting 2026-03-13 |
| **053** | QA Skills & Cookie Import | Generic Claude Code skills (`/qa`, `/setup-cookies`) using Chrome DevTools MCP. Cookie decryption, systematic QA crawl with health scoring, regression tracking. | 16–21 hrs | Plan 053 |
| **052 P4** | Playwright E2E — CI Integration | Phase 4 of Plan 052 (deferred). GitHub Actions workflow, JUnit reporter, separate tsconfig. Add after smoke suite stable locally for 1 week. | 2–3 hrs | Plan 052 |
| **082** | Add `data-testid` attributes to key landing elements | Improve E2E test stability. No `data-testid` exists anywhere in the app — tests rely on role + text + CSS classes (brittle). Add to list headings, card containers, tab links, primary CTAs. | 3–4 hrs | Plan 052 execution finding 2026-04-09 |
| **083** | Update stale `.claude/smoke-tests/rfp-wizard-create.md` | The `/rfps/new` flow now shows an AI-extract vs step-by-step method chooser BEFORE the 5-step mat-stepper. Smoke test doc references the old flow. Also: `app-list-page` renders title as `<h2>` not `<h1>`; Angular template `{{ tab.label }}` whitespace issue. | 1 hr | Plan 052 execution finding 2026-04-09 |
| **084** | App-wide i18n migration (ngx-translate) | Migrate all hardcoded English strings to `@ngx-translate/core` keys. **Scaffolding exists** — library is installed, `TranslateService` wired up, `en.json` exists but empty. **Conventions locked** — see `.planning/docs/I18N_CONVENTIONS.md` (PascalCase, `Sm.*` namespace, ICU MessageFormat, suffix conventions, promote-on-reuse for shared components, CSS-handled uppercase). **zb/ui reference** — `projects/zb-ui-lib/src/lib/assets/i18n/en.json` is a cautionary tale (41% duplication, 38 casing inconsistencies) — analysis in `.planning/notes/i18n-zb-ui-analysis.md`. **Plan 078 pilots** the conventions. Plan 084 is the app-wide migration once pilot proves out. Includes lint script implementation (`scripts/i18n/lint-i18n.ts`). | 30-50 hrs migration + 4-6 hrs lint tooling | Plan 078 prep 2026-04-09 |
| **085** | Prefer ngx-library form components; per-component helpers for bespoke ones | **Direction, not a sprint.** When touching a form component, prefer `zb-simple-autocomplete` / `zb-simple-multi-autocomplete` (gain Playwright helpers for free, less custom Material wiring). When the UX needs more than ngx-library provides (free-text creation, protected chips, chip colors, immediate persist, collapsible/removable section wrappers, "show all as chips" mode, `Set<string>` selection shape), keep the custom component AND write per-component E2E helpers in `e2e/helpers/<component>.ts` — don't migrate just for testability. **Three known custom components** stay as-is until a feature naturally redesigns them: `app-catalog-filter-section` (RFP/services filter drawer), `app-resource-tag-autocomplete`, `app-sme-resource-tag-editor`. **None implement `ControlValueAccessor`** — Layer 1 race may not apply (verify with a spike before assuming a helper is needed). **Also watch for upstream contribution opportunities:** if we build a reusable pattern (filter drawer wrapper, chips+autocomplete+inline-create, protected-chip predicate), consider proposing it to `@zerobias-org/ngx-library` rather than keeping it SME-Mart-only. The `selectValue()` PRs (#5, #6) are the model: identify pattern → draft for ngx-library → ship upstream → consume back. Full decision process documented in `.planning/notes/e2e-testing-guide.md` § "When you hit a custom (non-zb) Material autocomplete". | Ongoing — bake into daily decisions, no dedicated migration sprint | E2E analysis 2026-04-09 |
| **078** | Transparency Controls UI Spec | Design spec for "Transparency Controls" section on each party's tasks (Plan 071). Controls that publish/subscribe to transparency bridge. **IN PROGRESS:** (1) HTML UI concept sketches (`.claude/sketches/transparency-center-ui-concepts.html`) — 5 views. (2) Screenshots uploaded to Clark's Miro board. (3) UI-SPEC draft-prep written (`.claude/ui-specs/078-transparency-controls-UI-SPEC.md`) — GSD format, target view integration mapping, component inventory, copywriting contract, 8 open questions for wireframe session. Kevin clarified: TC is a FUNCTION (not a place). Next: Clark's red-box markup pass on Engagement/Project screens → low-fi wireframes. | 4–6 hrs | Brian 2026-03-27 |
| **076** | Spike: Ollama Local LLM Integration | Evaluate Ollama for cost-reduction + capability-expansion. POC: auto-tag suggestion, embedding-based semantic search (pgvector), CVE summarization. Hybrid architecture. | 8–12 hrs POC | Research |
| **081** | UI Screenshot Catalog + Miro Gap Analysis | **(1)** Snapshot Brian's Miro diagram as baseline. **(2)** Capture screenshots of all built views/flows via Chrome DevTools MCP (RFP lifecycle, bids, invitations, vendor profile, org nav, pilots, project detail). **(3)** Overlay screenshots onto Miro where they map to Brian's diagram nodes. **(4)** Identify diagram nodes with NO corresponding UI — these become specs for views we haven't built yet. **(5)** Annotate gaps with proposed UI direction. Output: annotated Miro board showing built vs unbuilt, plus a gap list feeding future backlog items. | 4–6 hrs | Brian feedback 2026-04-06 |
| **087** | Form Template Library (save → reuse → fork-on-edit) | **From Phase 16 UAT feedback 2026-04-14.** Buyers build a form once and reuse it across RFPs. Every saved form becomes a library entry; selecting a template in the wizard pre-fills fields; editing a template forks a new version rather than mutating the original. **Auto-save on create** — starting a new form immediately creates a draft template so the user can leave and come back to edit it later, no explicit "save" step needed. **Scope:** (1) Schema — new `FormTemplate` class (`w3geekery/smemart/classes/FormTemplate.yml`) with `name`, `description`, `config` (JSON FormBuilderConfig), `ownerId`, `parentTemplateId` (fork lineage), `usageCount`, `status` (draft/published/archived). (2) Service — `form-template.service.ts` with CRUD + fork + list-by-org + auto-save draft on first field add. (3) UI in RFP wizard Step 2.5 — "Pick from library" button + template picker dialog + (implicit) auto-draft on first edit. (4) Library page — `/forms/templates` — list/search/fork/archive + drafts pinned at top. (5) Edit-detect — if user loads a published template then modifies, prompt "Save as new" vs "Overwrite" (owner-only + blocked if other RFPs reference this version). (6) **Org Documents Center integration** — from the existing Org Documents center (Plan 046 area), a link/section surfaces recent form templates; row click opens the template in the form builder for editing. Could be its own "Forms" section or embedded as a short list with "View all" → `/forms/templates`. **Dependencies:** builds on Phase 16 FormBuilderConfig model; coordinates with Plan 046 Org Documents surface. **Prompt for `/gsd:plan-phase`:** *"Plan a Form Template Library for SME Mart. Users save completed forms from the RFP wizard as reusable templates in their org's library, reuse them on future RFPs, and fork a new template when they edit one. Auto-save behavior: creating a new form immediately creates a draft template record so the user can leave and come back — no explicit 'save' button needed. Cover: (a) schema changes — new FormTemplate class with fork lineage, status (draft/published/archived), usage count, parentTemplateId; (b) service layer — form-template.service.ts with save/list/fork/archive/usage tracking + auto-draft on first field add (debounced autosave thereafter); (c) wizard UI — template picker in RFP Step 2.5 (Submission Form step), implicit autosave (no Save button while editing a fresh form), dirty-edit detection on published templates that asks Save-as-New vs Overwrite; (d) library page at /forms/templates with list/search/filter/fork/archive, drafts pinned at top with 'continue editing' affordance; (e) Org Documents Center integration — existing Org Documents view (Plan 046 area) gets a link/section showing recent form templates, row click opens that template in the form builder for editing; (f) access control — org-scoped, owner-only delete, fork preserves lineage pointer. Reference existing FormBuilderConfig (src/app/core/models/form-builder.model.ts), Phase 16 form-builder component, Plan 046 Org Documents center, and the template→instance pattern from Plan 015 (document templates) for variable-substitution parallels. Out of scope: marketplace/public template sharing, AI-generated templates."* | 22–32 hrs | Phase 16 UAT feedback 2026-04-14 |
| **088** | Split-screen Form Builder + WYSIWYG Canvas + Grouping + Info Field Type | **From Phase 16 UAT feedback 2026-04-14. Extended 2026-04-15 w/ WYSIWYG + grouping + Angular-native output.** Replace the current "expand to edit one field at a time" form builder UX with a two-pane layout: left = live rendered preview of the field being edited (or whole form when none selected), right = field properties editor. Preview tab becomes a cleaner version of the left pane — full form rendered with proper spacing, read-only. **Also adds Option B** from the same UAT conversation: new `'info'` field type for heading + description text blocks that aren't inputs (not stored in submissionData, no validation). Drops anywhere between fields as explainer content. **Scope:** (1) Redesign `form-builder.component` as two-pane grid (mat-sidenav or CSS grid). (2) Selection model — clicking a field row highlights it + loads editor in right pane. (3) Live render — left pane uses `form-field-renderer` in new `edit-preview` mode (interactive, not disabled, but no persistence). (4) Add `'info'` to `FormFieldType` enum; `form-field-editor` shows only label + description (markdown-capable) for info fields; `dynamic-form-renderer` renders them as styled callout blocks; excluded from form-group controls + submissionData. (5) Preview tab uses existing `dynamic-form-renderer` with `mode='preview'` but with builder chrome stripped (just spaced form). (6) **"Add Field" split-button** — primary button label "Add Field" inserts a field of the most-recently-used type (or default); caret opens menu of all field types (text, textarea, dropdown, number, file, checkbox, info) and selecting one inserts a field of that type pre-configured. Saves two clicks per field when the user knows what type they want. **Dependencies:** Phase 16 components exist.

**NEW (2026-04-15) — WYSIWYG canvas + grouping + Angular-native rendering:**
- **Research spike:** evaluate **HTML `<canvas>` (or SVG) drag-and-drop WYSIWYG** for field placement as an alternative to the stacked-panel + two-pane approach. Canvas enables true visual field arrangement (drag anywhere, snap-to-grid, live preview). Consider CDK drag-drop with a DOM-based canvas before committing to `<canvas>` — DOM-based may give us more for free (accessibility, text rendering, focus). Decide DOM vs. canvas based on complexity tradeoff in the spike.
- **Grouping / sub-grouping:** fields can be organized into **vertical groups, horizontal groups, and nested sub-groups**. Each group supports optional heading + description. Groups become first-class in the structured JSON config (not flat fields).
- **Rendered output contract (FIRM):** the form renderer outputs real **Angular `FormGroup` / `FormControl`** — not a custom validation shell. Groups in config map 1:1 to nested `FormGroup` instances; fields map to `FormControl` with Angular validators wired from the field's validation config (`Validators.required`, `Validators.minLength`, custom validators for regex/etc.). This preserves Angular forms semantics end-to-end — reactive forms, dirty/touched/valid states, status change streams — no custom mirror.
- **Storage:** forms stored as structured JSON (existing `FormBuilderConfig` model, extended for groups). JSON schema must round-trip through save/load/render without loss.
- **Field + group types extend Angular primitives:** custom field components wrap standard Material/ngx-library inputs but the underlying state is always a `FormControl`. Group components wrap Material layout (`mat-card` / fieldset) but the underlying state is always a `FormGroup`.

**RESEARCH COMPLETE (2026-04-16)** — 7-axis research doc at [`.planning/research/internal/2026-04-15-form-builder-refactor-research.md`](.planning/research/internal/2026-04-15-form-builder-refactor-research.md). Headline findings: (a) **DOM + Angular CDK drag-drop over HTML canvas** — canvas's accessibility/text/reuse costs outweigh the pixel-placement benefit, and arbitrary placement is actually an anti-feature for form builders; CDK covers reorder, nested drop zones, connected lists, horizontal orientation, keyboard drag, palette-to-canvas copy. (b) **Schema migration v1→v2** — discriminated union `FormNodeConfig = FormFieldConfig | FormGroupConfig` with recursive `children: FormNodeConfig[]`; existing `FormSubmission.submissionData` untouched via preserved field IDs; silent migrator at load time. (c) **Build custom** vs ngx-formly/formio/SurveyJS — each incumbent fights the "renderer outputs real FormGroup" contract. Stays Angular-native and fits the 20–26 hr estimate. (d) **6 open questions for Discuss phase:** conditional fields scope, cross-field validators, undo granularity, perf ceiling, migration trigger, column-snap UX. (e) **5 sequencing recommendations** for the planner — schema migration first, group renderer before editor, keep `form-field-renderer` untouched, etc. Research is sufficient to skip the planner's research phase and go straight to Discuss on v1.4 kickoff.

**Prompt for `/gsd:plan-phase`:** *"Plan a split-screen Form Builder redesign for SME Mart Phase 16 follow-up, plus an 'info' (heading + description, no input) field type AND a WYSIWYG canvas-style builder with hierarchical grouping. Currently the form-builder shows a stacked list of expansion panels — one expanded at a time — for field editing. Redesign the EDITING side as two panes: left = WYSIWYG canvas showing live rendered fields that can be dragged into position and grouped, right = properties editor for the currently-selected field or group. Preview tab becomes a clean full-form render with no builder chrome. Also add FormFieldType 'info' for non-input explainer blocks with heading + optional markdown description, excluded from submissionData and validation. **Research spike first:** evaluate HTML canvas vs DOM-based CDK drag-drop for the WYSIWYG surface. Pick the simpler approach that supports: (a) drag fields onto the canvas from a palette, (b) rearrange existing fields by drag, (c) group selected fields into vertical or horizontal groups with optional heading + description, (d) nested sub-groups, (e) snap-to-row/column layout feedback. **Storage + rendering contract:** forms stored as structured JSON extending FormBuilderConfig to include groups. The renderer outputs real Angular reactive forms — groups become nested FormGroup instances, fields become FormControl with Angular validators wired from the field's validation config (Validators.required, Validators.minLength, custom regex). No custom validation shell — preserve Angular forms semantics end-to-end so consumers get FormGroup.valid, statusChanges, valueChanges, dirty/touched for free. Cover: (a) form-builder.component.{ts,html,scss} two-pane layout + canvas host, selection model for fields AND groups, responsive fallback; (b) group component (vertical / horizontal / nested) with optional heading + markdown description, wraps a child FormGroup; (c) field registry mapping FormFieldType → component + default Validator set; (d) form-field-renderer — new 'edit-preview' mode interactive but non-persisting, plus 'info' field rendering as styled callout; (e) form-field-editor + new group-editor — detect 'info' and group types to show appropriate property panels; (f) dynamic-form-renderer — build nested FormGroup tree from config, skip info fields in control creation and submissionData, render groups as layout wrappers; (g) preview tab simplification — reuse dynamic-form-renderer with mode='preview'. Also replace the current single 'Add Field' button with a split-button: primary click inserts a field using the most-recently-used type (default text on first use), caret opens a menu of all field types (text, textarea, dropdown, number, file, checkbox, info) plus 'Add Vertical Group' and 'Add Horizontal Group'. Reference existing Phase 16 components at src/app/shared/components/form-builder/, FormBuilderConfig model at src/app/core/models/form-builder.model.ts, Angular Reactive Forms docs, and Angular CDK drag-drop. Out of scope: collaborative editing, template library (Plan 087), form logic/conditional-display (future)."* | 20–26 hrs | Phase 16 UAT feedback 2026-04-14 |
| **086** | zbb local stack — full prod simulation for SME Mart | **PARTIALLY PROMOTED to Phase 19 (v1.3).** Phase 19 covers cloudfront-sim + minio SPA/login serving. Hub-server + Verdaccio + postgres deferred to **089**. Original scope: compose a `zbb` slot mirroring production SME Mart deployment. Kevin confirmed feasibility 2026-04-13. | ~3-4 hrs (SPA/login serving only, via Phase 19) | Slack discussion w/ Kevin 2026-04-13 |
| **090** | Seller Credentials Catalog (roles + certifications) | **Research complete 2026-04-23. Target v1.4 or v1.5.** Brian directive: Sellers multi-select compliance/cybersecurity credentials on their profile (CMMC CCA, CISSP, FedRAMP 3PAO, etc.); Buyers filter/search Sellers by credentials. Kevin (Content) confirmed 2026-04-23 Content team will NOT own this short-term — SME Mart owns the seed catalog, Content migrates later. **Curated catalog exists** (frozen seed, external to this repo): `~/Projects/zb/com/ui/.claude/proposals/sme-mart-compliance-catalog.xlsx` (58 roles, 126 certs, 33 role→cert requirements, 19 sources across cyberab.org CMMC/SCF/SCA, FedRAMP, HITRUST, PCI SSC, CSA STAR, IAPP, ISO, CREST, ISACA, ISC², CompTIA, GIAC, EC-Council, OffSec, Mile2, FITSI). **Data model decision locked:** 3 new GQL classes in `zerobias-org/schema` — `Issuer`, `Certification`, `ProviderCredential`. NOT Neon tables (DataProducer writes don't work). NOT tags (flattens issuer/expiry metadata). **Pattern reuse:** clone the expertise chip-picker UI (`my-profile-expertise.component.ts`), extend `catalog-filters.component.ts` with a 7th filter row, template moderation off Reviews + `VettingStatus` enum. **Prerequisite gap:** Provider individual-vs-company scope (`ProviderProfile` has no `type` field today; C3PAO is org-level, CCA is individual) — needs resolving before credentials honor the `scope` enum. **Open questions for discuss:** (1) Provider-as-GQL-class vs opaque-UUID link to Neon `ProviderProfile` (ask Kevin). (2) GIAC enumeration (62 certs, catalog has 12) — free-form `G*` validation or complete catalog. (3) Auto-verification roadmap (Cyber AB Marketplace API, ISACA registry). (4) DoD 8140.3 refresh cadence (CAC-gated source). (5) Ecosystem filter granularity (flat 130 certs vs hierarchical). **Full research doc:** [`.planning/research/internal/2026-04-23-seller-credentials-catalog-research.md`](.planning/research/internal/2026-04-23-seller-credentials-catalog-research.md). **Scope:** 3 GQL classes + dataloader validation (4-6h); XLSX→GQL seed script via PipelineWrite (4-6h); Seller profile credentials section (6-8h); Buyer search filter row (2-3h); Admin catalog tab + moderation queue (8-10h); "submit custom credential" flow (4-6h); tests (4-6h). **Not in v1:** auto-verification, `RoleRequirement` join class, GIAC full enumeration, DoD 8140.3 automation. | 32–45 hrs | Brian directive + Kevin content-team decision 2026-04-23 |
| **092** | Marketplace tagType adoption — refactor tag-filter components | **From DECISIONS.md "Marketplace tagType Is Preferred for New Tags" 2026-04-29.** `zerobias-com/tag` PR #1 (Daniel Rojas merged 2026-04-29) registered `marketplace` tagType + `platform_provider` + `demo` global tags. Forward decision: NEW SME Mart tags use `tagType: "marketplace"`; existing `other`-typed tags stay (UUID-churn cost too high to migrate). **This entry covers the code-side refactor:** (1) Audit tag-filter services and components that today filter by `tagType === 'other'` (or that hardcode the type at all). (2) Update each to accept BOTH `other` and `marketplace` during the coexistence period. (3) Update any tag-creation paths (e.g., resource-tag-autocomplete inline-create) to default new tags to `marketplace`. (4) Add a unit-test fixture covering both types in lookup. **Tag NAMES retain `sme-mart.` prefix** — no name-side changes in this work. **Not blocking anything.** Phase 24 (Demo Data Visibility Gate) will use the `demo` global tag as its implementation primitive — that's covered in Phase 24's brief, not here. v1.5 hygiene candidate. | 4–6 hrs | DECISIONS.md 2026-04-29 + Daniel's PR #1 merge |
| **091** | Provider detail cosmetic polish (Phase 26 follow-up) | Two small nits surfaced during Phase 26 detail-page verify on 2026-04-28 (root-causes investigated 2026-04-28). **(1) Avatar fallback path is broken when img URL fails to load.** Template at `provider-detail.component.html:11-15` only branches on `p.avatar_url` truthiness — `@if (p.avatar_url) { <img [alt]=display_name> } @else { <span class="initials">{{ initials() }}</span> }`. When the URL is truthy but the request 404s (current state on UAT — seed at `seed-zb-provider.ts:23` writes `logo_url: 'https://zerobias.com/logo.png'`, which doesn't resolve), the browser falls back to rendering the `alt` text "ZeroBias" inside the avatar circle's CSS clip, displaying "ZeroBia" (clipped). The intended `initials()` fallback ("Z") is never reached. **Fix:** add `(error)="onLogoError()"` to the `<img>` and toggle a signal that re-routes to the `@else` branch on load failure. (Optional: also fix the seed URL to a real asset or null, but the component-level fallback is the durable fix.) **(2) "jobs completed" row missing count prefix when null.** Template at `provider-detail.component.html:34`: `<span>{{ p.total_jobs_completed }} jobs completed</span>`. When `total_jobs_completed` is null (current ZB shape — `provider-profiles.service.ts:109` sets it null because corporate providers don't have completed jobs), template renders blank + space + " jobs completed". **Fix:** wrap the row in `@if (p.total_jobs_completed !== null && p.total_jobs_completed > 0)` to hide it for corporate providers, OR coalesce with `{{ p.total_jobs_completed ?? 0 }}`. Hide is preferred — corporate providers don't have a "jobs completed" concept. Both nits contained to `provider-detail.component.{ts,html}` (and possibly `provider-card.component.*` if same patterns there). Not Phase 26 closure-blocking — Director acknowledged. | 1–2 hrs | Phase 26 closeout 2026-04-28 |
| **093** | Spike: Vendor/Product Two-Sided Partition (Community vs Producer) | **From Marketplace Meeting 2026-04-28.** Brian's architecture spike: every Vendor (and Product, Suite) needs a **two-sided partition** — a `community` partition built and curated by ZB community members, and a `producer` partition claimed by the actual vendor. Both must coexist permanently. The producer can never delete or manipulate the community partition; the community can refute/extend/oppose the producer's claims with **machine-verifiable proof** (API operations, queries that prove a feature/security-standard exists or doesn't). The community may catalog a vendor's product *before* the vendor ever shows up on the platform. **Vendor claim flow:** today's claim flow is a Task ("I'm claiming this vendor/product") with a manual approve step. Brian wants this to become **programmatic** once the validation gates pass — domain ownership proof, email at domain, tax ID, Dun & Bradstreet, etc. Approval still gated by one of us clicking the "yep" button at first; auto-approval downstream. **Scope (research spike, not implementation yet):** (1) Decide where the partition lives — separate GQL classes (`VendorCommunityProfile` + `VendorProducerProfile` linked to a shared `Vendor`/`Product`), or a `partition` enum on existing classes with row-level access. (2) Decide how disagreements are surfaced in UI — side-by-side diff view, dispute markers, "claimed by producer / disputed by community" badges. (3) Ownership/edit semantics — community partition is community-writable (with moderation?), producer partition is producer-writable only after claim verification. (4) Audit trail — every change attributed to community member or producer principal. (5) Integration with existing claim Task flow — preserve Task as the acceptance ceremony but auto-create programmatically once D&B/domain/tax-ID checks pass. (6) Read API — single "view this vendor" endpoint that returns both partitions side-by-side with provenance. (7) UI sketch — vendor detail page with "Producer Says / Community Says" sections and a divergence indicator. **Dependencies:** existing vendor profile, existing claim task flow, ZB platform identity verification (D&B, tax ID — may not exist yet). **Out of scope for spike:** implementation, schema PRs, UI build. Spike output is a written design + a recommendation to promote into a milestone with phased work. **Likely v1.5 or v1.6** depending on platform-side dependencies. | 6–10 hrs spike | Marketplace Meeting 2026-04-28 (Brian) |
| **095** | Joe + Dan + Clark cross-team architecture sync — converge on engagement / project / subproject / workspace + transparency entanglement | **From Marketplace Meeting 2026-04-28 — Brian's strongest directive in that meeting.** Joe (Work Worlds) and Dan (Readiness Center) are building adjacent platform UIs that must share the SAME plumbing as SME Mart for engagement / project / subproject / workspace + transparency entanglement, even if their UIs differ. Brian quote: "All three of you guys could be different UIs. I just want to make sure it's the same damn thing underneath." **Scope:** (1) Clark drives a recurring sync (Brian suggested mornings via ZBDX). (2) Onboard Joe to SME Mart UAT login once Andre + Chris ship login fixes. (3) Share the transparency-architecture HTML diagram (`.planning/sketches/transparency-center-ui-concepts.html` or equivalent) with Joe; ask for feedback. (4) Drive consensus on the canonical entity shape for Engagement + Project + Subproject + Workspace + the entanglement (req↔sat) link contract. (5) Document the converged contract in `.planning/director/cross-team-platform-contract.md` as the single source of truth referenced by all three apps. Nick is already clued in on GQL/structure side; he should review the schema alignment. **Outputs:** documented contract, working sync rhythm, Joe + Dan + Clark all using the same structural names + link types. **Dependencies:** Joe needs SME Mart login (blocked on Andre + Chris). Phase 20 closure removes the only remaining v1.4 distraction. **Brian's framing — "intersection and collisions":** Brian explicitly wants productive collision, not committee-by-consensus. UIs differ; transparency must converge. | Ongoing — recurring sync + ~6 hrs to author the contract doc | Marketplace Meeting 2026-04-28 (Brian directive) |
| **096** | Stitch (Google) vs Claude Design — UI mockup comparison spike | **From Marketplace Meeting 2026-04-28.** Clark demoed Stitch (Google's UI mockup tool) generating Template Library screens — now wired via MCP, so Claude can drive Stitch directly. GSD turns Stitch screens into Angular code. Brian asked whether Stitch beats Claude's own design tool. **Spike:** generate the same screen (e.g., a v1.4 onboarding view) via both Stitch and Claude Design; compare quality of output, ergonomics of the Claude→tool workflow, code-conversion fidelity (how clean is the Stitch→Angular pass vs Claude Design→Angular). Output: 1-page memo with a recommended default and one-line rationale. **Out of scope:** rebuilding the UI in either tool, picking a "winner" beyond the comparison memo. **Low priority** — Brian was curious, not demanding. Run when the design pipeline is the gating constraint on a phase, not before. | 2-3 hrs | Marketplace Meeting 2026-04-28 (Brian curious) |
| **097** | Programmatic vendor-claim flow (auto-approve once D&B / domain / tax-ID validated) | **From Marketplace Meeting 2026-04-28. Cross-references BACKLOG #093 (vendor partition spike).** Today the vendor-claim flow is a Task ("I'm claiming this vendor/product") with manual approval — one of us clicks the "yep" button. Brian directive: this becomes **programmatic** once validation gates pass — domain ownership proof, email at domain, tax ID, Dun & Bradstreet, etc. Approval still gated by a human button-press at first; auto-approval downstream. **Scope:** (1) Define the validation-gate contract — which checks must pass before auto-approval kicks in (D&B match, domain DNS proof, email-at-domain proof, tax-ID lookup). (2) Identify which validations exist on the ZB platform today (likely none) vs which we build/integrate. (3) Wire the validation results into the existing claim Task — Task auto-completes when all gates green. (4) Preserve the human button-press as the first gate before flipping to fully automatic. (5) Document the integration points and which gates are open vs deferred. **Dependencies:** BACKLOG #093 (vendor partition) — partition shape determines what "claiming" means; ZB platform identity verification (D&B, tax ID — likely doesn't exist yet, so this is partly a platform ask). **Likely v1.6+** depending on platform-side dependencies. | 6-10 hrs scoping + N hrs integration depending on platform readiness | Marketplace Meeting 2026-04-28 (Brian directive) |
| **094** | Dead-code sweep on Phase 20 Wave 2-touched files | **Surfaced by gsd-execute Wave 2 finish 2026-04-29.** Pre-existing unused imports / class fields / locals on files Wave 2 touched, NOT introduced by Wave 2: (a) `note-hierarchy.service.ts:8` — unused `NOTE_FIELD_MAPPING`, `mapGqlToNeon` import (verified pre-existing via `git show 9853efe:<file>`); (b) unused class fields / locals across 4 production files — `tagService`, `shareId`, `documentId`, `escapeValue`, `impersonation` (×3 callers), `engagementId`, `findNodeByName`. `tsc --noEmit` does not fail on these (no `noUnusedLocals` set in any tsconfig) but the IDE flags them. **Action:** sweep all 5 files, delete genuinely-unreachable code, leave a comment for any kept-intentionally case. **Bigger question to evaluate during the sweep:** should we enable `noUnusedLocals` / `noUnusedParameters` in `tsconfig.json` to make this a tsc-time failure going forward? Trade-off: catches drift early, but breaks builds during in-progress refactors. v1.5 hygiene candidate. **Not blocking anything.** | 2-3 hrs | Phase 20 Wave 2 gsd-execute checkpoint 2026-04-29 |
| **099** | RFP-as-`platform.Project` architecture decision (A / B / C) | **From Phase 31 pre-walkthrough Director-Clark discussion 2026-05-13.** RFP is the first-class buyer surface: buyer creates RFP ("Request for Project"), providers bid, buyer hires, engagement ensues, RFP promotes to Pilot or regular Project after vetting completes. **Today** RFP lives as `SmeMartProject` GQL class (`projectType: 'rfp'\|'pilot'\|'project'` discriminator + `promotedProjectId` lineage + rich JSON fields: `formConfig`, `evaluationCriteria`, `budgetMin/Max`, `category`, `responseDeadline`, `wizardStep/Data`). Clark preference: use platform primitives where possible, only GQL where absolutely necessary. **Three candidate architectures:** **(A)** Status quo with discriminator fix — RFP stays SmeMartProject GQL; on bid acceptance spawn separate platform.Project (Engagement) per buyer-provider pair; engagement hosts vetting boards. Smallest change; GQL stays load-bearing. **(B) — Director + Clark preferred** — RFP IS a platform.Project tagged `sme-mart.project-type.rfp` (or similar) + sidecar GQL extension class keyed by `platform.Project.id` carrying the rich JSON fields. /rfps page filters platform.Project by type-tag namespace. On bid acceptance: spawn separate Engagement platform.Project per buyer-provider pair (D-49 namespace) — multi-engagement-per-RFP works naturally. Promotion to Pilot/Project = tag transition + hydra `resourceLink` lineage. RFP identity is platform-primitive; rich-field filtering (budget/category/deadline) stays indexed on GQL. **(C) — Cleaner endpoint, depends on Hydra capability** — Full platform.Project + hydra Resource metadata for rich fields (no GQL extension class). Zero GQL classes for RFP lifecycle. **Blocks on:** (i) does hydra support attaching JSON metadata to a Resource? (ii) is that metadata queryable enough for /rfps list filtering? (iii) where does the `FormBuilderConfig` blob live? Clark asked Nic about a `setMetadata()` feature; **pending Nic's response.** **Where vetting boards live in all three:** on the Engagement node (the `engagement`-role `platform.Project` per D-52), NOT on the RFP. Each buyer-provider engagement carries its own vetting Board(s) for req↔sat task pairs. RFP can carry a board for bidding-phase work (questions, evaluation criteria review) but that's not load-bearing for vetting. **Multi-engagement-per-RFP:** confirmed by Clark — one RFP can spawn N engagements (one per accepted provider) if buyer hires multiple providers off the same RFP. All three architectures handle this. **Next steps:** (1) Wait for Nic's response on `setMetadata()` / hydra metadata capability. (2) Spike (~3 hrs) evaluating C feasibility once Nic responds: write a tiny prototype attaching FormBuilderConfig as hydra metadata, query/filter test, retrieval test. (3) Output: one-pager recommending B-final or C. (4) Then design migration phase. **Existing RFP data** (any SmeMartProject rows with projectType='rfp'/'pilot'/'project') needs migration plan: dual-read window pattern (same as Phase 29.5) — primary read from new shape, fallback to legacy. **Affected surfaces (currently broken via errata 039 cross-contamination, hidden for v1.4 per Phase 31-A):** /rfps, /my/engagements, /my/projects, RFP creation wizard. These come back online when this architectural decision is implemented. **Out of scope for THIS entry:** implementation (becomes a phase once decision is made), schema PRs, UI migration. **Likely v1.5 or v1.6** depending on Nic's response timing and chosen architecture. Filed 2026-05-13 by Director, captures Clark-Director architectural discussion verbatim. | 3 hrs spike + N hrs implementation depending on chosen path | Phase 31 pre-walkthrough Director-Clark discussion 2026-05-13 |
| **098** | Org Profile revamp — auto-populate from ZB platform Org + LLM-prompt internet-gathering + evaluate folding into Vetting Board | **From Phase 31 pre-walkthrough 2026-05-13.** Today's `/org/profile` (Corporate Profile tab — newly first tab and default landing as of `80fff24` follow-up commit) renders an empty 6-section vendor profile (Corporate Identity, Attestation, Insurance, Personnel, Financial, Reference) with "Add item" buttons. Every section starts at 0 items. **Customer experience problem:** new customers are starting from scratch — every field manually entered — which is a poor onboarding experience that competing platforms (Crunchbase, Apollo, ZoomInfo, public web) could have pre-filled. **Three intertwined work streams:** (1) **Auto-populate from ZB platform Org fields.** ZB platform's `Org` entity already carries name, slug, possibly address / website / industry / size; map those into corresponding Corporate Identity fields at first profile load. Single SDK call (`getMeApi().listMyOrgs()` already happens in onboarding-guard); cheap. Audit what fields the platform Org actually exposes — see related Phase 25 (Platform Data Audit) findings. (2) **LLM-prompt internet-gathering preflight.** Build a Claude prompt + skill that takes `{orgName, orgWebsite, orgSlug}` as input and produces a structured pre-fill payload covering as much of the 6 sections as is publicly discoverable (Corporate Identity: legal name, DBA, EIN if public, year founded, HQ address, website, leadership names; Insurance: carrier names if discoverable from job postings/press releases; Personnel: leadership + headcount from LinkedIn-style sources; Financial: revenue band if public; etc.). Sources to evaluate: web search, Crunchbase API if available, LinkedIn public profiles, company "about" pages, press releases, SEC filings for public companies, Better Business Bureau, state corporation registry lookups. Output: a JSON document the customer reviews + accepts / edits per item before save. Could land as a `/llm-prefill-org-profile` Claude skill. (3) **Vetting Board interaction — evaluate folding.** A subset of the profile sections (Attestation, Insurance, Personnel, Financial — anything with renewal/expiration cycles) overlaps semantically with a Vetting Board's "documents under review with expiration tracking" pattern (see vetting-tab.component and Plan 07 VETTING-PLATFORM-MIGRATE-1 deferred work). Question: should those four sections move to the Vetting Board entirely, leaving the org Profile as just the "this is who we are" identity facts (Corporate Identity + Reference)? Or should profile and vetting be tightly cross-linked but kept separate UIs (profile = self-asserted, vetting = third-party-verified)? Spike output: UX recommendation + data-model implication. **Sub-issues from 2026-05-13 walkthrough surfaced same day:** welcome-card flash (renders briefly during load, dismissed by `items.length > 0` check at vendor-profile-tab.component.ts:152 — but sections still show 0 items, suggesting `items` come back but the `section` field shape doesn't match the SECTION_ORDER enum; data-shape bug worth investigating separately). **Dependencies:** Phase 25 (Platform Data Audit — what Org fields are available); existing vendor-profile-tab + vendor-profile-form components; Plan 07 vetting work for the fold-evaluation. **Out of scope for this entry:** rebuilding the form-builder; new schema classes (we'd reuse `MarketplaceProfileItem`). **Effort:** ~6-10 hrs for stream (1) auto-populate alone; ~8-12 hrs to build + tune the LLM-prefill skill (stream 2); ~3-4 hrs for stream (3) spike + recommendation. **Likely v1.5** depending on Phase 31 dogfood outcomes; the flash + empty-sections experience may motivate prioritization. Filed 2026-05-13 by Director. | 17-26 hrs total (split-or-combine across streams) | Phase 31 pre-walkthrough Chrome DevTools triage 2026-05-13 |
| ~~**101**~~ ✅ CLOSED 2026-05-15 (D-51) | ~~Engagement display-name convention — drop ASCII arrow, find a clearer supply/demand-aware format~~ | **CLOSED 2026-05-15 by D-51.** Convention adopted: `Engagement with provider ZeroBias Platform` (name) + `Platform services engagement provided by ZeroBias Platform for ${orgName}.` (description). Arrows dropped; provider/client asymmetric role label adopted. Provisioner code + spec updated; Brian's-Org engagement updated on UAT. See DECISIONS.md D-51. **Historical brainstorm (preserved for context):** **From Director-Clark conversation 2026-05-14.** Today's display name is `<Buyer> <- <Provider>` ASCII reverse-arrow (e.g. `W3Geekery <- ZeroBias`). Rationale (per memory `project_sme_mart_hierarchy_model`): buyer-first, supply flows toward the buyer, aligns with project's Demand/Supply vocabulary. **Problem:** the `<-` arrow is ambiguous-glyph to non-domain readers (looks like a typo or markdown artifact), doesn't survive copy-paste cleanly, and "supply flows toward buyer" isn't legible without the framing memo. **Kevin's suggestion 2026-05-14:** baseball-reference shape — `Owner-Org @ Invited-Org`. Crisp + familiar, but doesn't encode supply/demand direction at all — both teams in baseball are symmetric. **Clark's ask:** brainstorm display-name formats that explicitly say who's buyer and who's provider without relying on a glyph the reader has to interpret. **Important decoupling:** this is a **display-name** revisit, NOT a tag-rename. The canonical engagement tag (`sme-mart.engagement.{supply}-to-{demand}`, full word, supply-to-demand) stays per D-49. Anti-pattern (d) of D-43 still applies — no UUID-churn migration. Tag namespace + UUIDs are stable; only the human-readable display string under `platform.Project.name` would change for new engagements, with legacy display names left alone unless an operator opts to rename. **Seed brainstorm (NO ARROWS per Clark directive 2026-05-14):** (a) `Owner-Org @ Invited-Org` (Kevin baseball — clear but no direction); (b) `Acme [Buyer] / ZeroBias [Provider]` (paired with explicit role tags — verbose but unambiguous); (c) `ZeroBias serves Acme` (English verb; provider-first; supply-flow encoded as the verb itself); (d) `ZeroBias for Acme` (English preposition; concise; provider-first); (e) `Acme buys from ZeroBias` (English-explicit; buyer-first; longer); (f) `Acme/ZeroBias [Buyer/Provider]` (paired, slash-separated, role tags trailing — compact variant of (b)); (g) `Acme · ZeroBias` (middot, neutral, no direction — same problem as `@`); (h) Two-field display (no single concatenated string at all — UI renders "Buyer: Acme" and "Provider: ZeroBias" as separate fields, ditches the convention entirely for display, keeps the tag canonical). **Tradeoff dimensions:** (1) instant clarity to a non-domain reader, (2) compactness (fits in card titles, breadcrumbs, log lines), (3) glyph safety (copy-paste, accessibility), (4) supply/demand encodability, (5) symmetric vs asymmetric vocabulary (engagements are inherently asymmetric — buyer commissions, provider executes). **Scope when promoted:** (1) Director-Clark discuss to pick the format (likely a half-hour conversation, possibly with Brian/Kevin input). (2) Update `provisioner.service.ts` engagement-creation path to use the new format for `platform.Project.name`. (3) Optional: small operator UI on the admin tab to rename existing engagements one-by-one (no batch migration). (4) Update any place that parses the display name (audit — there shouldn't be any; tag is the parser-friendly key). (5) Update memory `project_sme_mart_hierarchy_model` + DECISIONS.md with the new convention. **Out of scope:** tag rename (forbidden per D-43-d), batch migration of existing engagements (operator-by-operator only if at all), broader vocabulary changes (e.g. renaming "Engagement" itself). **Effort:** ~1 hr discuss + decision + 1-2 hrs implementation. Filed 2026-05-14 by Director. | 2-3 hrs | Director-Clark conversation 2026-05-14 + Kevin baseball-format suggestion |
| **102** | Normalize `ZerobiasClientApp` injection name to `app` (single outlier) | **From audit 2026-05-14.** Every `ZerobiasClientApi` injection in `src/` uses the field name `clientApi` (23/23). `ZerobiasClientApp` injections use `app` in 16/17 sites; one outlier at `src/app/onboarding/company-profile-form.component.ts:73` names it `zbApp` (callsites at lines 91, 181). **Fix:** rename field `zbApp` → `app` and update both `this.zbApp.getCurrentOrgId()` callsites. Single file, 3 edits. Trivial — fold into a touch-it=fix-it pass when next editing that component, or batch with other hygiene. | 15 min | Audit 2026-05-14 |
| **103** | `/org/profile` Add-Item dialog is not theme-aware | **From Clark observation 2026-05-14, screenshot `Screenshot 2026-05-14 at 6.25.00 PM.png`.** On `/org/profile`, clicking the `+` to add a new profile section item opens an "Add `<section>` Item" dialog (target: `vendor-profile-form.component.*` in `src/app/pages/org/tabs/`). In dark mode the dialog renders with a hard-coded white panel background, white input field backgrounds, and (apparently) light-on-light text — labels are invisible against the white inputs and the Cancel button text is unreadable. Dialog is theme-blind: hardcoded `background-color: white` / no `--zb-background-card` / no `--zb-text` / no `--mat-sys-on-surface`. **Secondary bug visible same screenshot:** dialog title reads "Add Corporate_identity Item" — the section key (`corporate_identity`) is being interpolated raw with underscore preserved and only the first letter title-cased. Should be "Add Corporate Identity Item" (underscores -> spaces, title-case each word). Single-line `titlecase` pipe + `\| replace` or a one-off helper. **Scope:** (1) Migrate dialog SCSS from hardcoded colors to `--zb-background-card` / `--zb-text` / `--mat-sys-*` tokens so dark + light both render legibly. (2) Add a section-key formatter (`'corporate_identity' -> 'Corporate Identity'`) used in the dialog title and likely elsewhere (section headers in the parent tab use the same key — check `vendor-profile-tab.component.html` for similar). (3) Verify Cancel + Save buttons render via Material's themed defaults, not hardcoded overrides. (4) Smoke test in both themes via the toggle in `/my-profile/settings`. **Affected file(s):** `vendor-profile-form.component.{ts,html,scss}`. Possibly also `vendor-profile-tab.component.{html,scss}` if section labels share the formatter problem. **Effort:** ~1-2 hrs. Touch-it=fix-it candidate when next editing the vendor-profile-form, OR fold into BACKLOG-098 (Org Profile revamp) since the form is being rebuilt there anyway. | 1-2 hrs (or roll into 098) | Clark observation 2026-05-14 |
| **100** | Shared Tasks tab (Engagement + Project) — Boards list, board drill-down, board switcher | **Target: current milestone (v1.4)** — Clark directive 2026-05-14, all primitives ready. **Shape:** ONE shared component used on both `/engagements/:id/tasks` and `/project/:projId/tasks` because the underlying primitive is identical — `platform.Board.list({projectId})` returns all boards under that container (both the Engagement node and the delivery Project are `platform.Project`s; per D-52 Engagement is a node-role, not a depth tier). Main page renders the list of boards under the entity; clicking a board drills down to a task-board view; that drill-down view includes a **board chooser/switcher** so the user can flip between sibling boards without leaving the surface. **Reference implementation:** `zb/ui:feat/board-context-selector-mvp` branch (Clark's other Claude session is building the switcher MVP right now). Mirror that shape so the SME Mart implementation can later be **migrated upward into the platform / ngx-library** without a rewrite. **Scope (for `/gsd-plan-phase` to expand):** (1) New shared component `app-board-list-tab` (location TBD — likely `src/app/shared/components/board-list-tab/` so both engagement and project can import it) — props: `containerId: string` (the engagement-or-project UUID), `containerType: 'engagement' \| 'project'` (for context / breadcrumbs / empty-state copy). Calls `platform.Board.list(1, 100, undefined, undefined, undefined, new UUID(containerId))` (BoardApi positional sig: `projectId` is param 6, server-side filterable, no client-side filter needed — same pattern as ProjectBoardsTabComponent landed in commit `28fe83b`). Renders board cards with name + Default chip + boardType + status + created date; card click navigates to board drill-down. (2) Re-route `engagements/:id/tasks` from `TasksTab` (existing component) → the new shared tab OR refactor `TasksTab` to compose the shared component plus its existing legacy task-list. Audit what `TasksTab` does today before deciding. (3) Re-route `project/:projId/tasks` similarly (currently `ProjectComingSoonTab` per `project.routes.ts`). (4) Board drill-down view — new component or extend `ProjectComingSoonTab` route `boards/:boardId` — must surface: (a) the selected board's tasks (`platform.Board.listTasks(boardId)`), (b) the **board switcher** mirroring zb/ui's `feat/board-context-selector-mvp` shape (sibling list + active indicator + click-to-switch). (5) Migration-readiness: keep the shared component's API surface minimal and platform-primitive-only so when ngx-library hoists this pattern, the migration is mostly a path-rename + dependency swap. **Dependencies:** ProjectBoardsTabComponent (`28fe83b`) shows the data-fetch pattern works; `platform.Board.list` + `platform.Board.listTasks` SDK methods (already in `@zerobias-com/platform-sdk`); engagement-detail Tasks tab existing component for context. **Cross-team alignment:** coordinate with Clark's zb/ui session — review `feat/board-context-selector-mvp` before drafting the switcher contract so SME Mart's version matches. **Pre-plan-phase checks:** (1) Pull latest from zb/ui:feat/board-context-selector-mvp and capture the switcher's input/output shape + UX behaviors in `.planning/notes/board-switcher-reference.md`. (2) Verify `platform.Board.listTasks` returns what we expect for the W3Geekery default board (likely empty today — that's OK, drill-down still renders the empty state honestly). **Prompt for `/gsd-plan-phase`:** *"Plan a shared Tasks tab component for SME Mart that powers both `/engagements/:id/tasks` and `/project/:projId/tasks`. The component's main view is a list of `platform.Board`s under the container (engagement Project or Project-tier Project), backed by `platform.Board.list(.., projectId)` (server-side projectId filter). Clicking a board drills into a board detail view that includes a board switcher (siblings list, active indicator, click-to-switch) mirroring the MVP being built in `zb/ui:feat/board-context-selector-mvp` so the implementation can migrate upward into ngx-library / platform later. Cover: (a) shared component location + naming (`board-list-tab` under shared/components/); (b) inputs (`containerId`, `containerType`); (c) data fetch (reuse the BoardApi.list pattern from ProjectBoardsTabComponent at commit 28fe83b); (d) routing — replace `ProjectComingSoonTab` at `/project/:projId/tasks` and decide whether engagement's existing TasksTab composes the new component or is replaced; (e) board detail drill-down (board-detail.component) with switcher widget mirroring zb/ui MVP — pull that branch's API surface first into `.planning/notes/board-switcher-reference.md`; (f) navigation contract between drill-down and switcher (URL changes vs in-place swap); (g) migration-readiness — keep the component dependency-free of SME-Mart-specific services so ngx-library can hoist it. Out of scope: task creation/edit UI (separate phase), task-detail page, board CRUD."* | 12–18 hrs | Director-Clark conversation 2026-05-14 — clicked through `/project/:id/boards` after `28fe83b` landed, realized Tasks tab on both surfaces needs the same treatment plus drill-down + switcher |
| **108** | Mirrored Engagement Vetting — Vetting as depth-3 Project + template-driven Board layout (Option 5-prime) | **From Clark + Brian discussion 2026-05-19; AMENDED 2026-05-22 from Option 3 → Option 5-prime** (extended Vetting-shape thread + Brian's 2026-05-19 marketplace-meeting reframe + Nic link-type discussion). Refines BACKLOG-106's Vetting Board scope to handle the **bidirectional** nature of Vetting confirmed in this session: both Buyer AND Provider have requirements the other side must satisfy. The current `vetting-tab.component.*` schema already has dual `buyer_requires` / `provider_requires` sections, AND the SHACL/OWL/Holon framing per `.claude/handoffs/shacl-owl-holon-quantum-overlay-2026-05-19-fixed.html` treats every Requirement as an entangled demand-half + supply-half task pair joined by the Requirement contract id with `acceptance_primitive` as the measurement operator. **LOCKED shape (Option 5-prime — Director decision 2026-05-19, amended 2026-05-22; RESHAPED 2026-06-01 by D-52):** Vetting boards live on the **Engagement node** (the standalone `engagement`-role `platform.Project` that governs the delivery tree per [[project_sme_mart_hierarchy_model]] + DECISIONS D-52) — NOT a depth-3 sub-Project under a depth-2 "Workspace" (that framing is superseded; Engagement is no longer a structural tier). The shared Requirement registry (entangled demand-half/supply-half task pairs) lives once on that Engagement node. **Board layout is template-driven, not fixed:** DEFAULT = a single Vetting Board (section-tags + direction-tags + the **perspective signal** below). **Escape hatches (template-selected):** section-split, direction-split, or full-split into N Boards — or promote a section to its own **depth-4 sub-Project** when that section's membership / visibility / policy genuinely diverges. The engagement template parameterizes which layout an engagement gets (template-parameterization wizard → [[BACKLOG-111]]). **Entanglement link (Req↔Sat):** Nic is POSSIBLY adding a Task↔Task `ResourceLinkType` **`satisfies` / `satisfiedBy`** to express the Requirement-to-Satisfaction entanglement between the demand-half and supply-half tasks — explicitly NOT `twin_of` (which carries no semantic context). This SUPERSEDES the earlier asymmetric `requires_supply_from ↔ supplies_demand_of` framing. Interim until Nic ships: `relates_to` + a direction tag. **Ad-hoc metadata = hydra Tags, NOT `Task.customFields`** (customFields is catalog-locked — defined by the Activity, and `platform.Activity` has no create/update API); migrate to Board Settings custom fields if/when platform ships them. **Perspective signal (applies to the default single-Board layout):** sourced from the current user's Party context — default view = "my side" (demand-half tasks I'm owed + supply-half tasks I owe), toggle to "all" / "other side" / "both"; plus a visible "Buyer expects from Supplier" / "Supplier expects from Buyer" UI pill. **Why 5-prime (vs. Option 3's rigid single Board):** Brian's 2026-05-19 marketplace meeting reframed Vetting from "ONE Board" to "Project + sub-projects + N Boards per Vetting section" (banking, corporate identity, D&B, financial, MSA, background checks). So the single Board becomes the DEFAULT, but a template can escalate structure when a section needs its own membership/policy — without disturbing the D-50 depth-1/2 hierarchy (Vetting nests at depth-3+). Still maps onto the Holon framing: Engagement = the seam (Holon at depth 1), each Party = a Boundary (sub-Holon), each Vetting Board = a projection / Hologram of the shared Requirement registry from a party's boundary. **Naming:** "Vetting" stays as the symmetric umbrella term (no rename); explicit per-task direction labels disambiguate within. **Plan-phase open questions:** (1) direction + entanglement encoding — once Nic ships `satisfies`/`satisfiedBy`, direction is implied by the link's from/to roles; until then use `relates_to` + a hydra direction tag (reversible + queryable). Decide whether to keep a direction tag after the link lands (belt-and-suspenders for queryability) or rely on the link alone. (2) Perspective-signal source — derive from current user's Party UUID via membership lookup, OR pin to the engagement's `ownerId` party? Should be membership-derived so users with cross-party visibility (admins, mediators) see "all" by default. (3) Notification routing + RACI inheritance — each side's notified/accountable need perspective-aware filtering so a buyer doesn't get spammed by supply-half supplier-internal task updates. **Compass implications:** satisfies C-1 (entangled-pair preserved — now via `satisfies`/`satisfiedBy` link semantics) + C-4 (party-boundary scoping via depth-3 Vetting Project + per-task direction) + C-7 (template-instantiated Vetting Project / Boards / sections must record `template_id` + `template_version` + ontology source for provenance) per `.planning/docs/RDF-COMPASS.md`. **Target:** v1.5 alongside Phase 33 Polish (Vetting migration). **Prerequisites:** Phase 33's Vetting-tab-to-Vetting-Board migration design (BACKLOG-106 part g) — this entry IS the LOCKED shape that migration targets. **Files likely touched (additive to BACKLOG-106's list):** task direction tag/metadata in `platform-engagement-provisioner.service.ts`, perspective-signal logic in the shared `app-board-card` + `app-board-list-tab` (already in BACKLOG-100 scope), party-context resolution helper, notification filter on per-side delivery. **Effort:** layered on top of BACKLOG-106's Vetting migration; additional ~6-10 hrs for the perspective-signal projection logic + direction labeling + notification filtering. | +6-10 hrs on top of #106 (g) | Clark + Brian 2026-05-19 mirrored-Vetting discussion |
| **109** | RDF / SHACL / Holon readiness — SME Mart shape compass (no painted corners) | **From Brian's 2026-05-19 directive.** SME Mart designs touching Engagement / Project / Task / Vetting / Record shapes MUST be eventually serializable into the agreed-upon JSONL/RDF/Turtle container per `.claude/handoffs/shacl-owl-holon-quantum-overlay-2026-05-19-fixed.html`. SME Mart does NOT build the gateway / triplestore / OSCAL binding — those are platform-team scope (Kevin/Nic). SME Mart's job is **don't paint corners**. **Canonical compass:** `.planning/docs/RDF-COMPASS.md` (drafted 2026-05-19). Contains: ZB↔W3C vocabulary mapping, 6 non-negotiable constraints, the 5-question Director / plan-phase checklist (C-1..C-5), scope walls. **Cross-referenced from:** `/CLAUDE.md` Quick Reference table, `DIRECTOR-PARKS-RESUME.md` rules block, basic-memory `zerobias/integration/rdf-final-state-compass`. **Scope of this BACKLOG entry:** (1) MAINTAIN the compass as new W3C / OSCAL / Brian-directive signal arrives. (2) APPLY C-1..C-5 at every design review on touched entities — failing checks need remediation or explicit Director "accept gap, file debt" call. (3) FOLD compass clauses into Phase 32 + Phase 33 plan-phase + execute-phase non-negotiable patterns blocks (alongside Angular 21 modernization rules). (4) FILE debt entries here when a phase ships with a compass gap consciously accepted (e.g., "Phase 33 Vetting migration ships without explicit SHACL profile version pin — accept; remediate when platform-team ships version surface"). (5) UPDATE `.planning/docs/RDF-COMPASS.md` Open Questions section as platform-team answers land. **Target:** v1.6+ strategic — no hot phase scope; informs every phase that follows. **Not a phase candidate; a STANDING constraint.** | Standing | Brian directive 2026-05-19 |
| **110** | Holon / Hologram vocabulary adoption — Brian/Kevin/Nic decision | **From handoff section 9 item #6, Brian 2026-05-19.** Decide: should ZB adopt "Holon" + "Hologram" as **schema-of-record vocabulary** (new ZB entities — Holon record, Hologram record, attached to existing Boundary + Assessment) OR **narrative-only vocabulary** (external pitch language, marketing, regulator decks)? Both are viable per the handoff section 8 ("Adopt 'Holon' + 'Hologram' in external comms — Brian's framing is technically precise"). Schema-of-record gives every Boundary an explicit Holon record + projectable Hologram surface; narrative-only keeps the schema lean and uses the terms only in publish/communication layer. **Director-side action:** add to Brian Tue/Fri meeting agenda. NOT SME Mart work; Director tracks the outcome and folds it into the RDF Compass once decided. **Compass implications:** if schema-of-record path wins, BACKLOG-109's compass adds explicit Holon record shape constraints (C-6+); if narrative-only path wins, compass stays as-is. **Target:** discussion only, no milestone. | Discussion | Brian directive 2026-05-19 (handoff section 9 #6) |
| **106** | Engagement: rename Tasks tab → Boards; card grid + pin-to-expand; seed default board with onboarding helper tasks; convert Vetting tab to Vetting Board | **From Clark 2026-05-18 during BACKLOG-103 walkthrough.** UX refinement layered on top of BACKLOG-100 (shared Tasks/Boards tab architecture). **Shape:** On `/engagements/:id`, rename the **Tasks** tab to **Boards**. The tab renders a card grid — one card per `platform.Board` whose owner/parent is the engagement Project. Cards show board name + boardType + status + task count + Default-board chip. Each card has TWO distinct affordances — they coexist, not alternatives: (a) a **"pin this"** icon that *in-place expands the card into a paged tasks-list table* (≤ 25 tasks per page, summary preview — `platform.Board.listTasks(boardId)`). Multiple cards can be pinned at once (the table replaces the summary face of THAT card; other cards stay as summaries). **Pin state stored in `localStorage` (immediate, per-device) AND PKV (per-user, multi-device sync via the same preferences mechanism used elsewhere — see [[BACKLOG-099]] for PKV spike status)** so pins survive reload and follow the user across devices. (b) A **drill icon** (separate from the pin icon — likely an "open in detail" / arrow-out glyph) that *navigates to the board-detail page* at **`/boards/:boardId`** (LOCKED 2026-05-18 — global, top-level route). Breadcrumb on the detail page reconstructs `Engagement > Project > Board` (or just `Project > Board` for non-engagement contexts) via the board's `projectId → project.parentId` chain — no route segments needed for that. **Why top-level beats scoped routes (decision rationale, locked):** (i) ONE route + ONE component serves both engagement and project entry-paths — no duplicated route definitions or per-scope breadcrumb branches. (ii) Aligns with zb/ui's pattern for top-level entity detail pages (Task, Resource) — ngx-library hoist later is a path-rename only. (iii) **Unlocks a `/boards` top-level LIST page** — cross-org view of all boards the current user can see across all orgs + projects they have perms on, with `orgIds` filter (multi-select) and a per-card **scope-flavor indicator** (org name / engagement name / project name as compact provenance chip). (iv) **Unlocks User/Private boards** — boards scoped to a single user instead of a project, which only make sense at top-level since they don't live under any project hierarchy. (v) Route guard is a one-liner: `board.projectId → can I see this project?` (already the engagement/project list-page logic). **Plan-phase verification:** confirm via ZB MCP that `platform.Board.search` (or `.list`) accepts an `orgIds: UUID[]` filter parameter — Clark believes it does. If it does, the `/boards` list page is a straight `zb-remote-table` over `Board.search` with an org-multi-select FilterPanel. If it doesn't, file a Brian/Kevin/Nic ask to add it (cheap server-side change vs. client-side scatter-gather across N orgs). **Board-detail page contents:** (i) **Cog/config-panel icon** opening a side panel for inline **name / description edit** plus per-user **defaults via PKV** (sort order, default filters, column visibility, etc.). (ii) **Infinite-scroll tasks list** rendered with **ngx-library `zb-remote-table`** (search + filtering already wired in the component — reuse, don't rebuild). Table shape should mirror **zb/ui's tasks list view** — capture columns + actions in `.planning/notes/zb-ui-tasks-list-reference.md` before planning. (iii) **Triple-dot / actions menu** with admin-visible **"Open in ZB Platform"** link to the corresponding `platform.Board` page on `app.zerobias.com` (gated by `getPrincipal().isAdmin` per [[project_sme_mart_admin_detection]]). A **`+ Create Board`** button on the tab creates a new board with the engagement Project as `parent` / `projectId`. **Default board seeding:** the provisioner (`platform-engagement-provisioner.service.ts`) already creates an auto-Board at engagement provision time. Extend it to seed N helper tasks on that default board (suggested next-steps + "how to find help" + "what is this engagement" info-tasks) so the user lands on a non-empty Boards tab. **Vetting tab → Vetting Board conversion:** today there's a separate **Vetting** tab on engagement-detail. Convert that to a *Board* whose parent is the engagement Project — so it shows up as a card in the Boards list alongside the default board. Likely needs a `boardType: 'vetting'` discriminator (or a `sme-mart.board.vetting` tag) so the Vetting card renders its specialized UI (vetting circle / checklist) when pinned, while default/custom boards render the generic tasks-list table. **Open questions for plan-phase:** (1) Pin and drill coexist on each card (pin = in-place paged overview of tasks for a quick glance; drill = navigate to `/boards/:boardId` for immersive board context) — BACKLOG-100's shared component needs to support BOTH affordances. The board switcher proposed in 100 lives on the **detail page**, not the card grid. Card-grid surface adds the pin behavior on top. **(1a) Pinned-card layout — LOCKED 2026-05-18 via Sketch 001:** **inline expand-in-place** (CSS Grid `repeat(auto-fill, minmax(320px, 1fr))` with `grid-column: span 2` on pinned cards; on narrow viewports `< 760px` the pinned card spans full width). Multiple pins coexist; "everything in glance" preserved; single-grid component is the cheapest build and aligns with shared-component-for-Project-Boards constraint. Sketch artifact at `.planning/sketches/001-boards-pin-expand/` (variant A wins; variant B left-right split is filed for posterity but not the chosen pattern). **(1b) Cross-org `/boards` list page — NEW scope (added 2026-05-18 with route-decision rationale above):** the same `app-board-card` + `app-board-list-tab` shared components power the cross-org list at `/boards`, with an org-multi-select FilterPanel sourced from the user's accessible orgs (use existing `ZerobiasClientApi.listMyOrgs` data) and a per-card scope-flavor indicator chip showing the board's owning org / engagement / project. User/Private boards (boards with no project parent, scoped to a single user) also surface here — these are a NEW board class not currently in the SME Mart codebase; plan-phase confirms whether `platform.Board` supports null `projectId` + a `userId` field today, and if not, files the gap to Kevin/Nic. (2) What helper tasks land on the seed default board? Brian-ask + Clark draft list. Director suggests: "Welcome to your engagement", "How to get help", "Add a corporate profile item", "Invite a team member" (linking to /org/profile, /docs, /members where they exist). (3) How does Vetting-as-Board affect the existing Vetting workflow (D-32/33 verbiage, vetting-circle component, any vetting-specific Task fields)? Need to inventory current Vetting tab implementation before migrating. (4) Should boards have an explicit "kind" enum (default / vetting / custom / workspace) or rely on `sme-mart.board.*` tags? Per `platform.Board` schema today — `boardType` is a free-form string, and tags are the marketplace-extension pattern (D-50). Prefer tags. **Scope blocks (rough):** (a) Tab label rename + route audit (`tasks` → `boards` on engagement?). (b) Card grid with pin (inline expand-in-place via CSS Grid + `grid-column: span 2`, paged ≤25 task preview, localStorage+PKV pin state) AND drill (navigate) affordances per card (extends or wraps BACKLOG-100 component). (c) `+ Create Board` button + create-board dialog. (d) **Board detail page** at top-level `/boards/:boardId` — cog/config side panel with inline name/description edit + PKV defaults; `zb-remote-table` infinite-scroll tasks list mirroring zb/ui's tasks view; triple-dot menu with admin-gated "Open in ZB Platform" link. (e) **Cross-org `/boards` LIST page** — new top-level surface (NOT a sub-route) using the SAME `app-board-card` + `app-board-list-tab` shared components, with org-multi-select FilterPanel + per-card scope-flavor indicator chip (org / engagement / project name) + User/Private boards section. Verify `platform.Board.search(.., orgIds: UUID[])` filter capability at plan-phase. (f) Provisioner seed-tasks extension (new helper-task templates + `Task.create` calls in provisioner). (g) Vetting tab → Vetting Board migration (data migration for existing Vetting items, UI consolidation). (h) Update DECISIONS.md with new tier-tag conventions if introducing `sme-mart.board.*` namespace. (i) PKV write/read plumbing for board preferences — depends on [[BACKLOG-099]] (PKV spike) status. **Prerequisites:** BACKLOG-100 lands first (shared component architecture + board switcher widget on detail page). This entry refines that work; do them in the same milestone. [[BACKLOG-099]] PKV spike landing helps (board defaults + pin state) but pin state can fall back to localStorage-only if PKV not ready. **Capture before plan-phase:** `.planning/notes/zb-ui-tasks-list-reference.md` (column shapes + row actions from zb/ui tasks view to mirror on the board-detail tasks table). **Plan-phase + execute-phase reminders (NON-NEGOTIABLE — paste into every gsd-plan / gsd-execute handoff):** (a) Invoke the **`sme-mart-architect`** skill (and/or **`/angular-architect`** for general Angular 21 questions) before drafting component contracts — this surface is rich (shared component, signal-based inputs, infinite-scroll remote-table integration, PKV plumbing, in-place layout state) and needs architectural rigor on standalone-component decomposition, signal-input shape, and ngx-library reuse. (b) Apply **all Angular 21 modernization rules** verbatim from [`.planning/docs/MODERNIZATION_GUIDE.md`](../docs/MODERNIZATION_GUIDE.md) on every new + touched file: `input()`/`output()`/`inject()`/`signal()`/`computed()`/`effect()`; no constructor injection; no `@Input`/`@Output`; standalone components; `ChangeDetectionStrategy.OnPush`; no `: any` (narrow types); no `@ViewChild` (use `viewChild()`); template `*ngIf`/`*ngFor`/`*ngSwitch` → `@if`/`@for`/`@switch`; no empty `catch {}` blocks; touch-it=fix-it on every modified file (pre-commit lint is diff-based `--max-warnings=0`). (c) Reuse **ngx-library** primitives before building custom — `zb-remote-table` (search + filter already wired), `zb-simple-panel`, `zb-button-label`, `zb-dialog`, `zb-resource-status`, `zb-chip-colors-directive`, theme tokens (`--zb-background-card`, `--zb-text`, `--mat-sys-*`); never hardcode colors (see [[BACKLOG-103]] for what that costs). (d) Shared-component design: the `app-board-list-tab` (from #100) and any new `app-board-card` / `app-board-detail` components must be **dependency-free of SME-Mart-specific services** so the Project > Boards tab (and eventually ngx-library hoisting) can reuse without rewrite. **Anti-pattern:** doing 106 standalone without the 100 substrate — would duplicate the card-list + data-fetch code that 100 is meant to centralize. **Files likely touched:** `engagement-detail.component.{ts,html}` (tab label + route), `project-detail.component.{ts,html}` (Project > Boards tab will adopt the same shared component — likely replaces existing `project-boards-tab.component.ts` from commit `28fe83b`), new `app-board-list-tab` (per 100) extended with pin + drill affordances, new `app-board-card` (shared, supports both summary face + expanded-with-table face via signal-input state), new `create-board-dialog.component`, new `board-detail.component.{ts,html,scss}` at top-level `/boards/:boardId` (cog/config panel + `zb-remote-table` tasks list + actions menu + board switcher widget from #100), new top-level `boards-list.component.{ts,html,scss}` at `/boards` (cross-org list with org-multi-select FilterPanel + scope-flavor chips + User/Private boards section, also `zb-remote-table`-driven), `platform-engagement-provisioner.service.ts` (seed tasks), existing `vetting-tab.component.*` (migrate to board), `app.routes.ts` (route rename + new top-level `boards/:boardId` + `boards` routes), a PKV helper service (if not landed via [[BACKLOG-099]]). **Effort:** layered on top of BACKLOG-100's 12-18 hrs; additional ~20-30 hrs for pin (localStorage+PKV inline-expand) + Create Board button + Board-detail page (cog panel + remote-table infinite scroll + actions menu) + cross-org `/boards` list page (multi-org filter + scope-flavor chips + User/Private boards) + seed tasks + Vetting migration. | +20-30 hrs on top of #100 | Clark 2026-05-18 during BACKLOG-103 walkthrough |
| **113** | Boards-tab: group by owning project + relationship chips + scope-include selector | **From zb/ui mock parity 2026-05-22** (see `.planning/notes/zb-ui-boards-tab-reference.md`; mocks `s5-boards-tab-composite.html` + screenshot). Refines [[BACKLOG-106]] card-grid. Group boards under their owning project with a group header (project dot + name + **relationship chip**: `This project` / `child` / `resource-linked`) — replaces a single TOP/SUB badge with three relationship types. Multi-scope **include selector** ("Include boards from: this project + child + linked"); visibility membership-gated. Grouping is **context-aware** — engagement boards, project boards, and the cross-org `/boards` list each group by whatever's useful. Use real ngx-library chips + theme tokens (mock markup is reference only). | ~8-12 hrs | Clark zb/ui mock review 2026-05-22 |
| **114** | Pinned-open board embeds `zb-remote-table` (projection + Create Task, infinite scroll) | **From zb/ui mock parity 2026-05-22.** Supersedes the Phase 32 `BoardCardPinnedPreviewComponent` count-only stub (hardcoded `pinnedTaskCount=0`). A pinned/expanded board renders an embedded **`zb-remote-table`** of its tasks WITH the table's projection area — search/filter + action buttons — so **+ Create Task is reachable directly from the pinned open board**; infinite scroll. Backed by `platform.Board.listTasks(boardId)`; column shape per [[zb-ui-tasks-list-reference]] + the boards-tab reference note. Refines [[BACKLOG-106]] scope block (b). | ~10-16 hrs | Clark zb/ui mock review 2026-05-22 |
| **115** | Resizable pinned boards (drag handles for height/width) | **From Clark 2026-05-22.** Let the user set how tall/wide a pinned/expanded board is via drag handles, so they decide how much of the embedded task table to see inline. Builds on [[BACKLOG-114]] (embedded table) + [[BACKLOG-106]]. **Open questions:** (1) **Persistence model** — auto-persist per-board dimensions to user prefs / **PKV** (reuse the PKV-primary prefs path already built, like pin state) OR an explicit **"save this layout" button** that captures the current arrangement on demand? (2) Persistence scope — per-board, per-context (engagement vs project vs cross-org view), per-user? (3) Does a saved layout capture only dimensions, or also the pin set + card ordering? (4) Sensible min/max + reset-to-default. | ~6-10 hrs (depends on persistence choice) | Clark zb/ui mock review 2026-05-22 |
| **116** | Standard entity-detail tab set: Overview / Boards / Tasks / Members / Hierarchy | **From zb/ui Projects mock 2026-05-22** (`s5`/`s4` composites). Adopt the zb/ui Projects detail tab pattern — **Overview · Boards · Tasks · Members · Hierarchy** — for SME Mart **Project and Engagement** detail pages so both mirror the platform shape (eases future ngx-library/platform hoist). Cross-cutting: touches `engagement-detail.component` tabs (today: overview/projects/documents/boards/vetting/timeline/notes — reconcile against the standard set) and the future Project-detail surface. Audit current tabs before changing. | ~6-10 hrs | Clark zb/ui mock review 2026-05-22 |
| **111** | Engagement template library + template-parameterization wizard | **STUB — filed 2026-05-22 by Director (dangling ref from [[BACKLOG-106]]/[[BACKLOG-108]] finally stubbed); expand at plan-phase.** From Brian's 2026-05-19 marketplace meeting ("engagement TEMPLATE LIBRARY wanted") + Clark's wizard idea (parkit-14). **Concept:** a library of reusable engagement templates that parameterize how a new engagement is provisioned — which Vetting Board layout it gets (single Board / section-split / direction-split / full-split / depth-4 section sub-Projects per BACKLOG-108 Option 5-prime), which seed boards + helper tasks land (BACKLOG-106 part f), and which Requirement sections apply. Driven by a **Q&A wizard** at engagement-creation time (Clark's example: "does Finance require access controls to banking info?" → promotes the banking section to its own depth-4 sub-Project). Output: a structured template descriptor consumed by `platform-engagement-provisioner.service.ts`. **Compass:** C-7 — template instantiation must record `template_id` + `template_version` + ontology source on every provisioned Project / Board / section for provenance (`.planning/docs/RDF-COMPASS.md`). **Unblocks:** the template-driven layout escape hatches in BACKLOG-108; without it every engagement gets the default single Vetting Board. **Prerequisites:** BACKLOG-108 LOCKED shape (done); BACKLOG-106 provisioner seed-task work; [[BACKLOG-112]] ontology spike informs the section taxonomy. **Target:** v1.6+ (after Phase 33 Vetting migration). | TBD — expand at plan-phase | Brian 2026-05-19 meeting + Clark wizard idea; ref'd by 106/108 |
| **112** | Industry-ontology research spike (W3C commerce/credentials, FIBO banking) | **STUB — filed 2026-05-22 by Director (dangling ref from [[BACKLOG-106]]/[[BACKLOG-108]] finally stubbed); expand at plan-phase.** From Brian's 2026-05-19 marketplace meeting (research spike to ground SME Mart's Requirement/Vetting taxonomy in real-world ontologies). **Concept:** a time-boxed research spike surveying existing industry/domain ontologies that SME Mart's Engagement / Requirement / Vetting-section vocabulary should map onto, so the eventual RDF/OWL/SHACL serialization ([[BACKLOG-109]] compass) aligns with standards rather than inventing bespoke terms. **Candidate sources:** W3C commerce + Verifiable Credentials (VC Data Model); **FIBO** (Financial Industry Business Ontology — for banking/financial Vetting sections); schema.org Organization/Service; OSCAL control/assessment vocabulary (already in the Holon handoff). **Output:** a one-pager mapping SME Mart's section taxonomy (banking, corporate identity, D&B, financial, MSA, background checks) to standard ontology terms + a recommendation on which to adopt as canonical. **Compass:** feeds C-7 (ontology-source provenance) + BACKLOG-109 ZB↔W3C vocabulary mapping. **Unblocks:** [[BACKLOG-111]]'s template section taxonomy + the RDF-COMPASS mapping table. **Not urgent** per Brian ("build scaffolding first"). **Target:** v1.6+ strategic research. | ~4-6 hrs spike | Brian 2026-05-19 meeting industry-ontology directive; ref'd by 108/111 |
| **117** | Members tab on Engagement + Project detail (per zb/ui s6) | **STUB — filed 2026-05-22 by Director (zb/ui mock review s6 + zb/ui Claude confirm + transparency handoff §6.4); expand at plan-phase.** Adopt the s6 Members tab pattern on engagement-detail + project-detail. **Maps to existing platform contract** — Phase 21 Project Scoped Access Rules (`platform.Project.addMember` / `listMembers` / `removeMember` with `roleIds[]`) + RoleScope=Project (Nic-confirmed). No new platform work needed. **Mock shape:** columns MEMBER (avatar + name + optional LEAD chip) / TYPE (User/Group/Service Account) / ROLE(S) (multi-chip from platform role catalog) / ORG / ACCESS (INTERNAL/EXTERNAL derived from `member.orgId` vs `project.ownerId`) / ADDED + row overflow + `+ Add Member` + search. The mock's specific role labels (Owner/Editor/Viewer/Reviewer/Contributor) are placeholder chrome per zb/ui Claude — render the live platform role catalog. **Open questions for plan-phase:** (1) Group member-count aggregate — SDK-provided or lazy-fetch? (2) INTERNAL/EXTERNAL chip — reuse `zb-resource-status` or new variant? (3) Aligns with [[BACKLOG-116]] standard 5-tab adoption. **Files:** new shared `app-members-tab` (engagement + project + future project-tier all reuse). **Target:** v1.5. | ~10-14 hrs | zb/ui mock review 2026-05-22 (s6) |
| **118** | Hierarchy tab — Tree/Graph toggle + tier chips + Lateral-link side panel (per zb/ui s7) | **STUB — filed 2026-05-22 by Director (zb/ui mock review s7 + zb/ui Claude confirm + transparency handoff §5.2); expand at plan-phase.** Adopt the s7 Hierarchy tab pattern. **Three load-bearing pieces:** (a) family summary header ("[Entity] + N projects · X% · Y on-track / Z at-risk / W blocked · View tasks across family →"); (b) Tree / Graph view toggle (Tree default; Graph is BIDIR-labelled in mock); (c) Lateral-link side panel rendering Project↔Project resource links separately from the parent/child tree. **Lateral vocab is platform-fact** per handoff §5.2: `depends_on / relates_to / blocked_by / supersedes / derives_from / requires` — existing `ResourceLinkType`s. **NOT gated on `satisfies`/`satisfiedBy`** (those are Task↔Task, different concern). Env-specific link-type UUIDs via ZB MCP — do NOT hardcode. **RDF Compass:** directly satisfies C-2 (hierarchy ≠ lateral made visible). **Open questions for plan-phase:** (1) per-node tier label depends on [[BACKLOG-119]] (tier-chip primitive); (2) Graph rendering — ngx-library has no graph primitive; evaluate `vis-network` / `cytoscape` / SVG-by-hand; (3) family rollup — client-side aggregation or platform-team rollup endpoint ask? (4) "View tasks across family" routes to a cross-project task view ([[BACKLOG-113]] scope selector). **Files:** new shared `app-hierarchy-tab` (recursive `platform.Project.list` + `hydra.Resource.listResourceLinks` for laterals). **Dependencies:** [[BACKLOG-119]] for tier chips; [[BACKLOG-116]] standard tab adoption. **Target:** v1.5/v1.6. | ~14-20 hrs | zb/ui mock review 2026-05-22 (s7) + handoff §5.2 |
| **119** | Reusable tier chip primitive for Project entities | **STUB — filed 2026-05-22 by Director (zb/ui mock review s3/s7 + transparency handoff §4.3); expand at plan-phase.** Standalone Angular component rendering a tier chip beside any `platform.Project` name surface (engagement-detail title, project-detail title, hierarchy-tab node, board-detail breadcrumb). **Canonical tier ladder** (D-52, [[project_sme_mart_hierarchy_model]]): `project / workspace / aperture / thread` (depth-0 = project), then Task / Sub-Task. **Engagement and Program are node-ROLE tags, NOT tiers** — the chip should render the **tier** label and, separately, an optional **node-role** badge (engagement / program). Flexible middle (workspace/aperture/thread renameable per [[BACKLOG-111]] wizard / platform Hierarchy Editor [PLANNED]). **Component contract:** `<app-tier-chip [tier]="...">` — the label string is whatever the resolved tier source returns, NOT a hard-coded enum. Component just styles (color per depth, theme-token-backed). **Open standup question (tracked, NOT blocking the chip itself):** where the tier-label DATA lives — `Project.tagId` as canonical tier-carrier vs reserved hydra-tag namespace (analog to `sme-mart.tier.*`). Pending Brian/Kevin/Nic (zb/ui memex `tier-as-tag-convention-open-question-for-standup`). Until decided, the chip consumer derives the label via whichever source wins; chip is pure render. **Anti-pattern:** hard-coding tier strings (WORKSPACE/APERTURE/THREAD) or conflating node-roles (engagement/program) with tiers — defeats per-template flexibility and the D-52 two-axis model. **Files:** `shared/components/tier-chip/`; tier→color map theme-token-backed; small resolver helper (interchangeable strategy, swap when standup resolves). **Dependencies:** none — cheapest of the 5; should land early. **Target:** v1.5. | ~4-6 hrs | zb/ui mock review 2026-05-22 (s3/s7) + handoff §4.3 |
| **120** | Engagement/Project Overview tab enrichment — Tags + Resource Links + Family Rollup + inline previews (per zb/ui s3) | **STUB — filed 2026-05-22 by Director (zb/ui mock review s3); expand at plan-phase.** Enrich the Overview tab on engagement-detail + project-detail to match the s3 mock's right-column Details sidebar + inline previews. **Five components (all already-have primitives):** (a) Tags panel listing all hydra Tags on the Project + `+` add; (b) Resource Links panel listing `hydra.Resource.listResourceLinks(projectId)` grouped by link type; (c) Family Rollup card ("[Engagement] + N children · X% combined · Y tasks · Z on-track / W at-risk / Q blocked · View tasks across family →"); (d) inline Boards preview (top-3 + "View all →"); (e) inline Members preview (top-5 + "View all →"). **Open questions for plan-phase:** (1) Family Rollup aggregation cost — client-side over recursive `Task.list` may be heavy; evaluate batching / Kevin ask for server-side rollup; (2) inline previews — share components with full-tab versions in `compactPreview` signal mode, or condensed siblings? **RDF Compass:** Tags + Resource Links surfaces help satisfy C-2 (typed predicates visible). **Files:** existing engagement-detail Overview template + small additive sections; future project-detail Overview. **Dependencies:** [[BACKLOG-117]] (Members) for the preview row model; [[BACKLOG-119]] (tier chip) if cards render tier chips. **Target:** v1.5. | ~10-14 hrs | zb/ui mock review 2026-05-22 (s3) |
| **123** | Project-scoped Requirements feature (3-class schema + org library + project targeting) — DEFERRED PENDING KEVIN | **STUB — DEFERRED 2026-05-27 by Director.** Surfaced 2026-05-27 from Brian's huddle (Requirements as first-class, before-tasks pipeline) + ui-meta-director coordination prompt asking for a single `Requirement` GQL class + Clark's expanded vision (org-level catalog/library + per-project framework targeting + backend-generated Boards/Tasks tree). **Scope expanded past what a single class can carry** — needs 3 classes (`OrgFrameworkAdoption`, `ProjectFrameworkTarget`, `ProjectRequirement`) plus naming-collision resolution against existing platform `BoundaryRequirement` (Director recommendation: `ProjectRequirement` not `Requirement` for scope-explicit naming). **Full architectural capture lives at [`.planning/notes/requirements-architecture-2026-05-27-pending-kevin.md`](notes/requirements-architecture-2026-05-27-pending-kevin.md)** — preserves the 3-layer model, proposed schema shapes with field-shape adjustments (camelCase, ALL_CAPS enums, scalar-not-link for platform-native entities, drop redundant created_at/updated_at), carrier-rule application, Brian-directive friction (OrgProfileItem-as-catalog-carrier conflicts with queryability need), platform context (Standard/Control/BoundaryRequirement legacy), and 7 open questions for Kevin/Daniel/Nic. **Why deferred:** needs Kevin alignment on (a) whether `platform.StandardResource` already provides org-adopts-framework semantics (might not need OrgFrameworkAdoption at all), (b) custom-framework definition storage (new class vs MarketplaceProfileItem extension per Brian directive), (c) BoundaryRequirement migration plan (legacy under Brian's Boundary=security-only reframe), (d) global-catalog-app integration shape, (e) backend orchestration ownership for framework→Boards/Tasks expansion. **Out of scope for 2026-05-27 schema PR** (narrowed back to ResourceMetadata only). **When this resumes:** read the capture doc first; verify platform schema state hasn't changed; re-check zb/ui Projects App INTENT.md §2.8 for evolved consumption pattern; re-check Brian directive accumulation since 2026-05-27. **Dependencies:** Kevin alignment on questions in capture doc; Nic registration of `satisfies`/`satisfiedBy` hydra link types (BACKLOG-108 / zb/ui INTENT §5 Q6). **Related:** ResourceMetadata mechanism (separate, simpler, on the active schema PR); BACKLOG-117 (Members tab — assignee binding side); BACKLOG-126 (NICE NIST role binding — surfaced from huddle but not yet filed). **Target:** v1.6+ (after Kevin/Daniel input). **Files (eventual):** 3 class YAMLs + 15-20 field YAMLs + 3-5 enum YAMLs in `~/Projects/w3geekery/zb-forks/org/schema/package/w3geekery/smemart/`; backend orchestration service in SME Mart app (or platform-side if Kevin takes ownership). | Schema ~12-18 hrs + backend orchestration TBD | ui-meta-director coordination 2026-05-27 + Clark expanded vision 2026-05-27 + Brian 2026-05-27 huddle |
| **122** | ~~Notes carrier evaluation~~ — CLOSED 2026-05-27: transferred to zb/ui Projects App scope; no SME Mart migration needed (test-data only) | **CLOSED / TRANSFERRED 2026-05-27.** Originally filed earlier same day as a FileService-vs-GQL spike. Resolution arrived in three steps: (1) Kevin confirmed FileService is in-bounds for `.md` Notes (Slack 12:44-12:50 PT — "yes, you are in bounds / that is why we use it for tasks / attachments, etc / every File and Folder is a resource"). (2) ui-meta-director (zb/ui Claude) accepted Notes-feature ownership transfer; canonical implementation now lives in zb/ui Projects App scope per their Tell-block 2026-05-27. (3) Clark confirmed existing SME Mart Notes data is test-only — no migration script needed. **What this means for SME Mart:** sunset `/engagements/:id/notes` routes + `notes-tab.component.*` + the Notes/Notebook/Folder shared components alongside the broader Engagement-detail sunset when Platform Projects App ships its Notes tab. NO new Notes investment in SME Mart. NO migration work. **Where the canonical lives now:** zb/ui Projects App, FileService-backed (Folders + `.md` files as hydra Resources), tracked in zb-dx scope. Director Parks watch-points: confirm Notes lands as a tab in their INTENT.md tab strip; ensure the 5 SME Mart value-adds (markdown-editor task-status plugin, note→Task injection, sharing, meeting-minutes first-class, AccessLevel three-tier) survive into their plan. **Provenance:** Kevin Slack 2026-05-27 12:44-12:50 PT; ui-meta-director Tell-block reply same-day. **Carrier rule that emerged from this decision** (broader than Notes — captured in memex [[carrier-rule-cardinality-query-shape-not-feature-category]]): cardinality + query-shape, not feature category, decides DB vs FileService. | 0 hrs SME Mart-side (sunset only; covered by broader Engagement-detail teardown work) | Kevin Slack 2026-05-27 + ui-meta-director ownership transfer same-day |
| **121** | Portfolio multi-lens entry point (Wheel/List/Tree/Timeline) — WAIT-LISTED on zb-dx INTENT.md | **STUB — filed 2026-05-22 by Director (zb/ui mock review s1/s8); expand at plan-phase. WAIT-LISTED on zb-dx `INTENT.md` for the Wheel data contract.** zb/ui s1/s8 mocks introduce a top-level Portfolio page with a lens row: Wheel | List | Tree | Timeline — same dataset, four renders. **List lens** (s1): grid of `platform.Project` w/ columns NAME / OWNER / BOUNDARY / STATUS / VISIBILITY / BOARDS / MEMBERS / UPDATED. **Wheel lens** (s8, Bob Cheek's design, Plan/Audit mode toggle): donut/orbital status viz, focus-on-blocked for audit mode; Vendor Audit 2026 inner card + Focus Metrics tile + linked-projects orbit. **Tree lens:** `parentId` walk (similar to s7 Hierarchy Tree view but at portfolio level). **Timeline lens:** Gantt-like over Task `due` / Project `target` ([PLANNED] platform field per handoff). **Why WAIT-LISTED on Wheel:** wheel positional ordering rule, Focus Metrics tile set, health-pill derivation, Drift / event-log dependency are not yet specified. zb/ui Claude is authoring `INTENT.md` in zb-dx (`~/Projects/zb/zerobias-org/zb-dx/architecture/`) to capture those contracts. SME Mart Wheel adoption waits on it. List/Tree/Timeline lenses can move forward earlier if needed. **Open questions:** (1) Portfolio routing — top-level `/portfolio`, inside engagement-detail as a lens row, or separate cross-engagement landing? zb/ui Dashboard vs Portfolio split (see zb/ui memex `dashboard-vs-portfolio-two-distinct-tabs-in-the-projects-app`); SME Mart's analog is engagement-detail Overview vs hypothetical cross-engagement portfolio. (2) Audit mode is compliance-platform vocabulary — adopt as-is or relabel? (3) Plan/Audit toggle interaction with existing engagement-status filters. **Target:** v1.6+ strategic. | ~20-30 hrs total across 4 lenses (Wheel TBD pending contract) | zb/ui mock review 2026-05-22 (s1 + s8) |
| **089** | zbb Hub Module Local Stack — **re-evaluate need first** | **Deferred from Phase 19 (2026-04-16). Director analysis 2026-04-17 suggests this may not be needed at all — READ BEFORE PROMOTING.** Original framing: extend Phase 19 infrastructure with local Hub module hosting (Verdaccio + hub-server + postgres) to iterate on SME Mart's custom Hub Module. **Architecture research finding (2026-04-17):** Hub is already a published zbb stack (`@zerobias-com/hub`) with ghcr.io images (hub-server, hub-events, hub-pkg-proxy), depends on `@zerobias-com/dana-stack`. `hub-pkg-proxy` is the proper module-publishing target, not plain Verdaccio. **Director analysis — do we need a custom Hub Module at all?** Answer: **probably no.** SME Mart is Angular 21 static export with no server routes. Data access is already served by existing platform hub modules: GQL reads, `Pipeline.receive` writes, `hydra.Tag`, `hydra.Resource`, File SDK, Dana auth. Remaining Neon-direct usage (7 tables per memory) is being migrated to Pipeline+GQL schemas. **Problems a custom Hub Module would solve + sanity check:** (1) Server-side authorization — real gap today, but Brian's v1.4 task-gated boundary API vision solves it upstream; building custom Hub Module solves it twice, probably wrong. (2) Secrets out of browser (Neon connection string, errata 015) — real, but the fix is completing the Pipeline+GQL migration, not adding a Hub Module. (3) FormSubmission server-side validation — real gap (Phase 16 Pitfall #3), but not uniquely a Hub Module's job (GQL resolver extension or platform-service module also work). (4) Domain-aggregated API — GQL already does this; if it can't, enrich the schema. (5) Long-running/batch processes — none in v1.3/v1.4 scope. (6) External-service integrations with secrets — none in SME Mart scope. (7) Centralized audit — not a stated SME Mart requirement. **Net:** One real problem (authorization) is being solved upstream. Every other problem is either already-solved, not-in-scope, or has a better-positioned solution. A custom Hub Module would be a solution looking for a problem. **Outstanding Kevin questions** (if 089 ever activates): (a) Is `hub-pkg-proxy` the right publish target for custom modules? (b) Is the W3Geekery custom Hub Module publishing pipeline working on UAT/prod (stale Feb-2026 note says it was broken)? **If activated, scope becomes:** re-evaluate whether SME Mart ever needs a custom Hub Module given Pipeline+GQL direction, likely archive. Otherwise, scope becomes consuming `@zerobias-com/hub` zbb stack + dana-stack + publishing via hub-pkg-proxy (not custom Verdaccio). **Not blocking anything.** Phase 19 v2 ships without this. | ~4-6 hrs IF activated; likely archive | Phase 19 scope reduction + Director analysis 2026-04-17 |

## Partially Complete Plans

| # | Plan | What's Done | What Remains |
|---|------|-------------|--------------|
| **054** | RFP Package Builder | v1.2 Phases 13 (pilot) + 14 (invitations) code complete. Phase 14 UAT blocked on GQL schema availability. Phase 15 research complete (template variable syntax design doc). | Phases 15-17 (templates, form builder, demo scripts). S2 deferred to v1.3. |
| **034** | GQL Schema Migration | Phases 1–4 done. Schema live in prod (17 classes), receiver pipeline created. | Phase 5: service layer. |
| **022** | Engagement → Project UI Restructuring | Phase 1 done (project shell + routes + My Projects). | Remaining phases deferred — project detail UI is Project App territory. |

## Deferred — Platform Project App (Kevin's Team)

These depend on platform Task/Board/Boundary work. UI implementation deferred until the platform Project app takes shape.

| # | Plan | Notes |
|---|------|-------|
| **057** | SmeBoard/SmeActivity/SmeWorkflow (Project Bloom MVP) | Schema entities exist (PR #8). UI deferred. |
| **058** | Saved Task Views & Board Management | Depends on 057 + platform Board. |
| **064** | Project Members View | Scoped roles, boundary membership. **Update 2026-04-13:** must render multi-engagement origins per party — each row shows "from Engagement X". Supports CE1 cross-engagement model. |
| **067** | Project Schedule View (Gantt/Calendar) | Depends on platform milestones/tasks. |
| **068** | Project Financials | Budget/billing in project context. |
| **069** | Compliance Framework Linkage | Task→control mapping depends on platform Board/Task. |
| **070** | Project Reviews / Retrospectives | PM retrospectives in project context. |
| **071** | Transparency **Entangled Tasks** (Task Pairs) | **Renamed 2026-04-13 per Brian CEO notes** — was "Entangled Task Pairs". UI labels, docs, API field names all use "Entangled Tasks" or "Task Pairs". Option B selected (design done). Implementation depends on platform Task/Board + entangled link types. |
| **072** | Task Queuing & Shift Handoff | Platform task system dependency. Brian 2026-03-24. |
| **073** | Agentic Memory Capture in Tasks | Platform task system dependency. Brian 2026-03-24. |
| **074** | Dual-Party GSD / SDD Toolkit | Depends on 071 + platform + Claude Agent SDK. Brian 2026-03-24. |

## Fire-and-Forget Remediation Polish (v1.5)

**Purpose:** Phase 20 Wave 2 remediated all 42 fire-and-forget sites (33 CRITICAL + 9 MEDIUM) with the SIMPLE pattern: `await` + `try/catch` + `MatSnackBar` toast + explicit `callSiteTag` + re-throw. Sites needing MORE than the simple pattern (form-button-disable, retry queues, batch rollback semantics) got the simple fix in Wave 2 PLUS a polish entry below for v1.5 follow-up.

**Closed:** Wave 2 site coverage lives in `.planning/phases/20-fire-and-forget-audit/AUDIT.md` (rows 1-42) and across commits `d64d11d`, `a1321a0`, `8c2236c`, `2a66bc2`, `13415a5`, `8f50a66`, `6f0e58b`, `9418936`, `c8210ed`, `769bfde`, `b2e014c`, `150df9a`, `eb228ce`, `672024e`, `7422387`. No sites remain unremediated at the SIMPLE level.

**Polish entries (v1.5 follow-up — non-blocking nice-to-haves):**

| # | Site | Current state (SIMPLE pattern, Wave 2) | Polish goal (v1.5) | Est. |
|---|---|---|---|---|
| FF-POLISH-1 | `bids.service.ts:368` (Bid submit / save draft) | `await` + snackbar on rejection + re-throw. Submit button has no in-flight disabled state. | Disable submit + spinner while in-flight. On error, surface inline form error with retry CTA, keep form data intact, allow user to retry without re-typing. Bid submit is core marketplace flow — generic toast undersells the failure. | 3-4 hrs |
| FF-POLISH-2 | `vetting.service.ts:184` (initializeVetting batch) | `await pushEntities` + snackbar on batch rejection. Batch is atomic — partial-success state isn't surfaced. | Per-item error handling: if 7/10 items succeed and 3 fail, surface the 3 failures individually with retry, keep the 7 successes. Optimistic UI rollback for failed items only. Coordinate with platform on Pipeline.receive partial-success semantics OR fall back to per-item sequential writes for better error granularity. | 4-6 hrs |
| FF-POLISH-3 | Submit-button-disable sweep (cross-cutting) | Form-driven actions across reviews / engagements / service-offerings / rfp-invitation / project-prd / project-plan / sme-mart-task have `await` + snackbar but no in-flight disabled state on submit buttons. Rapid double-clicks can fire duplicate Pipeline writes. | App-wide pattern: shared form directive or signal-based `isSubmitting` state on form components. Disable submit + show spinner while in-flight; re-enable on success/error. Reuse a single helper across all marketplace forms. | 4-6 hrs |

**Severity:** v1.5 nice-to-have. SIMPLE pattern from Wave 2 already prevents silent failures (toast surfaces every error + re-throws so callers can react). Polish improves UX (button state, batch granularity, retry affordance) but doesn't change the data-integrity story.

**Related:** Errata 011, Errata 023, Phase 20 AUDIT.md, `.planning/phases/20-fire-and-forget-audit/AUDIT.md`.

---

## Cross-Engagement Multi-3PAO Audit Model — ⚠️ RETIRED 2026-06-01 (CE1–CE14 dissolved)

> **The CE1–CE14 cluster is RETIRED as active SME-Mart backlog.** Two forces dissolved it: (1) the **governance-node model (DECISIONS D-52 / RL-001)** absorbed its structural mechanics into platform primitives; (2) the **2026-05-27 scope-reduction pivot** (`director/sme-mart-scope-reduction-pivot-2026-05-27.md`) moved all engagement-execution/transparency chrome OUT of SME Mart and INTO the Platform Projects App. The CE numbering predates both and is obsolete. **Nothing CE-numbered remains active.** What was durable survives in platform primitives, the Projects App, or standing memex (the transparency invariant). Full historical origin: the [research doc](.planning/research/external/2026-04-13-ceo-miro-cross-engagement-audit-model.md). The detail table below is **HISTORICAL — retained for context only, not active work.**

**Disposition map — (a) absorbed → governs/Program/tier-ladder/resource-links · (b) moved → Platform Projects App:**

| CE | Title | Disposition | Lands as |
|----|-------|-------------|----------|
| CE1 | Home + Linked Engagement | **(a) absorbed** | `governs` 0..N ResourceLinks (RL-001 multi-org: one Program governed by N Engagements) — replaces `homeEngagementId`/`linkedEngagements[]` |
| CE2 | *(superseded by CE6)* | — | Already dead pre-retirement |
| CE3 | Multi-3PAO Scope-Partitioned Audit | **(b) moved** | Projects App governance/scope (governs links are the primitive; scope/publish execution is theirs) |
| CE4 | Task Entanglement (N-party) | **(b) moved** | Projects App task-pair execution. The transparency invariant itself survives as a standing memex, not a CE item |
| CE5 | Protocol Gateway — Task-Level Grants | **(b) moved** | Projects App / platform permission enforcement |
| CE6 | Publish-to-Shared + Anonymity | **(b) moved** | Projects App transparency center |
| CE7 | Sub-Project Hierarchy (`parentProjectId`) | **(a) absorbed** | Tier ladder + `parentId` nesting (D-52) |
| CE8 | Cybersecurity SLA Contract Template | **(b) moved** | Projects App engagement-level legal/MSA (pivot §3). Catalog-product flavor may resurface as a *matchmaking* concern if Brian asks |
| CE9 | Nested Transparency Centers | **(b) moved** | Projects App |
| CE10 | Typed Project Relations (flat graph) | **(a) absorbed** | Platform hydra ResourceLinks (lateral `depends_on`/`relates_to`/etc.) — already exist |
| CE11 | Append-Only Activity Log | **(b) moved** | Projects App / platform activity-log infra |
| CE12 | Boundary Subset Enforcement (4-level) | **(b) moved** | Projects App / platform (Nic owns the boundary-subset chain — D-47) |
| CE13 | Within-Project Workspace (crew + aperture) | **(a) absorbed** | Tier ladder (`workspace`/`aperture` tiers) + tags; crew/membership execution is Projects App |
| CE14 | Portfolio (project wrapper under engagement) | **(a) absorbed** | The `program` node-role (coordination/fiscal-rollup root) — D-52 |

_Historical detail (pre-retirement) — From Brian's CEO notes + Miro board analysis. See [research doc](.planning/research/external/2026-04-13-ceo-miro-cross-engagement-audit-model.md). These reshaped the then-current two-party model into a cross-engagement network with selective disclosure and scope-partitioned publishing._

| # | Plan | Notes |
|---|------|-------|
| **CE1** | Home + Linked Engagement Project Model | **Confirmed 2026-04-14.** Asymmetric: project has ONE home (primary) engagement = Buyer↔Seller commerce, + N linked (secondary) engagements, typically Buyer↔Auditor aligned-with-Buyer. Linked engagements are **standing relationships** on retainer with engagement-level MSA/background/banking, but per-project commercial terms vary (not 1-contract-per-project). Seller consent to pulled-in auditors is **baked into the transparency-system agreement at primary engagement signing** — no per-request approval. Primary termination requires **mutual handshake**; linked-engagement termination auto-revokes that auditor across projects they were pulled into (cascade granularity TBD). Schema: `Project.homeEngagementId: UUID` + `Project.linkedEngagements: [{engagementId, pulledInByPartyId, scope, anonymityToggle}]`. **Foundational for CE3–CE9.** 15–20 hrs. |
| **CE2** | *(superseded by CE6)* | Originally "Selective Disclosure / Party Anonymity" as visibility gate. Reframed after screenshots — see CE6 Publish-to-Shared Pipeline. |
| **CE3** | Multi-3PAO Scope-Partitioned Audit | **Confirmed 2026-04-14.** Multiple 3PAOs linked via separate standing engagements, each with **Buyer-selected scope** — arbitrarily granular: whole-boundary, boundary-subset, AND intra-boundary partition (specific tasks/controls). Specialty-aligned task filter. Per Brian, specialties are "and, and, and, and" — healthcare vertical examples: clinical compliance, clinical engineering/HL7, cybersecurity (HIPAA/NIST/SOC2), AI governance. **Scope overlap explicitly supported** (Buyer "bake-off" second-opinion pattern). Publishing API-enforced to scope. **Conflict arbitration flows through the Cybersecurity SLA (CE8), not ad-hoc Buyer adjudication.** 3PAOs are "category #1" in the SME Mart directory. Depends on CE1 + CE5 + CE8. 20–28 hrs. |
| **CE4** | **Task Entanglement (N-party)** — formerly "Twinned Boundary Requirements" | **Updated 2026-04-15.** Every boundary requirement generates a Demand/Supply **entanglement** — not strictly a pair. Can be pair, trio, quartet, quintet, sextet... Minimum 2 parties (commerce axiom). Independent RAG status per party-role, party-scoped visibility. **This is the canonical opt-in data transparency mechanism** per the core invariant — the one and only seam for cross-party data. Entanglements have a lifecycle: evolve in-place (versioned as parties join) OR die-and-respawn on reassignment (rules tbd). Expands Plan 080; aligned with Plan 071 (Entangled Tasks). 14–22 hrs. |
| **CE5** | Protocol Gateway — Task-Level Grants | Permission enforcement at task/subtask granularity (not just boundary-level). Grant types: read, write, publish-to-shared, close-task. Default grant matrix per task-type template. Blocks CE3. 18–24 hrs. |
| **CE6** | Publish-to-Shared Pipeline + Anonymity | **Confirmed 2026-04-14.** Private 3PAO workspace → elevated findings published to shared transparency center. **Anonymity is a per-linked-engagement TOGGLE, Buyer-controlled** (not enforced platform-wide). Default/best-practice = anonymous (the "0-bias standard"). When on: mask org/logo/contact/principals; preserve role + specialty. Remediation flows THROUGH the transparency center (alerts + rights-to-cure 30/60/90d) — auditor identity stays anonymous through remediation; penalties/cure-windows/contractual-outs pre-defined in the Cybersecurity SLA (CE8). Replaces CE2's visibility-gate approach. Depends on CE1; integrates with CE8. 16–22 hrs. |
| **CE7** | Sub-Project Hierarchy (`parentProjectId`) | Add `SmeMartProject.parentProjectId` for structural work-breakdown. **ONE home engagement per project tree** — sub-projects inherit `engagementId` immutably. Cross-engagement participation for sub-projects handled via CE1 linked engagements on the root + scope narrowing per sub-project (extend `LinkedEngagementScope.subProjectIds: [UUID]`). Lateral project↔project relationships (`depends_on`, `relates_to`) continue to use platform resource links, not `parentProjectId`. Cascade: archive parent → archive children; delete blocked if children exist. Schema PR + 15-min reload. 6–8 hrs. |
| **CE8** | **Cybersecurity SLA — First-Class Contract Template** (NEW 2026-04-14) | Assessor-packaged continuous-monitoring contract: assessment logic + legal terms + rights-to-cure windows (30/60/90d) + penalty/out clauses. Buyer subscribes; terms are imposed into primary-engagement contract with Seller. **Authoritative for conflict arbitration** (scope overlap, threshold breaches). Likely a reusable SME Mart entity type published by 3PAOs, subscribed-to by Buyers, inserted into Engagement as SLA attachment. Depends on CE1 + CE3 + CE6. Source: Brian 2026-04-14 meeting. 18–24 hrs. |
| **CE9** | **Nested Transparency Centers** (NEW 2026-04-14) | Each project has its own transparency center. Linked projects create linked transparency centers. Buyer↔Auditor (linked engagement) may need a **secondary** transparency center that publishes into the **primary** transparency center. Brian's "inception" nesting concern — depth limit and rollup semantics unresolved. Touches CE1 (linked-engagement scope), CE6 (publish pipeline), and CE7 (sub-project hierarchy). Research-then-plan; scope estimate TBD after depth investigation. **Note:** CE11's 3-level activity log rollup may resolve most of this; re-evaluate after CE11. |
| **CE10** | **Typed Project Relations (flat graph)** (NEW 2026-04-15) | Add `project_relation` entity/edge for lateral project↔project links: `relates_to`, `depends_on`, `blocked_by`, `requires`, `supersedes`, `derives_from`. DAG semantics, validation layer prevents cycles on `depends_on`/`blocked_by`. Leverages ZB platform resource-link system. Complements (may deprecate) CE7's `parentProjectId`. Source: Multica `multica-flat-projects-with-relations` pattern. 8–12 hrs. |
| **CE11** | **Append-Only Activity Log with ZB Extensions** (NEW 2026-04-15) | New `ActivityLog` class: append-only, hash-chained (Merkle-style tamper evidence), with `boundary_set`, `engagement_id`, `portfolio_id`, `party_visible_to` fields. **Default private to its workspace** (confirmed Brian 2026-04-15); transparency propagation is **opt-in via linked task pairs (req↔sat)** — ties to CE4 Demand/Supply twins as the canonical opt-in mechanism. Rollup chain available when opted in: workspace → project → portfolio. Replaces concept-only references in CE6 + CE9. Source: Multica `multica-activity-log-pattern` + ZB multi-party extensions. 20–28 hrs. |
| **CE12** | **Boundary Subset Enforcement (4-level chain)** (NEW 2026-04-15) | Enforce `engagement.boundaries ⊇ portfolio.boundaries ⊇ project.boundaries ⊇ workspace.boundaries`. Each level can only tighten, never loosen. Middleware rejects violations at write time. Provides mathematical clarity for Brian's "deepest requirements auditing" goal — schema enforces scoping, not policy docs. Source: Multica `multica-boundaries-as-first-class` pattern + CE3 partial-scope language. 10–14 hrs. |
| **CE13** | **Within-Project Workspace (crew isolation + aperture)** (NEW 2026-04-15) | Net-new `Workspace` entity scoped to a project. Contains: members (humans + agents), skills, scoped activity log, boundary subset. **Aperture** is a **tag-based** attribute (hydra tags, malleable, user-customizable — confirmed Brian 2026-04-15 10:31 AM) — not a fixed enum; the 3PAO specialty taxonomy is a common seed set. **Workspaces are private + anonymous by DEFAULT** (confirmed Brian 2026-04-15). Transparency is opt-in via linked task pairs (req↔sat, ties to CE4 Demand/Supply twins). Workspace = "agent/human crew within a project." 3PAO audit crews are natural workspaces. Depends on CE10/CE11/CE12. 18–24 hrs. |
| **CE14** | **Portfolio (project wrapper under engagement)** (NEW 2026-04-15) | Net-new `ProjectPortfolio` entity wrapping N projects under **one engagement** (confirmed Brian 2026-04-15 10:28 AM — engagement is highest level, no cross-engagement portfolios "until someone asks"). Carries the multi-project transparency dashboard UX: projects sortable, workspaces drillable by aperture. Brian's "project portfolio / folio" term. Portfolio boundaries ⊆ Engagement boundaries (CE12). Nav sibling (no new entity for now): **Engagement Portfolio** — directory view sorting all engagements the party has access to ("just a directory structure" per Brian). Depends on CE1 + CE11 + CE12. 20–28 hrs. |

### Existing plans updated by this research

- **071** → renamed "Entangled Tasks" / "Task Pairs" (terminology change)
- **080** → expand to support twinned (Demand/Supply) boundary requirements (CE4)
- **078** → UI spec must cover Private workspace view, Publish-to-Shared dialog, RAG rollup per boundary type
- **064** → render multi-engagement origins per party
- **041** → add 3PAO capability declaration (specialty taxonomy: AI, Quantum encryption, IAM, cyber, agent audit, etc.)
- **056** → reframe "third-party facilitator role (D7)" as 3PAO/Auditor role detailed in CE1–CE6

### Resolved 2026-04-14 (Marketplace meeting w/ Brian — full answers in [research doc](.planning/research/external/2026-04-13-ceo-miro-cross-engagement-audit-model.md) + [meeting notes](.planning/notes/meetings/2026-04-14-marketplace.md))

1. ✅ Engagement ↔ Project cardinality → Asymmetric (primary + linked), not many-to-many
2. ✅ Seller consent → Baked into transparency-system agreement at Engagement 1, not per-request
3. ✅ Anonymity persistence → Auditor stays anonymous; remediation flows through transparency center via Cybersecurity SLA (see CE8)
4. ✅ 3PAO capability registry → Both — marketplace-curated directory + vendor-declared extensions; 3PAOs are "category #1"
5. ✅ Scope overlap arbitration → Buyer decides, authoritative via Cybersecurity SLA terms
6. ✅ 3PAO discovery → Both — catalog + BYO; BYO 3PAOs must register in SME Mart (private placement flow)

### New open questions (from 2026-04-14 meeting)

1. Nested transparency centers — depth limit before the model breaks? (drives CE9 scope)
2. Global/meta auditor role — does a regulator/board-level observer need cross-transparency-center visibility?
3. Cybersecurity SLA structure — reusable entity type vs free-form attachment (leaning entity type; drives CE8 schema)
4. Termination cascade granularity — auditor revoke global-per-buyer vs per-project?

### Resolved 2026-04-15 (Slack thread w/ Brian — workspace patterns)

Full analysis: [`.planning/research/internal/2026-04-15-workspace-patterns-for-sme-mart.md`](.planning/research/internal/2026-04-15-workspace-patterns-for-sme-mart.md)

1. ✅ Portfolio cross-engagement scope → **Single engagement only.** Engagement is the highest level; no cross-engagement spanning. Future concept: **Engagement Portfolio** = directory view of all engagements (nav only, not a new entity). Hierarchy: Engagement Portfolio → Engagement → Project Portfolio → Project → Workspace.
2. ✅ Aperture taxonomy → **Tag-based (malleable, user-customizable).** Uses hydra tags, not a fixed enum. 3PAO specialty list is a natural seed set.
3. ✅ Workspace activity privacy → **Default private + anonymous.** Transparency is **opt-in via linked task pairs (req↔sat)** — ties directly to CE4 Demand/Supply twins as the canonical opt-in mechanism.
4. ⏸ Workspace archive cascade → **Deferred** per Brian.

### New open questions (from 2026-04-15 11:43–11:49 AM — N-party task entanglement)

Brian: "May be more than a 'pair'. May be a trio, quartet: task entanglement." And: tasks can evolve or die-and-respawn on reassignment.

1. Entanglement cardinality growth — automatic when additional parties claim a req, or explicit Buyer invitation?
2. Entanglement lifecycle — which scenarios evolve-in-place vs die-and-respawn? What's the reassignment default?
3. Versioning semantics — when an entanglement evolves, does the old version stay queryable (audit history) or collapse into the new?
4. Role flexibility — always 1 Demand + N Supply, or can roles be more flexible (joint-buyer, multi-demand scenarios)?

## Automation (Claude `/schedule`)

Candidates for recurring scheduled tasks via Claude Code `/schedule` (cloud-hosted, runs on Anthropic infrastructure).

| Task | Frequency | What It Does |
|------|-----------|-------------|
| `#zb-poc-devs` Slack scan | Daily | Scan channel for new messages, flag items relevant to SME Mart |
| ZB dependency check | Weekly | Check `angular-client`, `ngx-library`, `data-utils` for new versions |
| `zb-poc-devs` repo scan | Daily | Check for new platform learnings, glossary updates, schema namespace changes |
| Schema PR status | On-demand | Poll open PRs on `zerobias-org/schema` for CI status |

## Future Concepts (Not Yet Planned)

| Item | Description | Source |
|------|-------------|--------|
| **040** | Project Bloom (AI Document Decomposition) | Accepted Bid → Engagement + Project → AI decomposes docs into typed task/subtask tree. Expand: assessment task templates (E2), deliverable templates (E4). |
| **042** | Project Plugin (MCP + Templates + Parsers) | Bundled plugin: MCP skills, document parsers, task type templates, questionnaire flow. Dual-path: legacy doc ingestion + native creation. |
| **050** | Internal Marketplace (BU-to-BU) | Intra-company marketplace using same supply/demand constructs. Meeting 2026-03-13. |
| **051** | Reverse Bid Flow (Supply-Originated Proposals) | Suppliers propose projects to demand side within existing engagements. Meeting 2026-03-13. |
| **AI Agent RFP Assistant** | Claude Agent SDK-powered conversational RFP creation + bid evaluation for non-technical buyers. | [Proposal 003](../../proposals/003-ai-agent-rfp-assistant.md) |
| **Agentic Optimization** | AI execution scoring, drift detection, policy-aware optimization, admin AI health panel. | [Notes](../../notes/agentic-optimization-concepts.md) |
| **Org Switcher (user menu)** | Org-switcher dropdown in the user menu, mirroring `zb/ui` portal pattern. Calls `app.selectOrg(org)` which writes Dana cookie + sessionStorage via SDK. Resolves the recurring "dropped into last-in-list org" issue (SDK `selectDefaultOrg` fallback at `zerobias-client-app.ts:177` picks `orgs.at(-1)` when no cached org). First-class UX instead of DevTools snippets. Reference: `~/Projects/zb/com/ui/` portal user-menu component. | 2026-04-15 Clark |
| **SME-MART-PLATFORM-ADMIN-PAGE-1** | Internal SME Mart admin surface for listing/inspecting raw `platform.Project` + `platform.Board` resources, scoped to ops troubleshooting (orphan detection, manual fixes, provisioning audit). Build only if/when actual friction emerges that the existing engagement list + vetting view can't solve. **Lazy-evaluated — do not build preemptively.** Rationale: per D-45, SME Mart cannot gate verification or ops work on ZB internal UI delivery (ZB Governance UI does not currently surface our Projects). If we ever need to see raw platform entities, build it as a SME Mart admin tab calling `platform.Project.list` + `platform.Board.list` via SDK. | 2026-05-11 Director (D-45 corollary) |
| **107** | GRC Finding Schema Conformance Spike | _(was BACKLOG-104 — reassigned 2026-05-18 to resolve ID collision with Phase 24 Plan 04 deferral; the `#104` cross-ref in BACKLOG-105 was updated to `#107`.)_ Evaluate adopting [`finding.schema.json`](https://github.com/GRCEngClub/claude-grc-engineering/blob/main/schemas/finding.schema.json) from `GRCEngClub/claude-grc-engineering` as the canonical shape for engagement deliverables (one resource × N control evaluations). SCF is already ZB's rosetta-stone crosswalk and ZB exposes SCF as a framework, so the `control_framework`/`control_id` backbone aligns out of the box. Goal: SMEs hand work back as portable, auditable Findings that downstream buyers' auditors can ingest without rework — and SME Mart becomes interoperable with the GRC Engineering plugin ecosystem (gap-assessment, OSCAL, evidence packages). Spike: (1) compare current/anticipated deliverable shape against schema v1.0.0, (2) identify gaps + extension points (run_id, source_version, evidence_refs, status vs severity), (3) decide: conform, profile/extend, or reference-only. SCF API: `https://grcengclub.github.io/scf-api/` (CC BY-ND, 1,468 controls × 249 frameworks). | 2026-05-15 Clark — claude-grc-engineering review |
| **105** | GRC Toolkit Interop Exploration (Vendors / Credentials / SCF / OSCAL) | Broader interop with the [`claude-grc-engineering`](https://github.com/GRCEngClub/claude-grc-engineering) ecosystem, beyond just the Finding schema (#107). Investigate: (a) compare their open `vendor.schema.json`, `exception.schema.json`, `metric.schema.json`, `policy.schema.json`, `risk.schema.json` against ZB `vendor` schema package + SME Mart entities — gaps either direction. (b) Catalog SME provider credentials / vendor records via the SME Mart credentials schema in a form interoperable with their Vendor shape (and potentially feed-able back to the ZB `vendor` schema repo). (c) Map SME framework expertise to SCF control-family / framework IDs for capability-based provider matching. (d) Track their `oscal` plugin and `fedramp-ssp` workflows — if SME Mart ever hosts FedRAMP-engagement deliverables, OSCAL SSP/SAP/SAR/POA&M is the lingua franca. (e) Plugin marketplace pattern (`.claude-plugin/marketplace.json`) and their connector-quality bar (idempotent setup, exit-code conventions, run-manifest log) as reference if SME Mart or ZB ship first-party Claude Code plugins or want a sharper hub-module quality checklist. (f) Persona plugin model (`grc-auditor`, `grc-internal`, `grc-tprm`, `teach-me`) maps onto SME Mart buyer/provider/onboarding roles. | 2026-05-15 Clark — claude-grc-engineering review |

## Future Backlog (Gap Analysis Leftovers)

Items deferred from the RFP gap analysis — revisit when Phase 1/2 plans are complete.

| # | Gap | Priority | Notes |
|---|-----|----------|-------|
| D8 | Bid/performance bond management | Low | — |
| D9 | GenAI disclosure tracking | Low | — |
| S6 | Legal attestation forms (notarized affidavits, non-discrimination) | Medium | — |
| E6 | Multi-year contract lifecycle management | Low | Renewal notifications, extension options |
| P1 | Vendor conference scheduling with calendar integration | Low | — |
| P3 | Site visit coordination through platform | Low | — |
| CLEANUP-25 | Phase 25 audit residue cleanup — `markDeleted` for `mpi-test-a-cd7105df`, `mpi-test-b-cd7105df` (MarketplaceProfileItem class `7bcf86a5-91dc-520d-b9bf-e308b1078d46`) and `64047b6c-52e7-4592-ac1d-27f5020d1e01` TAG-SHAPE-TEST-C (SmeMartProject class `c66114a2-48e2-5b93-b7d6-7ccd6ef45a03`) | Low | Pipeline.receive requires non-empty `data`; piggyback on next real ingest per class. Pipeline `43f08afd-7ab9-4e99-a93c-619c46adaabe`. |

## Modernization Cleanup (filed by Phase 27.5 — Modernization Rule Enforcement)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| MODERN-CLEANUP-1 | Reconcile pre-existing modernization violations (touch-it-fix-it) | Medium | **Inventory of record:** `.planning/phases/27.5-modernization-enforcement/INITIAL-AUDIT.md` (snapshot from Phase 27.5 Plan 04, dated 2026-05-01: 796 rule violations across 15 rules + 765 fatal parse errors from `parserOptions.project` config noise; per-rule counts and top-10 violating files captured). **Cleanup model:** *touch it = fix it* — when a file under `src/app/` is modified for any reason, every modernization-rule violation in that file is fixed as part of the same change. Plan 03's CI lint gate is diff-based, so modified files are guaranteed to be linted; the developer cannot avoid the rule. **No dedicated sweep, no `eslint-disable-next-line` annotations across the codebase** (rejected 2026-05-01 — would be ~15-20 hrs of churn for files that CI doesn't even check, plus a perpetual maintenance tax on every cleanup PR). **Closure:** MODERN-CLEANUP-1 closes when a re-run of `npx eslint . --format json` against the full repo reports zero rule violations (the 765 fatal parse errors are tracked separately as a follow-up to add `.angular-docs/**` to ESLint ignores and to wire `tsconfig.spec.json` into the parser config). **Re-audit cadence:** none scheduled; re-run the audit on demand when a milestone wants visibility into remaining work. **Pairs with:** MODERN-CLEANUP-2 (mat-spinner enforcement) + MODERN-CLEANUP-3 (theme-aware SCSS). The "touch it = fix it" rule itself is documented in `CLAUDE.md` and `.planning/docs/MODERNIZATION_GUIDE.md` (Plan 05). |
| MODERN-CLEANUP-2 | Pattern 6 — `<mat-spinner>` deprecated alias ban | Low | Phase 27.5 Plan 01 deferred this pattern; `@angular-eslint/eslint-plugin-template@21.3.1` ships no rule capable of banning a specific element name (no `no-restricted-syntax`, no `no-deprecated-element`, no equivalent). Existing handoff text-rule remains in force until enforcement lands. **Two implementation options to evaluate:** (1) base ESLint `no-restricted-syntax` against template AST `Element$1[name='mat-spinner']` — untested, ~30 min spike to verify selector resolves against angular-eslint template parser output; (2) tiny local ESLint plugin exposing `template/no-mat-spinner-alias` — ~45 min, deterministic, adds maintenance surface. Existing `<mat-spinner>` instances are inventoried in `.planning/phases/27.5-modernization-enforcement/INITIAL-AUDIT.md` (Plan 04 snapshot) — that's the verified target list when the rule is implemented. |
| MODERN-CLEANUP-FORM-BUILDER-1 | Touch-It-Fix-It targets — 5 shared components with known pending violations | Medium | **Files (22 lint errors total, snapshot 2026-05-11):** `shared/components/form-builder/dynamic-form-renderer.component.ts` (5: 1 CommonModule + 4 `any`), `shared/components/form-builder/form-builder.component.ts` (2: 1 CommonModule + 1 unused `FormArray` import), `shared/components/form-builder/form-field-editor.component.ts` (1: CommonModule), `shared/components/org-document-chooser/org-document-chooser.component.ts` (5: all `any`), `shared/components/timeline-filters/timeline-filters.component.ts` (9: 1 ViewChild + 6 `@Input()` + 2 `@Output()` legacy decorators + 1 `any`). **Origin:** lint-staged auto-fix from a prior session added `readonly` modifiers (input/output/signal/computed) to these files, but the commit was blocked by the 22 pre-existing errors per Touch-It-Fix-It. The `readonly` drift was discarded 2026-05-11; lint-staged will re-apply it (and force the full 22-error fix) when someone next legitimately touches any of these files for real work. **Estimated cleanup time:** ~30-60 min focused (heaviest cost is `timeline-filters` legacy-decorator migration to signal-based `input()` / `output()` / `viewChild()`). **Cadence:** per MODERN-CLEANUP-1 model, no dedicated sweep; organic when these files next get touched for feature work. |
| MODERN-CLEANUP-4 | Lint rule — ban hex literals in component SCSS / inline templates | Medium | **Origin:** Director ask 2026-05-12 during Phase 30 UI-SPEC review. Companion enforcement layer for MODERN-CLEANUP-3 (theme-aware SCSS migration). Phase 27.5's deferral condition (wait until 27.5 closes) is now met — 27.5 is fully closed. **What:** add a lint rule that fires on raw hex color literals (`#[0-9a-fA-F]{3,8}`) appearing in `color:` / `background:` / `background-color:` / `border:` / `border-color:` / `fill:` / `stroke:` / `box-shadow:` declarations inside `src/app/**/*.component.scss` and inside `style="..."` attributes in `*.component.html`. **Why:** even with the UI-SPEC Theme Awareness Directive in place, a missed hex literal slips by review unless a machine catches it. ZbThemeService theme-swap depends on `var(--mat-sys-*)` / `var(--zb-*)` resolution; hex literals break that contract silently (no runtime error, no test failure — just wrong colors when theme changes). **Two implementation options to evaluate:** (1) **stylelint** with `color-no-hex` + a custom plugin for the inline-template case (~1 hr to wire; mature ecosystem; new lint surface to maintain); (2) **eslint custom rule** consuming postcss for the SCSS portion + angular-eslint template selector for inline `style="..."` (~3 hr; keeps single-toolchain story; ESLint already wired in CI gate). **Allowlist:** DESIGN.md token-reference comments are fine (markdown, not selectors); Stitch prompt files are fine (not lintable); status-chip fixed palette (`#e9e9e9`/`#d7e0ee`/`#d8ecba`/`#eed5d1`/`#0f0f10`) is the only known legitimate exception — encode as a per-file `:disable` comment in the one component that owns chip rendering. **Pre-flight:** snapshot count of existing violations via `grep -rEn "(color\|background\|fill\|stroke).*#[0-9a-fA-F]{3,8}" src/app/**/*.scss src/app/**/*.html` so MODERN-CLEANUP-3 inherits a baseline. **Cadence:** pair with MODERN-CLEANUP-3 (cleanup first, then enforcement — or land enforcement first and let touch-it-fix-it organically reduce the count; Director's call when the next UI-touching phase lands). **Effort:** ~2-3 hr for rule + tests; cleanup work tracked separately under MODERN-CLEANUP-3. |
| MODERN-CLEANUP-3 | Theme-aware SCSS — replace hardcoded colors with Material / ngx-library CSS vars | Medium | **Origin:** Director ask 2026-05-01. SME Mart components use a mix of hardcoded color values, partial Material theme integration, and ad-hoc SCSS variables. zb/ui (`~/Projects/zb/com/ui`) is the reference: `portal/src/app/portal/components/portal-app-nav/portal-app-nav.component.scss` shows the canonical pattern — `var(--zb-primary)`, `var(--zb-background-card)`, `var(--zb-text)`, `var(--zb-text-on-dark-bg)`, `rgba(var(--zb-primary-rgb, ...), 0.1)` for transparency overlays. **Targets to audit:** card backgrounds (`mat-card`, custom card components, `zb-simple-panel`); container/surface backgrounds; button colors (primary/accent/warn variants — should use Material's `--mat-sys-primary` / `--mat-sys-on-primary` or zb wrappers); chip backgrounds (cross-reference ZB task-status chip colors already pinned in CLAUDE.md notes); text colors on hover/active/disabled states; borders + dividers. **Output:** every SCSS file is theme-aware; light/dark theme switching works without per-component rules. **Anti-patterns to flag and remove:** hex literals (`#0f0f10`, `#e9e9e9`), CSS color names (`red`, `blue`), Material color palette imports as direct values (`mat.$blue-palette`), inline `style="color: ..."` bindings. **Scope estimate:** sweep over `src/app/**/*.scss` (~80 files at last count); ~6–10 hrs depending on density. **Cadence:** can ship as a focused milestone OR be folded into the next UI-touch phase. Either way, encode the rule in `MODERNIZATION_GUIDE.md` so it's in the handoff block alongside the other patterns once enforcement lands. **Possible future enforcement (not now):** stylelint config banning hex literals + named colors in component SCSS; defer until Phase 27.5 closes and we have appetite for adding more lint surface. |

## Plan Verification Hygiene

| # | Item | Priority | Notes |
|---|------|----------|-------|
| PLAN-VERIFY-SCOPE-ALIGN-1 | Align plan verification commands with `.lintstagedrc.json` scope | Low | **Origin:** Wave 3 / Plan 30-05 deviation 2026-05-13. Plan Task 3 step 3 had `npx eslint src/app/pages/default-project-board/* ... --max-warnings=0` — the directory glob pulled in `.scss` files, which ESLint can't parse (no SCSS parser config) → "no matching configuration" warnings → exit 1 under `--max-warnings=0`. Executor correctly substituted `.ts` + `.html` only (the lintable subset matching `.lintstagedrc.json`'s `*.{ts,html}` scope). **Fix:** update the plan-authoring templates (or planner agent / discuss-phase brief) to scope ESLint commands to `*.{ts,html}` only — never use directory globs for ESLint verification because they pull in `.scss`, `.json`, `.spec.ts`, etc. that the project's lint surface isn't configured for. Likely lives in the `gsd-planner` prompt template or in `MODERNIZATION_GUIDE.md` as a "verification command authoring rule". **Effort:** 15-30 min to update template + verify next phase's planner output. **Why low priority:** workaround is mechanical at execute time; not blocking. **Cross-reference:** documented in `.planning/phases/30-default-project-board-coming-soon-placeholders/30-05-SUMMARY.md` "Side-Channel Findings". |

## CI / Deploy Hygiene (filed by Phase 27.5 Plan 03 + Director directives)

| # | Item | Priority | Notes |
|---|------|----------|-------|
| DEV-CI-PURGE-1 | Strip 3P-app `dev`/`ci` deploy references from sme-mart | High | **Origin:** Director directive 2026-05-01. 3P customer apps in `zerobias-org/app` deploy ONLY to `uat`, `qa`, `prod`. `dev` and `ci` are valid ZB **platform** environments (ZB itself runs there) but are NOT deploy targets for any 3P customer app in this repo. **Audit + strip the deploy-implying surface:** branch→env mappings citing `dev` branch → `dev.zerobias.com` (in app-root `CLAUDE.md` and similar docs), `build:dev`/`build:ci` production-build script variants in `package.json`, GitHub Actions deploy workflows targeting dev/ci, any cross-fork PR target documentation listing dev/ci, `environment.dev.ts` / `environment.ci.ts` if present. **KEEP intact:** local dev-server scripts (`ng serve`-based) that target ZB platform's dev/ci/uat backends for local-machine testing — those are local dev against upstream platform envs, not app deployments. **Scope:** mostly mechanical doc + script sweep. **Encoded permanently in CLAUDE.md** "Deployment Paths" section (locked 2026-05-01). |
| CI-BUILD-GATE-1 | CI build-step gate (deferred from Phase 27.5 Plan 03) | Medium | **Origin:** Phase 27.5 Plan 03 omitted the build step from `test-and-lint.yml` because two prerequisites are unmet: (1) all `build:*` scripts gate on `.env.local` via `prebuild:*` env loaders that reference `NEON_DATABASE_URL`; CI has no `.env.local`; (2) `environment.neon.ts` is committed-but-gitignored (errata 015) — any built artifact would propagate the leaked secret. ENF-05's intent (lint as a CI gate) is satisfied by lint alone. **Required before this lands:** solve `.env.local` injection (GitHub Actions secrets → runtime env file in workflow) AND solve `environment.neon.ts` secret-in-tracked-file (errata 015 resolution — likely move secret to GH secrets + scrub history OR rotate Neon credentials). After both prereqs land, add a `build` job to `test-and-lint.yml` that depends on the lint job. |
| CI-LINT-INSTALL-1 | Replace `${RUNNER_TEMP}/lint-tools` symlink workaround in `lint.yml` | Medium | **Origin:** Phase 27.5 Plan 03 (commit `eb7adf3`). Workspace `.npmrc` forces private-registry resolution (`@zerobias-org` → `pkg.zerobias.org`, `@zerobias-com` → `npm.pkg.github.com`); the fork's CI lacks both auth tokens, so `npm install` of public-only lint tooling fails with E401. Current workaround: `npm install --prefix ${RUNNER_TEMP}/lint-tools` then symlink `node_modules` into the workspace. Functional but fragile — adds ~10 lines of bash to the workflow, breaks debuggability of "where did this dep come from", and any future `.npmrc` change risks breaking the install path silently. **Two cleaner approaches to evaluate:** (1) sibling `.ci-lint/package.json` with own restricted-scope `.npmrc` (no private registries) — installable directly without prefix gymnastics; (2) scope the workspace `.npmrc` to skip auth on `--no-save` installs (likely needs an `npm config` change in the workflow before install). **Effort:** ~1-2 hr spike to evaluate both options + land the chosen one. **Cadence:** low-priority — current workaround is operational; revisit when next adding/removing a lint dep or before any auth-related CI debugging session. |
| PRECOMMIT-TSC-GATE-1 | Add `tsc -p tsconfig.app.json && tsc -p tsconfig.spec.json` to pre-commit hook | **✅ DONE 2026-05-13** | **✅ DONE 2026-05-13.** Landed in `.husky/pre-commit` at app repo root. G1(a)/G2(a)/G3(a): always-on for SME Mart commits, both configs, sequential `&&`. Hook also gained `set -e` to prevent the trailing-tsc lines from masking earlier lint-staged failures (latent bug found mid-test). Six-pass test cycle: (1) outside-SME-Mart short-circuits exit 0; (2) SME Mart md-only commit fires tsc + passes 6.2s; (4) app-tsc break (TS2322 type mismatch) blocks commit exit 2 — ESLint passed clean, proving the gap; (5) spec-tsc break (`Partial<T>` mock-collapse pattern from Phase 30 Wave 2 — TS18048 + TS2339) blocks commit exit 2 — same gap pattern; (6) clean TS commit worst-case 8.95s. Cost ~9 sec wall-clock above the 3-7 sec backlog estimate but acceptable for the structural protection. **Originally filed:** Errata 028 (Phase 24 Wave 2, 2026-05-05) — 5th occurrence of the verification-gate-skipping pattern (errata 026, 027 same root cause, different surface). Each cycle: agent reports "tsc clean" without actually running both configs; defect ships; Director fix-loops; new errata; repeat. Adding more directive text to handoffs had reached diminishing returns — gate-skipping was a structural problem, not an instruction problem. **The fix landed:** the gate is now impossible to skip via the hook; defect literally cannot reach `git push` if either config fails. |
| VERCEL-PURGE-1 | Remove Vercel infrastructure entirely — DEFUNCT | Medium | **Status: DEAD CODE.** Vercel publishing is no longer used. Any code, config, env files, scripts, or docs referencing Vercel are defunct and must NOT be "fixed to work right" — they must be deleted. **Surface to remove:** (a) `src/environments/environment.vercel.ts` + `vercel` config in `angular.json` `fileReplacements` + `build:vercel` npm script + `prebuild:vercel` hook; (b) `proxy-vercel.*` if any; (c) `vercel.json` / `.vercel/` directories at repo root if they exist; (d) Vercel-Edge-Middleware files referenced in CLAUDE.md "Vercel Deployment (Temporary)" section; (e) the entire CLAUDE.md "Vercel Deployment (Temporary)" section; (f) Vercel env-var documentation in `.env.local.example` if any; (g) any `@vercel/*` packages in `package.json`. **Effort:** ~1-2 hr mechanical sweep. **Filed 2026-05-07:** Director directive — Vercel is dead. Watch list: anytime an agent encounters Vercel during normal work, treat it as defunct/dead code, do NOT attempt to make it work, and roll the cleanup into this entry rather than perpetuating. |
| PERMS-AUDIT-1 | Audit + refactor SME Mart permission code to use `hydra` role-search APIs | Medium | **Origin:** 2026-05-07 director investigation surfaced that SME Mart's only admin check uses the older `clientApi.danaClient.getOrgApi().getRequestOrgMember(userId).admin` flag (`onboarding.guard.ts:94-95`). The platform exposes richer role-search APIs (`hydra.Principal.searchRolesByPrincipal`, `hydra.Role.searchRoles`, `hydra.Org.searchOrgMembers`) that return direct AND transitive (via-Group) role memberships, with system roles `Organization Admin`, `Boundary Admin`, `Boundary Read-Only`, etc. already covering SME Mart's needs. **Reference:** [`.planning/notes/zb-permissions-reference.md`](.planning/notes/zb-permissions-reference.md) — read FIRST before doing any permission work. **Audit scope:** (1) Find every callsite that determines user permissions / admin status / role membership across SME Mart code (services, guards, components). Today's only known producer is `onboarding.guard.ts`; verify nothing else is independently checking. (2) For each callsite, classify: org-level admin? boundary-level? engagement-level? finer-grained? (3) Plan migration to the appropriate `searchRolesByPrincipal` / `searchOrgMembers` / `searchRoles` recipe per [`zb-permissions-reference.md`](.planning/notes/zb-permissions-reference.md) "API recipes" section. **Refactor scope (likely small):** (a) `onboarding.guard.ts` admin-detection swap (single function); (b) potentially expand `ProjectContextService.setIsAdmin` into multi-dimensional `setIsOrgAdmin` / `setIsBoundaryAdmin(boundaryId)` if boundary-level checks land in the same milestone; (c) align `DemoModeService` email-allowlist with the unified admin signal (or split out as a separate cleanup, see parkit "demo toggle alignment" item). **Tests:** add unit tests verifying both direct-binding and via-Group transitive admin detection (the new APIs handle this; verify our code consumes correctly). **Out of scope here:** introducing new custom roles — the system roles cover known cases. **Effort:** ~4–8 hrs incl. audit + small refactor + tests. **Filed 2026-05-07.** |

## Platform Alignment

| Item | Priority | Status | Notes |
|------|----------|--------|-------|
| GQL Schema Package | High | **Live** (17 classes on UAT + Prod) | Plan 034. RfpInvitation + isInvitationOnly pending (Phase 14). |
| Task-backed Reviews | Medium | Planned | Reviews use ZB Task approval workflow instead of custom status field |
| `@zerobias-org/types-core-js` — PagedResults\<T\> | Low | Evaluate | Replace custom pagination. Requires Node 22+ ESM — verify Angular 21 build compat. |
| Document / Service Offering types | Pending | Blocked on Kevin | Are these existing ZB types or defined via GQL schema extension? |
| **ZBUI-PROVISIONING-ACTION** — zb/ui governance app provisioning action | Medium | **Triggered by:** Nic ships Project/Workspace/Board features in zb platform | Add a "Provision Marketplace Engagement" action in the zb/ui governance app so platform admins can provision an org's platform engagement + default project without logging into sme-mart. Likely becomes a backend service call by then rather than a client-orchestrated 5-call recipe. Decided 2026-05-06 with Clark. |
| **ORG-SELF-PROVISIONING** — End-user-driven platform-engagement provisioning | Low | **Triggered by:** Ops decide to remove the manual-only gate | Add a "Provision Platform Engagement & Default Project" button in sme-mart, gated on a new platform role (e.g. "Marketplace Provisioning"). Visible only when org has no platform engagement AND user has the role. Pre-reqs to revisit at that time: (a) hydra tag-name uniqueness within scope (race protection), (b) backend health check before fire, (c) audit log of who provisioned what, (d) decide whether provisioning becomes a backend service vs client-orchestrated. Decided 2026-05-06 with Clark. |
| **ENGAGEMENT-BOUNDARY-SCOPE-REVISIT-1** — Revisit boundary scope on Engagement Project | Low | **Triggered by:** Compliance requirement surfaces (e.g., audit context requires the engagement itself to be boundary-bound, not just its descendants) | v1.4 provisions Engagement Project as **org-level** (`boundaryId` omitted). Project tier child also org-level. If compliance ever requires the Engagement Project to carry a specific `boundaryId` (e.g., to isolate cross-org commercial relationships from each other at the boundary layer), revisit the provisioner recipe to set `boundaryId` at Step C + cascade to children via D-47 platform enforcement. Decided 2026-05-12 with Clark during UAT walkthrough. |
| **D-49-NAMESPACE-MIGRATE-1** — Migrate provisioner.service.ts to D-49 namespace + dual-namespace probe | **✅ DONE 2026-05-13** | **✅ DONE 2026-05-13.** Landed in `platform-engagement-provisioner.service.ts`. `isOrgProvisioned` probes BOTH `sme-mart.engagement.*` (new) and `sme-mart.eng.*` (legacy) namespaces, unions tag matches, and verifies the Engagement Project actually exists (defends against orphan-tag false-positive — Brian's-Org case). Tag creation uses NEW namespace exclusively. Tests cover all 6 cases (new-with-project, new-orphan, legacy-with-project, legacy-orphan, neither, error). **Scope expanded by Director investigation 2026-05-13:** also fixed three latent SDK-shape bugs hidden by `as never` casts — see errata 036. (a) `Project.list({tagId, parentId})` silently ignored filters → switched to `list(undefined, 100, undefined, ownerId)` + client-side filter. (b) `Project.create` `ownerId` field silently dropped (NewProject DTO has no ownerId) → removed dead field; documented in code that owner is session-derived. (c) Create call used object-literal cast instead of `new NewProject(...)` constructor → switched to constructor with explicit field assignments. **Originally filed:** Errata 030 (Plan 06, 2026-05-12) — code probed legacy namespace; live UAT artifact used new namespace per D-49; `isOrgProvisioned()` returned false against the actual artifact. **Coexistence model:** legacy `sme-mart.eng.*` tags retain their UUIDs and names per D-43 anti-pattern (d) (no UUID-churn renames); they coexist harmlessly with new namespace creates. |
| **PROVISIONER-PROBE-PAGINATION-1** — Switch Project.list probes to pagination loop when D-46 multi-engagement state lands | Low | **Target: v1.5+ (when D-46 future state lands).** **Triggered by:** First org reaches >100 top-level platform Projects (likely after D-46 enables marketplace engagements with multiple counterparties per org) | `platform-engagement-provisioner.service.ts` uses `PROBE_PAGE_SIZE = 100` for all three probe sites (`isOrgProvisioned`, `ensureEngagementProject`, `ensureProjectTier`). Single-page assumption holds for v1.4 (one platform engagement per org). When D-46 multi-engagement state lands and an org accumulates >100 top-level Projects (marketplace engagements with vendors/auditors/etc.), the page-size becomes the wrong abstraction. **Fix:** wrap the `getProjectApi().list(...)` calls in a `pageToken`-driven loop that pages through all results before client-side filtering. Alternative: petition for a server-side `tagId` / `parentId` filter parameter on `ProjectApi.list` (would obviate client-side filtering entirely; see errata 036 (a) for the SDK-shape gap that necessitated the client-filter pattern). **Effort:** ~1-2 hrs (pagination loop) or N/A (server-side filter — depends on Kevin/platform). Filed 2026-05-13 alongside errata 036 closure. |
| **VETTING-PLATFORM-MIGRATE-1** — Migrate vetting items off GQL `EngagementVettingItem` → `platform.Task` + `platform.Board` lazy-create | Medium | **Target: v1.5+.** **Triggered by:** Vetting modernization gets prioritized | Current `vetting.service.ts` rides legacy GQL `EngagementVettingItem` via `PipelineWriteService` (fire-and-forget). D-12's vetting `platform.Board` lazy-create pattern (`boardType="list"`, `isDefault=false`) is forward-looking and was never implemented. Plan 07 was sized for this architecture and obsoleted by Plan 02 v3 amendment (which surgically excluded vetting modernization). Scope: (a) Migrate vetting items → `platform.Task` with `parentTaskId` for buyer/provider subtasks. (b) Introduce vetting `platform.Board` lazy-create per D-12. Supersedes Plan 08's conditional `[VETTING-BOARD-RENDERING]` entry (broader scope than rendering). Decided 2026-05-12 (errata 031) — Option A: defer + backlog. Plan 07 marked PREMISE-OBSOLETE in CLOSURE.md. |
| **PROVIDER-MY-ENGAGEMENTS-1** — Swap provider engagement UI from GQL SmeMartProject reads to `platform.Project.list` | Medium | **Target: v1.5+ (organic).** **Triggered by:** Next time provider dashboard is touched, OR before dual-read window closes (Phase 31) | Provider dashboard currently reads from GQL `SmeMartProject` search. Phase 29.5 Plan 03 established the dual-read window + `platform.Project` SDK primary path (engagements.service.ts + sme-mart-project.service.ts both have it). The provider-side UI surfaces have not been swapped to the platform primary path yet — they continue on the GQL fallback. Scope: swap provider-side UI to primary `platform.Project.list` path; GQL `SmeMartProject` remains as fallback. Aligns with demo-visibility reconciliation from Plan 03. **Estimate:** ~3 hrs. Filed 2026-05-12 by Plan 08 closure. |
| **PROJECT-SVC-RENAME-1** — Rename `SmeMartProjectService` → `PlatformProjectService` + swap callers | Low | **Target: v1.5+ (organic).** **Triggered by:** Dual-read window closes (post-Phase 31) — name is misleading until then | Phase 29.5 Plan 03 flipped `SmeMartProjectService` implementation from `Pipeline.receive` (legacy GQL `SmeMartProject` writes) to `platform.Project` SDK as the primary path. Service NAME now misleads readers: it's no longer SmeMart-specific. Safe to rename once dual-read window closes — at that point the GQL fallback is gone and the platform-native semantics are unambiguous. Scope: rename service file, class, exports, all callers; update test names. Pure mechanical refactor. **Estimate:** ~2 hrs. Filed 2026-05-12 by Plan 08 closure. |
| **SCHEMA-RETIREMENT-DELETE-1** — Delete `SmeMartProject` + `EngagementVettingItem` schema classes (not just deprecation) | Medium | **Target: post-closure (after Phase 31).** **Blocked by:** Dual-read window closure | Phase 29.5 Plan 05 marked `SmeMartProject` + `EngagementVettingItem` `@Deprecated` on `zerobias-org/schema` (dual-read window: Phases 29.5–31). After data aging + final dual-read removal in Phase 31, the deprecated classes can be **deleted** from schema. Coordinate with Daniel Rojas (Content team). No app-side changes needed at delete time — dual-read fallback should already be removed in Phase 31. Sequencing: (1) Phase 31 closes dual-read fallback in app code; (2) wait for one release of safety margin; (3) open schema PR to delete classes. **Estimate:** ~1 hr coordination + PR. Filed 2026-05-12 by Plan 08 closure. |

---

## Completed Plans (Reference Only)

<details>
<summary>Click to expand — 29 completed plans</summary>

| # | Plan | Completed |
|---|------|-----------|
| 079 | My Organizations Refactor (three-tier org nav) | ✅ v1.1 Phase 7 |
| 080 | Project-Centric Boundary Model (Internal/External, parties) | ✅ v1.1 Phase 12 |
| 041 | Supply-Side Vendor Profile (6-section, pointers, pre-fill) | ✅ v1.1 Phases 8-11 |
| 077 | Pilot Projects (projectType, completion, promotion) | ✅ v1.2 Phase 13 |
| 059 | AuditgraphDB Migration (Neon → Pipeline + GQL) | ✅ v1.0 Phases 1-6 |
| 015 | Navigation & Taxonomy Restructuring | ✅ |
| 017 | Engagement Tasks Tab | ✅ |
| 018 | Engagement Activity Center (Timeline) | ✅ |
| 019 | Markdown Components (Milkdown Crepe + Renderer) | ✅ |
| 026 | Notes Feature | ✅ |
| 027 | OneNote-Style Togglable Column Layout | ✅ |
| 028 | Drag-and-Drop for Folders & Notes | ✅ |
| 029 | Hierarchical Tag Naming Convention | ✅ |
| 030 | SmeMartResource Abstraction Layer | ✅ |
| 031 | Document Upload to Engagement | ✅ |
| 032 | RFP Creation Wizard | ✅ |
| 033 (P1–4) | Vendor Bid Response Flow | ✅ (Phase 5 pending) |
| 035 | Engagement Tab Routes Refactor | ✅ |
| 036 | RFP / Engagement Route Split | ✅ |
| 037 | ZB Resource Tag Editor Component | ✅ |
| 038 | Document-Notes Cross-Linking | ✅ |
| 039 | Tag Prefix Migration | ✅ |
| 043 | Proposal-to-Bid Rename Migration | ✅ |
| 046 (P1–5,7) | Org-Level Document Management | ✅ (Phase 6/8/9 pending) |
| 048 | Notification Center | ✅ |
| 049 | Unit Testing Strategy | ✅ |
| 060 | ensureDefaultFolder Race Condition Fix | ✅ |
| 061 | Pipeline Write Cache | ✅ |
| 062 | Notebook Info Page | ✅ |
| 063 | Corporate Vetting Flow | ✅ |
| 075 | RFP → Project Refactor | ✅ |
| 052 (P1-3) | Playwright E2E Smoke Tests | ✅ 2026-04-09 (Phase 4 CI deferred) |

Cancelled/Superseded: 016, 020, 021, 023 (stub), 024 (stub), 044, 045

</details>

---

## Key Architecture Decisions

Carried forward from PLAN.md — these inform all future milestones.

| Decision | Detail |
|----------|--------|
| Engagement/Project hierarchy | Engagement = corp-to-corp wrapper. Project = scoped work. One Engagement → many Projects. |
| Terminology | RFP = Request for Project. Vendor response = Bid (NOT Proposal). |
| Bidirectional requirements | Both buyer AND vendor have requirements at Engagement and Project levels. |
| MSA flexibility | MSA can attach at Engagement level (umbrella) OR Project level (scoped). |
| RFP → Bid → Project (phased bloom) | RFP is lightweight storefront. Accepted Bid triggers Engagement + Project. AI decomposes docs into task tree. |
| Supply-side one-time profile | Vendors load corporate docs/D&B/banking once. Pre-fills for every engagement. |
| Task status lifecycle | pending → in_progress → awaiting_approval → completed |
| Three-sided visibility | Demand (requirements + status), Supply (execution + internal notes), Shared (audit trail) |
| Platform realization | Vendor → Org. Service offering → Boundary. Owner → Principal. Per Kevin. |
| SME Mart scope | Marketplace + Engagements. Project management = separate platform app (Kevin, 2026-03-25). |

---

## Scanner Watch (auto-updated by Slack AI scanner)

External research drops from `#ai-*` Slack channels, mapped to plans. Full analyses in `.planning/research/external/`. Scanner source: `~/.claude/slack-scanner/`.

| Plan # | External Research Drops |
|--------|-------------------------|
| 033 P5 | 2026-04-09-pattern-skills-vs-mcp, 2026-04-13-pattern-coralos-marketplace |
| 041 | 2026-04-13-ceo-miro-cross-engagement-audit-model |
| 064 | 2026-04-13-ceo-miro-cross-engagement-audit-model |
| 065 | 2026-04-10-tool-promptql, 2026-04-13-pattern-coralos-marketplace |
| 071 | 2026-04-13-ceo-miro-cross-engagement-audit-model |
| 074 | 2026-04-09-pattern-skills-vs-mcp |
| 078 | 2026-04-10-tool-promptql, 2026-04-13-ceo-miro-cross-engagement-audit-model |
| 080 | 2026-04-13-ceo-miro-cross-engagement-audit-model |
| CE1–CE6 | 2026-04-13-ceo-miro-cross-engagement-audit-model (origin) |

---

## Scanner Spikes

| # | Plan | Description | Est. | Source | Analysis |
|---|------|-------------|------|--------|----------|
| **999.1** | PromptQL Pattern Extraction for Plan 065 | Evaluate thread-per-resource, wiki-from-conversations, and permission-aware messaging patterns from PromptQL for engagement-scoped message center design | 4–6 hrs | spike from scanner | ~/.claude/slack-scanner/analysis/deep/promptql.md |

---

*Migrated from `.claude/plans/public/PLAN.md` on 2026-03-30. Old plan files archived to `.claude/plans-archive/`.*
